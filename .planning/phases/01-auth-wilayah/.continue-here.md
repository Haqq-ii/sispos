---
context: phase
phase: 01-auth-wilayah
task: UAT
status: blocked
last_updated: 2026-06-30T18:55:16.865Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Docker no-hot-reload after worktree merge — On Windows, nodemon does NOT pick up file changes from git worktree merges. ALWAYS run `docker compose restart sispos-backend sispos-frontend` after merging worktrees, before testing.
- [ ] CONSTRAINT: seed.demo.ts missing — Plan 00-04 was skipped. Demo accounts DO NOT EXIST (warga=0, kader=0, puskesmas=0). Must create and run seed before any UAT.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| No container restart after merge | After merging worktree branches into main, backend served Phase 0 code. Login/register returned 404 until restart. | blocking | Always run `docker compose restart sispos-backend` immediately after wave worktree cleanup |
| Skipped Plan 00-04 | seed.demo.ts never created; UAT login impossible with empty user tables. | blocking | Before starting Phase N UAT, verify Phase N-1 plans are ALL complete including seeds |

<current_state>
Phase 01 all 4 plans are CODED AND COMMITTED. UAT failed due to infrastructure gaps, not code bugs. Two minor bugs were fixed (VerifyOtpResponse type mismatch + maskedPhone→nomorPonselMasked). Phase is in `human_needed` state pending UAT approval.
</current_state>

<completed_work>
- 01-01: Auth backend scaffold (prev session) ✓
- 01-02 Task 1: login/refresh/logout + authMiddleware + Kader 10x PIN lock (commit b91ebc6) ✓
- 01-02 Task 2: LoginPage + RoleBadge + KaderLockScreen + ProtectedRoute + Axios 401 refresh (commit c3f3d37) ✓
- 01-03: Wilayah seed 1508 records + 4 GET /api/wilayah/* endpoints (commits 1eb76a2, f14b4bc) ✓
- 01-04 Task 1: register.schema.ts + useOtpCountdown + useWilayah + OtpInput (commit 8f7ede2) ✓
- 01-04 Task 2: WilayahSelect + RegisterPage + VerifikasiOtpPage + OnboardingLokasiPage + LokasiSelesaiPage + router (commit 47ffff0) ✓
- Bug fixes: VerifyOtpResponse data shape + nomorPonselMasked (commit e7722ec) ✓
- Wilayah data in DB: 1508 records ✓
- Schema migrated: all tables exist ✓
</completed_work>

<remaining_work>
1. IMMEDIATE: `docker compose restart sispos-backend` (Phase 1 routes not active — backend running Phase 0 code)
2. Plan 00-04: Create prisma/seed.demo.ts + prisma/seed.ts + run them (seed.demo.ts does NOT exist in repo)
3. Phase 01 UAT re-run (5 items in 01-UAT.md)
4. Mark Phase 01 complete → start Phase 02
</remaining_work>

<decisions_made>
- 01-02 continued from partial: Task 1 was already committed (b91ebc6), only Task 2 spawned
- 01-03 Task 2 (human checkpoint) deferred: wilayah endpoints only verifiable after backend restart
- Phase 01 UAT status NOT approved — infrastructure gaps, not code correctness
- Windows worktree file conflicts: use Node.js fs.unlinkSync (not Bash rm) due to path resolution differences
</decisions_made>

<blockers>
- Backend restart needed: `docker compose restart sispos-backend`
- seed.demo.ts missing: execute Plan 00-04 via `/gsd-execute-phase 00`
- Demo accounts empty: warga=0, kader=0, puskesmas=0
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-auth-wilayah/01-UAT.md` — 5 UAT items to verify after infrastructure fixed
2. `.planning/phases/00-infrastructure-setup/00-04-PLAN.md` — Plan to create seed.demo.ts and seed.ts

## Infrastructure State
- sispos-backend: UP (but running Phase 0 code — needs restart)
- sispos-frontend: UP
- sispos-db: UP, healthy — all tables migrated, wilayah=1508, warga/kader/puskesmas=0
- sispos-redis: UP, healthy
- sispos-nginx: UP, port 80 exposed

## Demo Credentials (after seed.demo.ts runs)
- Citizen: NIK `3471012345670001` / password `Demo1234!`
- Kader: HP `081234560001` / PIN `123456`
- Puskesmas: `demo@puskesmas-mergangsan.go.id` / password `Demo1234!`

<context>
All Phase 01 code is done and correct. The UAT failure is purely infrastructure: (1) backend serving stale code, (2) no demo accounts. Fix order: restart backend → run Plan 00-04 seeds → re-run UAT. After UAT passes, Phase 01 is complete and Phase 02 (posyandu module) can begin.
</context>

<next_action>
1. `docker compose restart sispos-backend`
2. `/gsd-execute-phase 00` to run Plan 00-04 (seed.demo.ts creation)
3. Re-run UAT from 01-UAT.md
</next_action>

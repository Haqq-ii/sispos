---
phase: 04-dashboard-dss-ai-chatbot
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/modules/users/users.service.ts
  - backend/src/modules/users/users.controller.ts
  - backend/src/modules/users/users.routes.ts
  - backend/src/app.ts
  - backend/tests/users.test.ts
  - frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx
  - frontend/src/router/index.tsx
autonomous: true
requirements:
  - KADER-MGT-01
must_haves:
  truths:
    - "GET /api/users/kader returns all kader scoped to the authenticated puskesmasId (KADER-MGT-01)"
    - "PATCH /api/users/kader/:id/unlock sets gagalLogin=0 and terkunciSampai=null in DB atomically"
    - "AuditLog with aksi='MASTER_OVERRULE', tabelTerkait='kader', userId=puskesmasId is written in the same prisma.$transaction"
    - "IDOR guard: unlockKader throws AKSES_DITOLAK when kader.posyandu.puskesmasId !== caller puskesmasId"
    - "Puskesmas user sees /puskesmas/pengguna with kader list and 'Buka Kunci' button for locked kaders"
    - "Users test file passes: IDOR test and AuditLog transaction test"
  artifacts:
    - path: "backend/src/modules/users/users.service.ts"
      provides: "getKaderList, unlockKader with IDOR guard and prisma.$transaction AuditLog"
      exports: ["getKaderList", "unlockKader", "KaderListItem"]
    - path: "backend/src/modules/users/users.routes.ts"
      provides: "usersRouter GET /kader PATCH /kader/:id/unlock"
      exports: ["usersRouter"]
    - path: "backend/tests/users.test.ts"
      provides: "IDOR and AuditLog unit tests"
      min_lines: 40
    - path: "frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx"
      provides: "Kader list with lock status and unlock button"
      min_lines: 60
  key_links:
    - from: "ManajemenPenggunaPage.tsx"
      to: "/api/users/kader/:id/unlock"
      via: "useMutation PATCH + queryClient.invalidateQueries(['users','kader'])"
      pattern: "kader.*unlock"
    - from: "unlockKader in users.service.ts"
      to: "prisma.$transaction"
      via: "atomic kader.update + auditLog.create with aksi='MASTER_OVERRULE'"
      pattern: "MASTER_OVERRULE"
---

<objective>
Backend users module implements kader management for Puskesmas: list kaders with lock status, and the master-overrule unlock endpoint that atomically resets the PIN-lock and writes an AuditLog. Frontend ManajemenPenggunaPage displays the kader list with an unlock button.

Purpose: Puskesmas can unblock a kader who has been locked out after 10 failed PIN attempts, fulfilling CLAUDE.md Â§Keamanan requirement for MASTER_OVERRULE.

Output:
- GET /api/users/kader â€” kader list scoped by puskesmasId from JWT
- PATCH /api/users/kader/:id/unlock â€” reset lock + AuditLog MASTER_OVERRULE in $transaction
- frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx â€” kader list UI with status badges
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md
@.planning/phases/04-dashboard-dss-ai-chatbot/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Users backend module + unit tests</name>
  <files>
    backend/src/modules/users/users.service.ts,
    backend/src/modules/users/users.controller.ts,
    backend/src/modules/users/users.routes.ts,
    backend/src/app.ts,
    backend/tests/users.test.ts
  </files>
  <read_first>
    - prisma/schema.prisma â€” Kader model (id, posyanduId, namaLengkap, nomorPonsel, isAktif, isKetua, gagalLogin, terkunciSampai) and AuditLog model (userId, userRole, aksi, tabelTerkait, recordId, dataSebelum, dataSesudah, ipAddress, userAgent)
    - backend/src/modules/growth/growth.service.ts â€” AuditLog write pattern inside prisma.$transaction
    - backend/src/app.ts â€” current state after plan 04-01 changes, to know exact registration pattern
    - backend/vitest.config.ts â€” test framework config (globals: false â€” must import describe/it/expect from vitest)
    - .planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md Â§Pattern 5 â€” unlockKader implementation
  </read_first>
  <action>
    STEP 1 â€” users.service.ts:

    Create backend/src/modules/users/users.service.ts. Export:

    Interface KaderListItem: { id: string; namaLengkap: string; nomorPonsel: string; isAktif: boolean; isKetua: boolean; gagalLogin: number; terkunciSampai: Date | null; posyandu: { id: string; namaPosyandu: string } }

    Function getKaderList(puskesmasId: string): Promise<KaderListItem[]>:
    - prisma.kader.findMany where posyandu.puskesmasId equals puskesmasId
    - Use: where: { posyandu: { puskesmasId } }
    - include: { posyandu: { select: { id: true, namaPosyandu: true } } }
    - orderBy: [{ posyandu: { namaPosyandu: 'asc' } }, { namaLengkap: 'asc' }]
    - Return the result array (Prisma types match KaderListItem)

    Function unlockKader(kaderId: string, puskesmasId: string, meta: { ip?: string; userAgent?: string }): Promise<void>:
    - Load kader: prisma.kader.findUnique where id=kaderId, include posyandu select { puskesmasId: true }
    - If not found: throw Object.assign(new Error('Kader tidak ditemukan'), { code: 'KADER_TIDAK_DITEMUKAN' })
    - IDOR guard: if kader.posyandu.puskesmasId !== puskesmasId: throw Object.assign(new Error('Akses ditolak'), { code: 'AKSES_DITOLAK' })
    - Store dataSebelum: { gagalLogin: kader.gagalLogin, terkunciSampai: kader.terkunciSampai }
    - In prisma.$transaction(async tx => ...):
        tx.kader.update({ where: { id: kaderId }, data: { gagalLogin: 0, terkunciSampai: null } })
        tx.auditLog.create({ data: { userId: puskesmasId, userRole: 'puskesmas', aksi: 'MASTER_OVERRULE', tabelTerkait: 'kader', recordId: kaderId, dataSebelum, dataSesudah: { gagalLogin: 0, terkunciSampai: null }, ipAddress: meta.ip ?? null, userAgent: meta.userAgent ?? null } })
    - Log with pino: logger.info({ kaderId, puskesmasId }, 'Master overrule: kader unlocked')

    STEP 2 â€” users.controller.ts:

    Create backend/src/modules/users/users.controller.ts. Import AuthRequest from auth.middleware.

    getKaderListHandler(req: AuthRequest, res: Response): void
    - puskesmasId = req.user!.userId
    - Await getKaderList(puskesmasId), respond 200 { success: true, data: result }
    - Catch: 500 { success: false, error: 'INTERNAL_ERROR', message: 'Gagal memuat daftar kader.' }

    unlockKaderHandler(req: AuthRequest, res: Response): void
    - kaderId = req.params.id
    - puskesmasId = req.user!.userId
    - meta = { ip: req.ip, userAgent: req.headers['user-agent'] as string | undefined }
    - Await unlockKader(kaderId, puskesmasId, meta)
    - Respond 200 { success: true, message: 'Kader berhasil dibuka kuncinya.' }
    - Catch: map { KADER_TIDAK_DITEMUKAN: 404, AKSES_DITOLAK: 403 } â†’ status; fallback 500

    STEP 3 â€” users.routes.ts:

    Create backend/src/modules/users/users.routes.ts. Export usersRouter (Router). Register:
      GET /kader â†’ authMiddleware, requireRole('puskesmas'), getKaderListHandler
      PATCH /kader/:id/unlock â†’ authMiddleware, requireRole('puskesmas'), unlockKaderHandler

    STEP 4 â€” app.ts registration:

    In backend/src/app.ts, add import for usersRouter and register:
      import { usersRouter } from './modules/users/users.routes'
      app.use('/api/users', usersRouter)

    Place after the rekapHarianRouter line and before the /api/sesi alias.

    STEP 5 â€” users.test.ts:

    Create backend/tests/users.test.ts. Import { describe, it, expect, vi, beforeEach } from 'vitest'. Use vi.mock to mock '../../src/config/db' and '../../src/config/env'.

    Mock prisma with:
    - prisma.kader.findUnique: vi.fn()
    - prisma.kader.findMany: vi.fn()
    - prisma.$transaction: vi.fn()
    - prisma.auditLog.create: vi.fn()

    Test suite 1 â€” getKaderList:
    - Test: calls prisma.kader.findMany with correct where clause including posyandu.puskesmasId filter

    Test suite 2 â€” unlockKader:
    - Test: throws AKSES_DITOLAK when kader.posyandu.puskesmasId !== caller puskesmasId (mock findUnique returns kader with posyandu.puskesmasId = 'other-id')
    - Test: calls prisma.$transaction when IDOR guard passes (mock findUnique returns kader with matching puskesmasId)
    - Test: throws KADER_TIDAK_DITEMUKAN when prisma.kader.findUnique returns null

    Note: Import path for service is '../../src/modules/users/users.service' (relative from backend/tests/).
    The mock path for db must be '../config/db' relative to the module under test, but vi.mock resolution is by the actual module path. Use vi.mock('../../src/config/db', ...) in the test file and vi.mock('../../src/config/env', () => ({ env: { NODE_ENV: 'test' } })).
  </action>
  <verify>
    <automated>cd backend && npx vitest run tests/users.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All users.test.ts tests pass (IDOR test, AuditLog transaction test, 404 test)
    - curl GET /api/users/kader with puskesmas JWT â†’ 200 with kader array
    - curl PATCH /api/users/kader/:id/unlock with puskesmas JWT â†’ 200 { success: true }
    - curl PATCH /api/users/kader/:id/unlock with citizen JWT â†’ 403 FORBIDDEN
  </done>
</task>

<task type="auto">
  <name>Task 2: ManajemenPenggunaPage + router update</name>
  <files>
    frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx,
    frontend/src/router/index.tsx
  </files>
  <read_first>
    - frontend/src/router/index.tsx â€” current puskesmas routes to know exact insertion point
    - frontend/src/pages/kader/meja/Meja5Page.tsx â€” useMutation + queryClient.invalidateQueries pattern (lines 1-50)
    - .planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md Â§Pattern 5 â€” Kader Unlock Button code example (unlock mutation + toast)
  </read_first>
  <action>
    STEP 1 â€” ManajemenPenggunaPage.tsx:

    Create frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx (the puskesmas/ subdirectory already exists from Phase 2).

    Pull Figma design context for frame 5:14204 (Manajemen Pengguna), 5:14838 (Reset PIN), 5:15180 (Blokir) from file key 4DIazKntakgAGXBDYefjbD before implementing visuals.

    Hook useKaderList(): useQuery({ queryKey: ['users', 'kader'], queryFn: () => apiClient.get('/users/kader').then(r => r.data.data as KaderListItem[]) }). Define KaderListItem interface locally: { id, namaLengkap, nomorPonsel, isAktif, isKetua, gagalLogin, terkunciSampai: string | null, posyandu: { id, namaPosyandu } }.

    Mutation useUnlockKader(): useMutation({ mutationFn: (kaderId: string) => apiClient.patch('/users/kader/${kaderId}/unlock').then(r => r.data), onSuccess: () => { void queryClient.invalidateQueries({ queryKey: ['users','kader'] }); toast({ description: 'Kader berhasil dibuka kuncinya.' }) }, onError: () => toast({ description: 'Gagal membuka kunci.', variant: 'destructive' }) }).

    Lock status helper: isLocked(kader: KaderListItem): boolean = kader.terkunciSampai !== null and new Date(kader.terkunciSampai) > new Date().

    UI layout:
    - Header bar: back button (Link to /puskesmas/dashboard with ArrowLeft icon), title "Manajemen Kader"
    - Loading state: simple "Memuat..." text or Skeleton component while useKaderList().isLoading
    - Empty state: "Belum ada kader terdaftar" when data is empty array
    - Kader list: map over data, each kader rendered as a card/row with:
        - namaLengkap (bold), nomorPonsel (small gray), posyandu.namaPosyandu (badge or italic)
        - Status badge: if isLocked(kader) â†’ red badge "Terkunci" + show "Buka Kunci" button; if kader.isKetua â†’ green badge "Ketua"; else green badge "Aktif"
        - "Buka Kunci" button: onClick calls unlockMutation.mutate(kader.id), disabled while unlockMutation.isPending, text changes to "Membuka..." while pending
        - gagalLogin counter shown as small text: "Percobaan gagal: N/10" (informational)

    Import apiClient from '@/lib/axios', useToast from '@/hooks/use-toast', useMutation/useQuery/useQueryClient from '@tanstack/react-query', Link from 'react-router-dom', ArrowLeft from 'lucide-react'.

    STEP 2 â€” router/index.tsx:

    In frontend/src/router/index.tsx:
    1. Add lazy import: const ManajemenPenggunaPage = lazy(() => import('@/pages/puskesmas/ManajemenPenggunaPage'))
    2. Add route inside the puskesmas protected routes section (after the /puskesmas/jadwal route, before the 404 route):
       path="/puskesmas/pengguna" with ProtectedRoute allowedRoles={['puskesmas']} wrapping ManajemenPenggunaPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript finds no errors in ManajemenPenggunaPage.tsx
    - Browser at http://localhost/puskesmas/pengguna shows kader list (initially the demo kader)
    - Demo kader row shows "Aktif" badge and no "Buka Kunci" button (since gagalLogin=0, terkunciSampai=null)
    - Clicking "Buka Kunci" on a locked kader shows toast "Kader berhasil dibuka kuncinya." and list refreshes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser â†’ Express API | puskesmasId from JWT only; kaderId from URL param (verified against puskesmasId in service) |
| prisma.$transaction | MASTER_OVERRULE audit write must be atomic with kader update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | unlockKader IDOR | mitigate | Service loads kader with posyandu.puskesmasId, compares to puskesmasId from JWT â€” throws AKSES_DITOLAK on mismatch |
| T-04-02-02 | Tampering | AuditLog atomicity | mitigate | prisma.$transaction wraps both kader.update and auditLog.create â€” either both succeed or both roll back |
| T-04-02-03 | Elevation of Privilege | PATCH /api/users/kader/:id/unlock | mitigate | requireRole('puskesmas') â€” citizen/kader JWT returns 403 |
| T-04-02-04 | Information Disclosure | GET /api/users/kader | mitigate | requireRole('puskesmas') + puskesmasId from JWT ensures only own kaders are returned |
</threat_model>

<verification>
1. cd backend && npx vitest run tests/users.test.ts â€” all tests pass
2. curl GET /api/users/kader (puskesmas token) â†’ 200 with array of kader including demo kader
3. curl PATCH /api/users/kader/<demo-kader-id>/unlock (puskesmas token) â†’ 200 { success: true }
4. Verify AuditLog written: docker compose exec backend npx prisma studio â†’ audit_log table shows MASTER_OVERRULE row
5. Browser: /puskesmas/pengguna loads without TS errors; "Buka Kunci" button visible on locked kaders
</verification>

<success_criteria>
- users.test.ts: 3 tests pass (IDOR, AuditLog transaction, 404 on missing kader)
- GET /api/users/kader returns kader scoped to puskesmasId with posyandu.namaPosyandu included
- PATCH /api/users/kader/:id/unlock sets DB fields and writes AuditLog in same $transaction
- ManajemenPenggunaPage renders at /puskesmas/pengguna with kader list and conditional lock status
- TypeScript: 0 new errors
</success_criteria>

<output>
Create .planning/phases/04-dashboard-dss-ai-chatbot/04-02-SUMMARY.md when done
</output>


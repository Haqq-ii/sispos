---
phase: 04-dashboard-dss-ai-chatbot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - backend/src/modules/dashboard/dashboard.service.ts
  - backend/src/modules/dashboard/dashboard.controller.ts
  - backend/src/modules/dashboard/dashboard.routes.ts
  - backend/src/app.ts
  - frontend/package.json
  - frontend/src/pages/PuskesmasDashboardPage.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - "GET /api/dashboard/stunting?bulan=YYYY-MM returns 200 with StuntingMapPoint array (DASH-01)"
    - "GET /api/dashboard/stats?bulan=YYYY-MM returns 200 with totalPemeriksaan and breakdown (DASH-01)"
    - "puskesmasId for both endpoints is taken from JWT (req.user!.userId), never from query params (IDOR guard)"
    - "PuskesmasDashboardPage renders MapContainer with CircleMarker per posyandu (DASH-01)"
    - "Month filter input updates queryKey without page reload — map markers re-render in place (DASH-02)"
    - "Posyandu model in schema.prisma has latitude Float? and longitude Float? after migration"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Posyandu model with lat/lng"
      contains: "latitude   Float?"
    - path: "backend/src/modules/dashboard/dashboard.service.ts"
      provides: "getStuntingMapData, getDashboardStats"
      exports: ["getStuntingMapData", "getDashboardStats", "StuntingMapPoint", "DashboardStats"]
    - path: "backend/src/modules/dashboard/dashboard.routes.ts"
      provides: "dashboardRouter GET /stunting GET /stats"
      exports: ["dashboardRouter"]
    - path: "frontend/src/pages/PuskesmasDashboardPage.tsx"
      provides: "Leaflet map with filter-bulan and stats cards"
      contains: "MapContainer"
  key_links:
    - from: "frontend/src/pages/PuskesmasDashboardPage.tsx"
      to: "/api/dashboard/stunting"
      via: "TanStack Query useQuery queryKey ['dashboard','stunting',bulan]"
      pattern: "dashboard.*stunting"
    - from: "backend/src/modules/dashboard/dashboard.service.ts"
      to: "prisma.posyandu.findMany"
      via: "nested include jadwal.slotSesi.antrian.pemeriksaan"
      pattern: "posyandu\\.findMany"
    - from: "backend/src/app.ts"
      to: "dashboardRouter"
      via: "app.use('/api/dashboard', dashboardRouter)"
      pattern: "dashboardRouter"
---

<objective>
Schema migration adds latitude/longitude to Posyandu. Backend dashboard module exposes two authenticated Puskesmas-scoped endpoints. Frontend replaces the placeholder PuskesmasDashboardPage with a real Leaflet map + stats cards + month filter.

Purpose: Enables Puskesmas to visualise stunting distribution across their posyandu set with colour-coded markers and filter by month without reloading.

Output:
- Database migration: add latitude Float? longitude Float? to posyandu table
- GET /api/dashboard/stunting?bulan=YYYY-MM — colored map data per posyandu
- GET /api/dashboard/stats?bulan=YYYY-MM — aggregate counts for stat cards
- PuskesmasDashboardPage with MapContainer (react-leaflet@4.2.1), CircleMarker, month filter
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + Dashboard backend module</name>
  <files>
    prisma/schema.prisma,
    backend/src/modules/dashboard/dashboard.service.ts,
    backend/src/modules/dashboard/dashboard.controller.ts,
    backend/src/modules/dashboard/dashboard.routes.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - prisma/schema.prisma — Posyandu model (line ~97-116): know all existing fields before adding lat/lng
    - backend/src/app.ts — current router registration block to know the exact import/use pattern
    - backend/src/shared/middleware/auth.middleware.ts — AuthRequest interface (req.user.userId)
    - backend/src/shared/middleware/require-role.middleware.ts — requireRole usage
    - .planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md §Pattern 6 — getStuntingMapData Prisma query logic
  </read_first>
  <action>
    STEP 1 — Schema migration (BLOCKING — do this before any other work in this task):

    In prisma/schema.prisma, add two fields to the Posyandu model immediately after the jamOperasional line and before createdAt:
      latitude   Float?
      longitude  Float?

    Then run the migration from inside the running backend container:
      docker compose exec backend npx prisma migrate dev --name add-posyandu-coordinates

    This command creates the SQL migration, applies it to the DB, and regenerates the Prisma client. After it completes, the prisma client inside the container knows about latitude/longitude on Posyandu.

    STEP 2 — dashboard.service.ts:

    Create backend/src/modules/dashboard/dashboard.service.ts. Define and export:

    Interface StuntingMapPoint: { posyanduId: string; namaPosyandu: string; kelurahan: string; lat: number; lng: number; total: number; breakdown: Record<string, number> }

    Interface DashboardStats: { totalPemeriksaan: number; totalBalita: number; breakdown: Record<string, number> }

    Function getStuntingMapData(puskesmasId: string, bulan: string): Promise<StuntingMapPoint[]>:
    - Parse bulan ('YYYY-MM') into startOfMonth (new Date(year, month-1, 1)) and endOfMonth (new Date(year, month, 0, 23, 59, 59))
    - Query prisma.posyandu.findMany where puskesmasId equals the passed puskesmasId (from JWT, not client), select id/namaPosyandu/kelurahan/latitude/longitude, nested include: jadwal (where tanggalPelaksanaan between start and end) > slotSesi > antrian (where statusAntrian 'selesai') > pemeriksaan (select statusGizi only)
    - Filter result to posyandu with latitude !== null and longitude !== null
    - Flatten the nested chain to get all pemeriksaan, build breakdown Record by statusGizi using reduce
    - Return mapped array of StuntingMapPoint

    Function getDashboardStats(puskesmasId: string, bulan: string): Promise<DashboardStats>:
    - Same date range from bulan
    - Reuse the same findMany query structure
    - Flatten all pemeriksaan, count total and breakdown
    - Count distinct wargaId from antrian in the period as totalBalita (use a separate prisma.antrian.findMany and reduce to unique wargaIds, or groupBy if available)
    - Return DashboardStats

    STEP 3 — dashboard.controller.ts:

    Create backend/src/modules/dashboard/dashboard.controller.ts. Define two async handler functions (not exported directly — used by routes):

    getStuntingMapHandler(req: AuthRequest, res: Response): void
    - Extract bulan from req.query.bulan as string; default to current month in YYYY-MM format if absent: new Date(Date.now() + 7*60*60*1000).toISOString().slice(0,7)
    - puskesmasId = req.user!.userId (NEVER from query params — IDOR guard per T-04-01-01)
    - Call getStuntingMapData(puskesmasId, bulan), respond 200 { success: true, data: result }
    - Catch: respond 500 { success: false, error: 'INTERNAL_ERROR', message: 'Gagal memuat data peta stunting.' }

    getDashboardStatsHandler(req: AuthRequest, res: Response): void
    - Same puskesmasId extraction from JWT
    - Same bulan extraction
    - Call getDashboardStats(puskesmasId, bulan), respond 200 { success: true, data: result }

    STEP 4 — dashboard.routes.ts:

    Create backend/src/modules/dashboard/dashboard.routes.ts. Export dashboardRouter (Router from express). Register:
      GET /stunting → authMiddleware, requireRole('puskesmas'), getStuntingMapHandler
      GET /stats → authMiddleware, requireRole('puskesmas'), getDashboardStatsHandler

    STEP 5 — app.ts registration:

    In backend/src/app.ts, add the import for dashboardRouter and register:
      import { dashboardRouter } from './modules/dashboard/dashboard.routes'
      app.use('/api/dashboard', dashboardRouter)

    Place the registration after the existing rekapHarianRouter line (before the 404 handler) to maintain consistency with the existing pattern.
  </action>
  <verify>
    <automated>docker compose exec backend npx prisma migrate status 2>&1 | grep "add_posyandu_coordinates"</automated>
  </verify>
  <done>
    - Migration "add_posyandu_coordinates" shows as "Applied" in prisma migrate status
    - GET /api/dashboard/stunting?bulan=2026-07 (with valid puskesmas JWT cookie) returns 200 { success: true, data: [] }
    - GET /api/dashboard/stats?bulan=2026-07 (with valid puskesmas JWT cookie) returns 200 { success: true, data: { totalPemeriksaan: N, totalBalita: N, breakdown: {} } }
    - GET /api/dashboard/stunting without auth returns 401
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend Leaflet install + PuskesmasDashboardPage</name>
  <files>
    frontend/package.json,
    frontend/src/pages/PuskesmasDashboardPage.tsx
  </files>
  <read_first>
    - frontend/package.json — current devDependencies and dependencies before modifying
    - frontend/src/pages/PuskesmasDashboardPage.tsx — current placeholder to be replaced
    - .planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md §Pattern 1 — Leaflet MapContainer+CircleMarker exact code pattern and critical pitfalls
    - .planning/phases/04-dashboard-dss-ai-chatbot/04-RESEARCH.md §Common Pitfalls — version mismatch, CSS import, MapContainer re-mount
    - frontend/src/router/index.tsx — confirm PuskesmasDashboardPage import path so no rename is needed
  </read_first>
  <action>
    STEP 1 — npm install (host, not Docker):

    From the project host, run inside the frontend directory:
      npm install react-leaflet@4.2.1 leaflet@1.9.4 @types/leaflet@1.9.21

    This updates frontend/package.json and frontend/package-lock.json.

    Then rebuild the frontend container so the image includes the new packages (the Dockerfile copies package*.json and runs npm install at build time; the node_modules volume mount does not exist):
      docker compose build sispos-frontend
      docker compose up -d sispos-frontend

    STEP 2 — Rewrite PuskesmasDashboardPage.tsx:

    Completely replace the placeholder at frontend/src/pages/PuskesmasDashboardPage.tsx with a real implementation. Key requirements:

    CSS import: Add import 'leaflet/dist/leaflet.css' at the very top of the file (before React import). This is required for Leaflet tile positioning. Since we use CircleMarker (no image markers), the L.Icon.Default fix is NOT needed.

    Month filter state: useState initialized to WIB-adjusted current month:
      useState(() => new Date(Date.now() + 7*60*60*1000).toISOString().slice(0, 7))
    Input type="month" bound to this state. Changing the input directly updates state — no reload needed.

    TanStack Query hooks (both scoped to bulan state so they refetch when filter changes):
    - usePuskesmasStunting(bulan): GET /api/dashboard/stunting with params { bulan }, queryKey ['dashboard','stunting',bulan], staleTime 5 minutes
    - usePuskesmasDashboardStats(bulan): GET /api/dashboard/stats with params { bulan }, queryKey ['dashboard','stats',bulan], staleTime 5 minutes

    getMarkerColor(breakdown: Record<string,number>): string function:
    - if (breakdown.buruk ?? 0) + (breakdown.sangat_pendek ?? 0) > 0: return '#ef4444' (merah)
    - else if (breakdown.kurang ?? 0) + (breakdown.pendek ?? 0) > 0: return '#f59e0b' (kuning)
    - else: return '#22c55e' (hijau)

    Layout:
    - Page header: "Dashboard Puskesmas" title, subtitle with selected month
    - Stats cards row (4 cards): Total Pemeriksaan, Total Balita, Gizi Normal (breakdown.normal ?? 0), Bermasalah ((breakdown.buruk ?? 0) + (breakdown.sangat_pendek ?? 0) + (breakdown.kurang ?? 0) + (breakdown.pendek ?? 0))
    - Filter bulan: input type="month" label "Filter Bulan" with Tailwind styling
    - MapContainer: center={[-7.7971, 110.3688]} (Yogyakarta), zoom={12}, style={{ height: '400px', width: '100%' }}, className="rounded-xl overflow-hidden"
    - TileLayer: url "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", attribution per OSM requirement
    - CircleMarker for each item in stuntingData (filter out items where lat or lng is 0/null): center={[item.lat, item.lng]}, radius={Math.max(8, Math.sqrt(item.total) * 3)}, pathOptions={{ color: getMarkerColor(item.breakdown), fillOpacity: 0.7 }}
    - Popup per CircleMarker: namaPosyandu, kelurahan, Total: item.total balita, then breakdown counts for normal/kurang/buruk
    - Loading state: show "Memuat peta..." text inside map area while isLoading
    - Navigation links: Link to /puskesmas/pengguna ("Manajemen Kader") and Link to /puskesmas/jadwal ("Manajemen Jadwal") in a quick-nav section

    CRITICAL — do NOT use a dynamic key prop on MapContainer. The MapContainer must remain mounted across re-renders; only the CircleMarker children re-render when bulan changes. Moving bulan state outside the MapContainer component tree prevents re-mount.

    Import note: react-leaflet types are imported from 'react-leaflet' (named exports). Leaflet types from 'leaflet' if needed (though CircleMarker usage rarely needs direct leaflet types).

    Pull Figma design context for Puskesmas dashboard (file key 4DIazKntakgAGXBDYefjbD, frame 5:13232) before implementing the visual layout to match the design. Adjust colours and positioning for consistency as permitted by CLAUDE.md.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TypeScript compiles without errors related to react-leaflet or PuskesmasDashboardPage
    - Browser at http://localhost/puskesmas/dashboard shows a real Leaflet map (not placeholder)
    - No "Map container is already initialized" console error
    - Changing month filter input updates the data without page reload (React refetch, not navigation)
    - Stats cards render (values may be 0 until seed data populates lat/lng)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express API | puskesmasId must ALWAYS come from JWT (req.user!.userId), never from client-supplied query params |
| npm install | New packages: react-leaflet@4.2.1, leaflet@1.9.4, @types/leaflet@1.9.21 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | dashboard.service.ts getStuntingMapData | mitigate | puskesmasId extracted from req.user!.userId (JWT) in both handlers; never from req.query.puskesmasId |
| T-04-01-02 | Information Disclosure | GET /api/dashboard/stunting | mitigate | authMiddleware + requireRole('puskesmas') on both routes |
| T-04-01-03 | Spoofing | getDashboardStats | mitigate | Same puskesmasId from JWT enforcement as T-04-01-01 |
| T-04-01-SC | Tampering | npm install react-leaflet, leaflet, @types/leaflet | accept | RESEARCH.md Package Legitimacy Audit: all three packages verified via npm registry + official homepages — leafletjs.com (mourner), react-leaflet.js.org (PaulLeCam). Disposition: accept (established packages, 10+ year history, millions of weekly downloads) |
</threat_model>

<verification>
1. docker compose exec backend npx prisma migrate status — "add_posyandu_coordinates" shows Applied
2. curl -s http://localhost/api/dashboard/stunting?bulan=2026-07 -H "Cookie: access_token=..." returns 200 with data array
3. curl -s http://localhost/api/dashboard/stunting -H "Cookie: access_token=<citizen_token>" returns 403 FORBIDDEN
4. Browser: http://localhost/puskesmas/dashboard — Leaflet map renders, no JS errors, month filter works
5. cd frontend && npx tsc --noEmit — 0 errors
</verification>

<success_criteria>
- Migration add-posyandu-coordinates is applied and Posyandu table has latitude/longitude columns
- GET /api/dashboard/stunting returns 200 array (empty until coordinates seeded, but structure correct)
- GET /api/dashboard/stats returns 200 with totalPemeriksaan, totalBalita, breakdown
- Puskesmas dashboard page renders Leaflet map (react-leaflet@4.2.1) with OSM tiles
- Month filter changes refetch data without page reload — queryKey includes bulan
- No react-leaflet version warnings or peer dependency errors in console
</success_criteria>

<output>
Create .planning/phases/04-dashboard-dss-ai-chatbot/04-01-SUMMARY.md when done
</output>

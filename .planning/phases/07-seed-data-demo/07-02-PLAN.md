---
phase: 07-seed-data-demo
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - prisma/seed.massal.ts
autonomous: true
requirements:
  - SEED-02

must_haves:
  truths:
    - "Database has 3 Puskesmas (including demo Puskesmas Mergangsan), 15 posyandu (5 per puskesmas)"
    - "Each posyandu has 25-35 balita — total >= 375 balita across all posyandu"
    - "Each balita has 12-15 months of pemeriksaan history with realistic BB/TB/Z-Score values per status gizi"
    - "Imunisasi dasar seeded per Kemenkes schedule for 70-80% of balita based on age"
    - "All posyandu have latitude/longitude for Leaflet map display"
    - "5 posyandu status-gizi groups represented: sehat, sedang, rawan, kritis, outlier (per D-08..D-12)"
    - "Posyandu Dahlia (kritis, kelurahan Wirogunan) serves as the dramatic stunting cluster (D-13)"
  artifacts:
    - path: "prisma/seed.massal.ts"
      provides: "Bulk data seed function with 3 puskesmas, 15 posyandu, 375+ balita, history, imunisasi"
      exports: ["seedMassal"]
      contains: "export async function seedMassal"
      min_lines: 200
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/seed.massal.ts"
      via: "import { seedMassal }"
      pattern: "seedMassal"
    - from: "prisma/seed.massal.ts"
      to: "prisma.posyandu"
      via: "findFirst + create loop"
      pattern: "posyandu.*findFirst"
---

<objective>
Create `prisma/seed.massal.ts` — the bulk data generator that populates the database with 3 Puskesmas, 15 posyandu (D-05, D-06), and 375-525 warga/balita (D-07) spread across all posyandu, plus 12-15 months of pemeriksaan history per balita (D-14) and imunisasi dasar based on age (D-18). Status gizi distribution varies per posyandu group (D-08..D-12) to power the Leaflet stunting map.

Purpose: Provide realistic bulk data for puskesmas dashboard statistics, stunting map cluster demo, and dummy antrian for today's kader session (consumed by Plan 07-03 per D-27).

Output: `prisma/seed.massal.ts` exporting `seedMassal(prisma: PrismaClient)`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seed-data-demo/07-CONTEXT.md
@.planning/phases/07-seed-data-demo/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed.massal.ts — Puskesmas, Posyandu, Warga, Balita</name>
  <files>prisma/seed.massal.ts</files>

  <read_first>
  - prisma/seed.demo.ts — full file: upsert pattern for Puskesmas, findFirst+create for Posyandu, upsert for Warga, findFirst+create for Balita
  - prisma/schema.prisma — lines 76-170: Puskesmas, Posyandu, Warga, Balita model fields and constraints
  - .planning/phases/07-seed-data-demo/07-CONTEXT.md — D-05 through D-17 (posyandu names, balita counts, distribution groups, Javanese names)
  - .planning/phases/07-seed-data-demo/07-PATTERNS.md — "bulk Warga + Balita loop pattern" section, "idempotency" section
  </read_first>

  <action>
  Create `prisma/seed.massal.ts`. File structure:

  **Imports:**
  - `import bcrypt from 'bcrypt'`
  - `import { PrismaClient } from '@prisma/client'`

  **Constants:**
  - `const BCRYPT_ROUNDS = 8` (faster than 10 for bulk; acceptable for demo data)

  **PUSKESMAS_DATA array** — 3 entries, each with: namaPuskesmas, email, alamat, nomorTelepon, wilayahKerja, passwordPlain:
  1. `{ namaPuskesmas: 'Puskesmas Mergangsan', email: 'demo@puskesmas-mergangsan.go.id', alamat: 'Jl. Kenari No. 10, Mergangsan, Kota Yogyakarta', nomorTelepon: '0274512345', wilayahKerja: 'Kecamatan Mergangsan', passwordPlain: 'Demo1234!' }`
  2. `{ namaPuskesmas: 'Puskesmas Gedongtengen', email: 'admin@puskesmas-gedongtengen.go.id', alamat: 'Jl. Tentara Pelajar No. 1, Gedongtengen, Kota Yogyakarta', nomorTelepon: '0274678901', wilayahKerja: 'Kecamatan Gedongtengen', passwordPlain: 'Massal1234!' }`
  3. `{ namaPuskesmas: 'Puskesmas Umbulharjo', email: 'admin@puskesmas-umbulharjo.go.id', alamat: 'Jl. Veteran No. 22, Umbulharjo, Kota Yogyakarta', nomorTelepon: '0274789012', wilayahKerja: 'Kecamatan Umbulharjo', passwordPlain: 'Massal1234!' }`

  **POSYANDU_DATA array** — 15 entries. Each entry: puskesmasEmail, namaPosyandu, kecamatan, kelurahan, rw, latitude, longitude, statusGroup, targetBalita.

  Mergangsan (puskesmasEmail: 'demo@puskesmas-mergangsan.go.id'):
  - `{ namaPosyandu:'Posyandu Mawar', kecamatan:'Mergangsan', kelurahan:'Wirogunan', rw:'003', latitude:-7.8160, longitude:110.3700, statusGroup:'rawan', targetBalita:28 }`
  - `{ namaPosyandu:'Posyandu Melati', kecamatan:'Mergangsan', kelurahan:'Brontokusuman', rw:'002', latitude:-7.8190, longitude:110.3680, statusGroup:'sehat', targetBalita:26 }`
  - `{ namaPosyandu:'Posyandu Anggrek', kecamatan:'Mergangsan', kelurahan:'Mergangsan', rw:'005', latitude:-7.8140, longitude:110.3720, statusGroup:'sedang', targetBalita:30 }`
  - `{ namaPosyandu:'Posyandu Dahlia', kecamatan:'Mergangsan', kelurahan:'Wirogunan', rw:'007', latitude:-7.8175, longitude:110.3710, statusGroup:'kritis', targetBalita:27 }` ← D-13 stunting cluster
  - `{ namaPosyandu:'Posyandu Kenanga', kecamatan:'Mergangsan', kelurahan:'Brontokusuman', rw:'009', latitude:-7.8200, longitude:110.3660, statusGroup:'outlier', targetBalita:25 }`

  Gedongtengen (puskesmasEmail: 'admin@puskesmas-gedongtengen.go.id'):
  - `{ namaPosyandu:'Posyandu Seruni', kecamatan:'Gedongtengen', kelurahan:'Pringgokusuman', rw:'001', latitude:-7.7920, longitude:110.3620, statusGroup:'sehat', targetBalita:28 }`
  - `{ namaPosyandu:'Posyandu Cempaka', kecamatan:'Gedongtengen', kelurahan:'Sosromenduran', rw:'003', latitude:-7.7935, longitude:110.3610, statusGroup:'sedang', targetBalita:30 }`
  - `{ namaPosyandu:'Posyandu Kamboja', kecamatan:'Gedongtengen', kelurahan:'Pringgokusuman', rw:'006', latitude:-7.7950, longitude:110.3630, statusGroup:'sedang', targetBalita:27 }`
  - `{ namaPosyandu:'Posyandu Aster', kecamatan:'Gedongtengen', kelurahan:'Sosromenduran', rw:'008', latitude:-7.7960, longitude:110.3640, statusGroup:'rawan', targetBalita:26 }`
  - `{ namaPosyandu:'Posyandu Bougenville', kecamatan:'Gedongtengen', kelurahan:'Pringgokusuman', rw:'011', latitude:-7.7975, longitude:110.3650, statusGroup:'kritis', targetBalita:28 }`

  Umbulharjo (puskesmasEmail: 'admin@puskesmas-umbulharjo.go.id'):
  - `{ namaPosyandu:'Posyandu Flamboyan', kecamatan:'Umbulharjo', kelurahan:'Muja Muju', rw:'002', latitude:-7.8050, longitude:110.3870, statusGroup:'sehat', targetBalita:28 }`
  - `{ namaPosyandu:'Posyandu Teratai', kecamatan:'Umbulharjo', kelurahan:'Tahunan', rw:'004', latitude:-7.8070, longitude:110.3890, statusGroup:'sedang', targetBalita:30 }`
  - `{ namaPosyandu:'Posyandu Wijayakusuma', kecamatan:'Umbulharjo', kelurahan:'Warungboto', rw:'003', latitude:-7.8090, longitude:110.3910, statusGroup:'rawan', targetBalita:27 }`
  - `{ namaPosyandu:'Posyandu Tulip', kecamatan:'Umbulharjo', kelurahan:'Sorosutan', rw:'006', latitude:-7.8110, longitude:110.3930, statusGroup:'kritis', targetBalita:26 }`
  - `{ namaPosyandu:'Posyandu Lavender', kecamatan:'Umbulharjo', kelurahan:'Pandeyan', rw:'008', latitude:-7.8130, longitude:110.3950, statusGroup:'outlier', targetBalita:25 }`

  **STATUS_GIZI_DIST map** — keys are group names, values are weighted arrays:
  ```
  sehat:   [{s:'normal',w:80},{s:'kurang',w:10},{s:'pendek',w:5},{s:'lebih',w:5}]
  sedang:  [{s:'normal',w:60},{s:'kurang',w:25},{s:'pendek',w:10},{s:'buruk',w:5}]
  rawan:   [{s:'normal',w:40},{s:'kurang',w:30},{s:'pendek',w:20},{s:'buruk',w:10}]
  kritis:  [{s:'normal',w:30},{s:'kurang',w:25},{s:'pendek',w:25},{s:'buruk',w:15},{s:'sangat_pendek',w:5}]
  outlier: [{s:'lebih',w:60},{s:'obesitas',w:25},{s:'normal',w:15}]
  ```

  **WARGA_IBU_NAMES array** (20 names): ['Siti Aminah','Dewi Lestari','Sri Wahyuni','Rini Handayani','Yuni Astuti','Eni Susanti','Wati Rahayu','Anik Suryani','Tutik Wulandari','Sari Utami','Endah Priyatni','Ning Raharti','Retno Widayati','Umi Kulsum','Puji Lestari','Hartini Soewondo','Kustiyah Broto','Lastri Mulyani','Sumarni Dwiputri','Niken Cahyani']

  **BALITA_NAMES_L array** (10 Javanese male names): ['Bima Prakoso','Aldi Susanto','Fajar Nugroho','Rizki Hidayat','Bayu Setiawan','Dimas Wicaksono','Agus Prasetyo','Hendra Gunawan','Wahyu Santoso','Teguh Kurniawan']

  **BALITA_NAMES_P array** (10 Javanese female names): ['Ayu Puspita','Rini Cahyanti','Dewi Kusuma','Sari Wulandari','Indah Permatasari','Bunga Rahayu','Tari Handayani','Laras Pertiwi','Citra Dewi','Melati Suci']

  **Helper functions:**
  - `pickWeighted(dist: {s:string, w:number}[], seed: number): string` — cumulative weight pick using `seed % 100` as the random value
  - `genNIK(posIdx: number, balIdx: number): string` — returns 16-digit NIK: `'3471' + String(posIdx).padStart(2,'0') + String(balIdx).padStart(4,'0') + String(posIdx * 100 + balIdx).padStart(6,'0')`
  - `genHP(posIdx: number, balIdx: number): string` — returns `'0812' + String(posIdx).padStart(3,'0') + String(balIdx).padStart(5,'0')`
  - `genTanggalLahirBalita(seed: number): Date` — returns a Date between 6 and 59 months before today; formula: `ageMonths = 6 + (seed % 54)`, `birthDate = today - ageMonths * 30 days`

  **Export function:**
  ```
  export async function seedMassal(prisma: PrismaClient): Promise<void>
  ```

  Inside `seedMassal`:
  1. Compute `todayStart` using WIB offset pattern from seed.today.ts:
     `const nowUtc = new Date(); const wibMs = 7*60*60*1000; const nowWib = new Date(nowUtc.getTime()+wibMs); const todayStart = new Date(Date.UTC(nowWib.getUTCFullYear(), nowWib.getUTCMonth(), nowWib.getUTCDate()))`

  2. Hash passwords once before loop: `const demoHash = await bcrypt.hash('Demo1234!', BCRYPT_ROUNDS); const massalHash = await bcrypt.hash('Warga1234!', BCRYPT_ROUNDS); const puskHash = await bcrypt.hash('Massal1234!', BCRYPT_ROUNDS)`

  3. Build puskesmasMap: for each entry in PUSKESMAS_DATA, upsert via `prisma.puskesmas.upsert({ where: { email }, update: { passwordHash }, create: { namaPuskesmas, email, passwordHash, alamat, nomorTelepon, wilayahKerja } })`. Store result in `Map<email, puskesmas>`.

  4. For each entry in POSYANDU_DATA:
     a. Look up puskesmas from map by puskesmasEmail
     b. `let posyandu = await prisma.posyandu.findFirst({ where: { puskesmasId: puskesmas.id, namaPosyandu: entry.namaPosyandu } })`
     c. If not found, create with all fields including latitude, longitude, provinsi:'DI Yogyakarta', kabupaten:'Kota Yogyakarta'
     d. Store posyandu in posyanduMap by namaPosyandu

  5. For each posyandu (using posIdx as loop index 0-14):
     For each balitaIdx from 0 to targetBalita-1:
     a. Compute NIK, HP using helpers
     b. Determine statusGizi via `pickWeighted(STATUS_GIZI_DIST[posyandu.statusGroup], posIdx * 100 + balitaIdx)`
     c. Compute tanggalLahir via `genTanggalLahirBalita(posIdx * 1000 + balitaIdx)`
     d. Upsert warga: `prisma.warga.upsert({ where: { nikIbu: nik }, update: {}, create: { nikIbu: nik, namaLengkap: WARGA_IBU_NAMES[balitaIdx % 20], nomorPonsel: hp, passwordHash: massalHash, statusVerifikasi: 'terverifikasi', provinsi:'DI Yogyakarta', kabupaten:'Kota Yogyakarta', kecamatan: posyandu.kecamatan, kelurahan: posyandu.kelurahan, rw: posyandu.rw, rt: String(1 + balitaIdx % 5).padStart(3,'0'), posyanduUtamaId: posyandu.id } })`
     e. Find or create balita: check `prisma.balita.findFirst({ where: { nikBalita: nik + '1' } })` (or `nikIbu`-derived), if absent create with: `nikBalita: nik.slice(0,15)+'9'`, `namaBalita: jenisKelamin==='laki_laki' ? BALITA_NAMES_L[balitaIdx%10] : BALITA_NAMES_P[balitaIdx%10]`, `tanggalLahir`, `jenisKelamin: balitaIdx%2===0 ? 'laki_laki' : 'perempuan'`
     f. Store (warga, balita, statusGizi, tanggalLahir, posIdx, balitaIdx) in a local array for Task 2 to use

  6. Console.log summary per posyandu: `console.log('✓', posyandu.namaPosyandu, '—', targetBalita, 'balita')`
  7. Final summary: `console.log('\n✅ seedMassal selesai — 15 posyandu, ${totalBalita} balita')`

  Use `require.main === module` guard at bottom for standalone execution.
  </action>

  <verify>
    <automated>grep -c "namaPosyandu" prisma/seed.massal.ts</automated>
  </verify>

  <acceptance_criteria>
  - `prisma/seed.massal.ts` exists with `export async function seedMassal(prisma: PrismaClient)`
  - POSYANDU_DATA has 15 entries: `grep -c "namaPosyandu" prisma/seed.massal.ts` returns >= 15
  - All 5 status groups present: `grep "statusGroup" prisma/seed.massal.ts` shows sehat, sedang, rawan, kritis, outlier
  - Posyandu Dahlia exists with `statusGroup:'kritis'` in Wirogunan (D-13 stunting cluster)
  - `grep "latitude" prisma/seed.massal.ts` matches — coordinates present for Leaflet map
  - Puskesmas Mergangsan uses `email: 'demo@puskesmas-mergangsan.go.id'` (matches demo account)
  </acceptance_criteria>

  <done>seed.massal.ts has 3 puskesmas, 15 posyandu with coordinates and status groups, and loops to create 375+ warga/balita.</done>
</task>

<task type="auto">
  <name>Task 2: Add Pemeriksaan history + Imunisasi dasar to seedMassal</name>
  <files>prisma/seed.massal.ts</files>

  <read_first>
  - prisma/seed.massal.ts — Task 1 output: understand the balita creation loop and stored data arrays
  - prisma/schema.prisma — lines 276-325: Pemeriksaan and Imunisasi models, all required fields
  - .planning/phases/07-seed-data-demo/07-CONTEXT.md — D-14 (12-15 months), D-15 (70-80% attendance), D-16 (varying trends), D-18 (imunisasi schedule), D-19 (70-80% complete)
  - .planning/phases/07-seed-data-demo/07-PATTERNS.md — "Pemeriksaan bulk create" and "Imunisasi bulk create" pattern sections
  </read_first>

  <action>
  Add two helper functions and extend the balita creation loop in `prisma/seed.massal.ts`.

  **Helper: Z-Score values by status gizi** — function `getZScores(statusGizi: string, seed: number)` returns `{ zBbU, zTbU, zScoreBbTb }` as approximate floats:
  - normal:      zBbU: -1.5 + (seed%30)*0.1, zTbU: -1.0 + (seed%20)*0.1, zBbTb: -0.5 + (seed%10)*0.1
  - kurang:      zBbU: -2.8 + (seed%8)*0.1, zTbU: -1.5 + (seed%10)*0.1, zBbTb: -1.2 + (seed%8)*0.1
  - buruk:       zBbU: -3.5 + (seed%5)*0.1, zTbU: -2.5 + (seed%5)*0.1, zBbTb: -2.0 + (seed%5)*0.1
  - pendek:      zBbU: -1.0 + (seed%20)*0.1, zTbU: -2.8 + (seed%8)*0.1, zBbTb: -0.5 + (seed%10)*0.1
  - sangat_pendek: zBbU: -1.5 + (seed%10)*0.1, zTbU: -3.8 + (seed%8)*0.1, zBbTb: -0.5 + (seed%5)*0.1
  - lebih:       zBbU: 2.0 + (seed%8)*0.1, zTbU: 0.5 + (seed%10)*0.1, zBbTb: 1.5 + (seed%8)*0.1
  - obesitas:    zBbU: 3.0 + (seed%5)*0.1, zTbU: 1.0 + (seed%5)*0.1, zBbTb: 2.5 + (seed%5)*0.1

  **Helper: BB/TB by age range** — function `getMeasurements(ageMonths: number, seed: number)` returns `{ beratBadan, tinggiBadan }`:
  - 0-6 mo:   BB: 3.0 + (seed%50)*0.1, TB: 50 + (seed%17)
  - 7-12 mo:  BB: 7.0 + (seed%30)*0.1, TB: 67 + (seed%8)
  - 13-24 mo: BB: 9.0 + (seed%30)*0.1, TB: 75 + (seed%10)
  - 25-60 mo: BB: 12.0 + (seed%60)*0.1, TB: 85 + (seed%25)

  **Pemeriksaan history loop** — after each balita is created or confirmed existing, add:

  ```
  const lookbackMonths = 12 + (posIdx + balitaIdx) % 4  // 12-15 months
  const ageAtBirthMs = todayStart.getTime() - tanggalLahir.getTime()
  for (let m = lookbackMonths; m >= 0; m--) {
    // Skip 25% of months (D-15: 70-80% attendance)
    if ((posIdx + balitaIdx + m) % 4 === 0) continue
    
    const examDate = new Date(todayStart.getTime() - m * 30 * 24 * 60 * 60 * 1000)
    const examDateStr = examDate.toISOString().split('T')[0]
    const tanggalPemeriksaan = new Date(examDateStr)  // @db.Date compatible
    
    // D-16 trend: index%3==0 improving, %3==1 stable, %3==2 declining
    const trendOffset = balitaIdx % 3 === 0 ? (lookbackMonths - m) * 0.05 :
                        balitaIdx % 3 === 2 ? -(lookbackMonths - m) * 0.05 : 0
    
    const ageMonthsAtExam = Math.floor((examDate.getTime() - tanggalLahir.getTime()) / (30*24*60*60*1000))
    if (ageMonthsAtExam < 0) continue
    
    const seed = posIdx * 10000 + balitaIdx * 100 + m
    const zs = getZScores(statusGizi, seed)
    const ms = getMeasurements(ageMonthsAtExam, seed)
    
    const existing = await prisma.pemeriksaan.findFirst({
      where: { balitaId: balita.id, tanggalPemeriksaan }
    })
    if (!existing) {
      await prisma.pemeriksaan.create({
        data: {
          balitaId: balita.id,
          beratBadan: Math.round((ms.beratBadan + trendOffset * 0.2) * 10) / 10,
          tinggiBadan: Math.round(ms.tinggiBadan * 10) / 10,
          zScoreBbU: Math.round((zs.zBbU + trendOffset) * 100) / 100,
          zScoreTbU: Math.round(zs.zTbU * 100) / 100,
          zScoreBbTb: Math.round(zs.zScoreBbTb * 100) / 100,
          statusGizi: statusGizi as any,
          tanggalPemeriksaan,
        }
      })
    }
  }
  ```

  **Imunisasi dasar loop** — after pemeriksaan loop, add:

  ```
  const IMUNISASI_SCHEDULE = [
    { namaVaksin: 'BCG',        dosisKe: 1, ageMonth: 0 },
    { namaVaksin: 'Polio',      dosisKe: 1, ageMonth: 0 },
    { namaVaksin: 'Polio',      dosisKe: 2, ageMonth: 2 },
    { namaVaksin: 'DPT-HB-Hib', dosisKe: 1, ageMonth: 2 },
    { namaVaksin: 'Polio',      dosisKe: 3, ageMonth: 3 },
    { namaVaksin: 'DPT-HB-Hib', dosisKe: 2, ageMonth: 3 },
    { namaVaksin: 'Polio',      dosisKe: 4, ageMonth: 4 },
    { namaVaksin: 'DPT-HB-Hib', dosisKe: 3, ageMonth: 4 },
    { namaVaksin: 'Campak',     dosisKe: 1, ageMonth: 9 },
  ]
  const ageMonthsNow = Math.floor((todayStart.getTime() - tanggalLahir.getTime()) / (30*24*60*60*1000))
  
  for (const [vIdx, vax] of IMUNISASI_SCHEDULE.entries()) {
    if (ageMonthsNow < vax.ageMonth) continue
    // Skip 20-30% for realism (D-19)
    if ((posIdx + balitaIdx + vIdx) % 4 === 3) continue
    const existing = await prisma.imunisasi.findFirst({
      where: { balitaId: balita.id, namaVaksin: vax.namaVaksin, dosisKe: vax.dosisKe }
    })
    if (!existing) {
      const injeksiDate = new Date(tanggalLahir.getTime() + vax.ageMonth * 30 * 24 * 60 * 60 * 1000)
      await prisma.imunisasi.create({
        data: {
          balitaId: balita.id,
          namaVaksin: vax.namaVaksin,
          dosisKe: vax.dosisKe,
          tanggalInjeksi: new Date(injeksiDate.toISOString().split('T')[0]),
        }
      })
    }
  }
  ```

  Place IMUNISASI_SCHEDULE as a module-level constant (outside the function) to avoid re-declaration on each loop iteration.

  Note: The `as any` cast for `statusGizi` is needed because Prisma's `StatusGizi` enum type is imported from `@prisma/client` — add `import { ..., StatusGizi } from '@prisma/client'` and cast as `statusGizi as StatusGizi` for strict-mode compliance.
  </action>

  <verify>
    <automated>grep -c "namaVaksin" prisma/seed.massal.ts && grep "pemeriksaan.create" prisma/seed.massal.ts && grep "imunisasi.create" prisma/seed.massal.ts</automated>
  </verify>

  <acceptance_criteria>
  - `prisma/seed.massal.ts` contains `pemeriksaan.create` (grep matches)
  - `prisma/seed.massal.ts` contains `imunisasi.create` (grep matches)
  - IMUNISASI_SCHEDULE has 9 entries: `grep -c "namaVaksin" prisma/seed.massal.ts` returns >= 9
  - `grep "lookbackMonths" prisma/seed.massal.ts` matches (months range 12-15 implemented)
  - `grep "StatusGizi" prisma/seed.massal.ts` matches (proper enum typing, no bare `any`)
  - File exports `seedMassal` and has `require.main === module` standalone guard
  </acceptance_criteria>

  <done>seed.massal.ts complete — 3 puskesmas, 15 posyandu, 375+ balita with 12-15 months pemeriksaan history and imunisasi dasar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → PostgreSQL | Bulk writes; runs inside sispos-backend container only |
| NIK generation helper | Synthetic NIKs must not collide with real citizen NIKs (prefix 3471 is valid DIY code but sequential padding makes collision with seed.demo.ts NIKs impossible) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | Hardcoded Warga1234! / Massal1234! in seed.massal.ts | accept | Bulk seed credentials are dev/demo only; clearly marked with comments; never in production DB migration |
| T-07-02-02 | Tampering | Synthetic NIK generation — collision with real users | mitigate | NIKs generated with pattern `3471{posIdx:2}{balIdx:4}{suffix:6}` are guaranteed unique within seed; upsert on nikIbu prevents duplicates on re-run |
| T-07-02-03 | Denial of Service | Large pemeriksaan loop (375 balita × 15 months = ~5600 inserts) | accept | findFirst guard prevents duplicate inserts on re-run; single sequential execution inside container; acceptable for demo seed |
</threat_model>

<verification>
After both tasks complete, verify file structure:
- `grep "export async function seedMassal" prisma/seed.massal.ts` → matches
- `grep -c "namaPosyandu" prisma/seed.massal.ts` → >= 15
- `grep "pemeriksaan.create" prisma/seed.massal.ts` → matches
- `grep "imunisasi.create" prisma/seed.massal.ts` → matches
- `grep "require.main === module" prisma/seed.massal.ts` → matches
</verification>

<success_criteria>
- `prisma/seed.massal.ts` exports `seedMassal(prisma: PrismaClient)`
- 15 posyandu defined across 3 puskesmas with coordinates and status groups
- Balita loop generates 375+ entries with Z-Score-informed pemeriksaan history and age-based imunisasi
- File is idempotent: findFirst guards on warga, balita, pemeriksaan, imunisasi before every create
</success_criteria>

<output>
Create `.planning/phases/07-seed-data-demo/07-02-SUMMARY.md` when done.

## Artifacts This Phase Produces

| File | Status | Purpose |
|------|--------|---------|
| `prisma/seed.massal.ts` | NEW | Exports `seedMassal(prisma)` — 3 puskesmas, 15 posyandu (with coordinates), 375-525 warga/balita, 12-15 months pemeriksaan, imunisasi dasar |
</output>

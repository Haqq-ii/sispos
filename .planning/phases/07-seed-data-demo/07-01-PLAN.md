---
phase: 07-seed-data-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/seed.ts
  - prisma/seed.wilayah.ts
  - backend/package.json
autonomous: true
requirements:
  - SEED-01

must_haves:
  truths:
    - "`npx prisma db seed` (inside container) executes all seed phases in order: wilayah → massal → demo → today"
    - "`prisma/seed.ts` exists and imports seedWilayah, seedMassal, seedDemo, seedToday as named functions"
    - "`backend/package.json` prisma block has seed entry pointing to ts-node invocation of prisma/seed.ts"
    - "`prisma/seed.wilayah.ts` exports `seedWilayah(prisma: PrismaClient)` and retains standalone execution path"
  artifacts:
    - path: "prisma/seed.ts"
      provides: "Master seed orchestrator"
      contains: "await seedWilayah(prisma)"
    - path: "backend/package.json"
      provides: "prisma db seed config"
      contains: "ts-node --project ./tsconfig.json ./prisma/seed.ts"
  key_links:
    - from: "backend/package.json"
      to: "prisma/seed.ts"
      via: "prisma.seed config key"
      pattern: "seed.*prisma/seed\\.ts"
    - from: "prisma/seed.ts"
      to: "prisma/seed.wilayah.ts"
      via: "import { seedWilayah }"
      pattern: "seedWilayah"
---

<objective>
Wire the `prisma db seed` entry point so that a single `npx prisma db seed` command inside the container runs all seed phases in the mandated order (D-01, D-02).

Purpose: All downstream plans (07-02, 07-03) produce files imported by this orchestrator. This plan creates the orchestrator skeleton and configures the npm entry point so the full pipeline is runnable once 07-02 and 07-03 deliver their files.

Output: `prisma/seed.ts` (new), `prisma/seed.wilayah.ts` (add export), `backend/package.json` (add seed entry).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seed-data-demo/07-CONTEXT.md
@.planning/phases/07-seed-data-demo/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor seed.wilayah.ts + Create seed.ts orchestrator</name>
  <files>prisma/seed.wilayah.ts, prisma/seed.ts</files>

  <read_first>
  - prisma/seed.wilayah.ts — full file (568 lines): understand main() body, all prisma.wilayah.createMany calls, existing PrismaClient declaration
  - prisma/seed.demo.ts — lines 1-10: PrismaClient import + top-level declarations pattern
  - .planning/phases/07-seed-data-demo/07-PATTERNS.md — "orchestrator core pattern" section and "PrismaClient Lifecycle" section
  - .planning/phases/07-seed-data-demo/07-CONTEXT.md — D-01, D-02, D-03 decisions
  </read_first>

  <action>
  **Step A — Refactor `prisma/seed.wilayah.ts`:**

  The file currently has `const prisma = new PrismaClient()` at line 15 and a `main()` function containing all seeding logic, plus a standalone `main()` call at lines 561-568.

  Make the following edits:
  1. Remove the top-level `const prisma = new PrismaClient()` at line 15 (this instance is only needed for standalone execution).
  2. Find `async function main()` (line ~22). Replace it with `export async function seedWilayah(prisma: PrismaClient): Promise<void>`. The entire function body stays unchanged — it already uses `prisma.*` calls.
  3. After the closing brace of `seedWilayah`, add the standalone execution block so the file still works independently:

     ```
     // Standalone execution (backward compatibility)
     if (require.main === module) {
       const standaloneClient = new PrismaClient()
       seedWilayah(standaloneClient)
         .catch((e: unknown) => { console.error('Seed wilayah gagal:', e); process.exit(1) })
         .finally(async () => { await standaloneClient.$disconnect() })
     }
     ```
  4. Remove or comment out the original standalone `main()` call block at lines 561-568.

  Do NOT change any of the actual data arrays or createMany logic inside seedWilayah.

  **Step B — Create `prisma/seed.ts`:**

  Create a new file `prisma/seed.ts` with this exact structure:
  - Import `PrismaClient` from `@prisma/client`
  - Import `{ seedWilayah }` from `./seed.wilayah`
  - Import `{ seedMassal }` from `./seed.massal`
  - Import `{ seedDemo }` from `./seed.demo`
  - Import `{ seedToday }` from `./seed.today`
  - `const prisma = new PrismaClient()`
  - `async function main()` that:
    - logs `'🌱 SISPOS Full Seed — mulai...'`
    - logs separator `'━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'`
    - awaits `seedWilayah(prisma)` with surrounding console.log
    - awaits `seedMassal(prisma)` with surrounding console.log
    - awaits `seedDemo(prisma)` with surrounding console.log
    - awaits `seedToday(prisma)` with surrounding console.log
    - logs `'\n✅ SISPOS Full Seed — selesai!'`
  - Lifecycle: `main().catch((e) => { console.error(e); process.exit(1) }).finally(async () => { await prisma.$disconnect() })`

  Note: At this stage, `./seed.massal`, `./seed.demo` (as module export), and `./seed.today` (as module export) do not yet exist with the required exports. TypeScript will report import errors — this is expected and will be resolved by Plans 07-02 and 07-03. Do NOT add `// @ts-ignore` directives; the imports are correct and will resolve.
  </action>

  <verify>
    <automated>grep -n "export async function seedWilayah" prisma/seed.wilayah.ts && grep -n "require.main === module" prisma/seed.wilayah.ts && grep -n "import { seedWilayah }" prisma/seed.ts && grep -n "import { seedMassal }" prisma/seed.ts && grep -n "await seedDemo(prisma)" prisma/seed.ts && grep -n "await seedToday(prisma)" prisma/seed.ts</automated>
  </verify>

  <acceptance_criteria>
  - `prisma/seed.wilayah.ts` contains `export async function seedWilayah(prisma: PrismaClient)` (exact string)
  - `prisma/seed.wilayah.ts` contains `require.main === module` block for standalone compatibility
  - `prisma/seed.wilayah.ts` does NOT have a bare top-level `const prisma = new PrismaClient()` line
  - `prisma/seed.ts` exists and imports all four: seedWilayah, seedMassal, seedDemo, seedToday
  - `prisma/seed.ts` calls all four in order: seedWilayah → seedMassal → seedDemo → seedToday
  </acceptance_criteria>

  <done>seed.wilayah.ts exports seedWilayah(prisma), seed.ts orchestrator exists with all four imports and ordered await calls.</done>
</task>

<task type="auto">
  <name>Task 2: Update backend/package.json prisma seed entry</name>
  <files>backend/package.json</files>

  <read_first>
  - backend/package.json — lines 49-52: current prisma block (currently only has "schema" key)
  - .planning/phases/07-seed-data-demo/07-PATTERNS.md — "backend/package.json" section showing updated block
  - .planning/phases/07-seed-data-demo/07-CONTEXT.md — D-04 decision
  </read_first>

  <action>
  Edit `backend/package.json`. Find the `"prisma"` block (lines 49-51):

  ```json
  "prisma": {
    "schema": "./prisma/schema.prisma"
  }
  ```

  Add the `"seed"` key so the block becomes:

  ```json
  "prisma": {
    "schema": "./prisma/schema.prisma",
    "seed": "ts-node --project ./tsconfig.json ./prisma/seed.ts"
  }
  ```

  The path `./prisma/seed.ts` is relative to the backend directory (`/app` inside container), which is correct because Prisma CLI runs from the directory containing `package.json`. The `--project ./tsconfig.json` flag points to `backend/tsconfig.json` which is mounted at `/app/tsconfig.json`.

  Do NOT modify any other field in package.json.
  </action>

  <verify>
    <automated>node -e "const p = require('./backend/package.json'); if (!p.prisma.seed) process.exit(1); console.log('seed:', p.prisma.seed)"</automated>
  </verify>

  <acceptance_criteria>
  - `backend/package.json` prisma block has both `schema` and `seed` keys
  - `seed` value is exactly `"ts-node --project ./tsconfig.json ./prisma/seed.ts"`
  - File is valid JSON (node -e "require('./backend/package.json')" exits 0)
  - No other fields in package.json were changed
  </acceptance_criteria>

  <done>Running `docker compose exec sispos-backend npx prisma db seed` invokes ts-node on prisma/seed.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → PostgreSQL | Prisma client writes to DB; only runs in controlled dev/demo context |
| ts-node invocation | Executes TypeScript from /app/prisma inside container; no external network calls in orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | prisma/seed.*.ts hardcoded credentials | accept | Demo credentials (Demo1234!, PIN 123456) are acceptable in seed scripts for dev/demo environment. NEVER deploy seed files to production. Comment in each file: "// DEV/DEMO ONLY — não usar em produção" |
| T-07-01-02 | Tampering | backend/package.json prisma.seed | accept | Only modifies the dev/build toolchain; runs inside Docker container only; not exposed to host network |
| T-07-01-03 | Spoofing | seed.wilayah.ts standalone execution after refactor | mitigate | `require.main === module` guard prevents unintended standalone execution when imported as module |
</threat_model>

<verification>
After Task 1 and Task 2 complete:
- `grep "export async function seedWilayah" prisma/seed.wilayah.ts` → matches
- `grep "require.main === module" prisma/seed.wilayah.ts` → matches
- `grep "seed.*prisma/seed.ts" backend/package.json` → matches
- `cat prisma/seed.ts | grep -c "await seed"` → 4 (four awaited calls)
</verification>

<success_criteria>
- `prisma/seed.ts` exists with four imports and four sequential awaited calls
- `prisma/seed.wilayah.ts` exports `seedWilayah` and retains standalone path
- `backend/package.json` prisma block has seed key pointing to `ts-node --project ./tsconfig.json ./prisma/seed.ts`
</success_criteria>

<output>
Create `.planning/phases/07-seed-data-demo/07-01-SUMMARY.md` when done.

## Artifacts This Phase Produces

| File | Status | Purpose |
|------|--------|---------|
| `prisma/seed.ts` | NEW | Master orchestrator — calls all 4 seed functions in order |
| `prisma/seed.wilayah.ts` | MODIFIED | Added `export async function seedWilayah(prisma)` + `require.main` guard |
| `backend/package.json` | MODIFIED | Added `"seed": "ts-node --project ./tsconfig.json ./prisma/seed.ts"` to prisma block |
</output>

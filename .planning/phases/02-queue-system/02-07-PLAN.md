---
phase: 02-queue-system
plan: "07"
type: execute
wave: 5
depends_on:
  - 02-05
  - 02-06
files_modified:
  - frontend/src/hooks/useAntrianSocket.ts
  - frontend/src/components/antrian/CountdownEstimasi.tsx
  - frontend/src/components/antrian/StatusAntrian.tsx
  - frontend/src/components/antrian/BatalkanAntrianDialog.tsx
  - frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx
  - frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx
  - frontend/src/pages/citizen/CitizenDashboardPage.tsx
  - frontend/src/pages/PuskesmasDashboardPage.tsx
  - frontend/src/router/index.tsx
autonomous: false
requirements:
  - QUEUE-02
  - QUEUE-03
  - QUEUE-04
  - QUEUE-06
must_haves:
  truths:
    - "TiketAntrianPage: on mount, socket.connect() is called and socket.emit('queue:join', { slotId, antrianId }) is sent"
    - "TiketAntrianPage: on unmount, socket.disconnect() is called (cleanup — per 02-UI-SPEC.md; do NOT leave room on tab hide)"
    - "TiketAntrianPage: countdown shows '±{N} menit' with N = computeCountdown({nomorUrut, nomorAktif:0, estimasiDurasiMenit, durasiRataAktual})"
    - "TiketAntrianPage: nomorUrut is displayed zero-padded 2 digits (String(nomorUrut).padStart(2,'0'))"
    - "TiketAntrianPage: 'Batalkan Antrian' button only appears when statusAntrian === 'menunggu'"
    - "KonfirmasiAntrianPage: 409 SLOT_PENUH shows Alert variant='destructive' with 'Ganti Sesi' ghost button"
    - "CitizenDashboardPage: shows 'ANTRIAN AKTIF HARI INI' card when citizen has active antrian; shows CTA card 'Ambil Antrian' when none"
    - "All 5 citizen antrian routes and 1 puskesmas jadwal route are registered in router/index.tsx with correct ProtectedRoute allowedRoles"
  artifacts:
    - path: "frontend/src/hooks/useAntrianSocket.ts"
      provides: "Socket.IO room join/leave + queue:update handler"
      exports: ["useAntrianSocket"]
    - path: "frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx"
      provides: "Screen 4 — tiket with realtime countdown"
    - path: "frontend/src/pages/citizen/CitizenDashboardPage.tsx"
      provides: "Replaced placeholder — shows antrian status card or CTA"
    - path: "frontend/src/router/index.tsx"
      provides: "All Phase 02 routes registered"
      contains: "/citizen/antrian"
  key_links:
    - from: "frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx"
      to: "frontend/src/hooks/useAntrianSocket.ts"
      via: "useAntrianSocket(slotId, antrianId)"
      pattern: "useAntrianSocket"
    - from: "frontend/src/hooks/useAntrianSocket.ts"
      to: "frontend/src/lib/socket.ts"
      via: "import { socket }"
      pattern: "socket\\.connect"
    - from: "frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx"
      to: "frontend/src/hooks/useCountdownEstimasi.ts"
      via: "computeCountdown or useCountdownEstimasi"
      pattern: "computeCountdown"
---

<objective>
Wave 5 — Final citizen screens (Konfirmasi, Tiket) + CitizenDashboard replacement + full router wiring. This is the terminal plan that makes all Phase 02 routes accessible.

This plan contains a human-verify checkpoint because the end-to-end antrian flow, Socket.IO realtime behavior, and the race condition UX must be verified by a human operating two browser tabs.

Critical rules:
- Socket.IO: socket.connect() on mount, socket.disconnect() on unmount (do NOT leave room on tab hide — 02-UI-SPEC.md Executor Notes #5)
- Countdown prefix WAJIB "±" — never display absolute time (QUEUE-03, CLAUDE.md)
- nomorUrut WAJIB zero-padded 2 digits: String(nomorUrut).padStart(2, '0')
- BatalkanAntrianDialog only visible when statusAntrian === 'menunggu' (D-06)
- WA notice text "Notifikasi akan dikirim ke WhatsApp Anda" (QUEUE-06 UX)
- router/index.tsx uses lazy imports + ProtectedRoute with allowedRoles for role enforcement

Purpose: After this plan, the complete Phase 02 flow is runnable end-to-end. The human-verify checkpoint runs the 5 success criteria from STATE.md.

Output: 9 files modified/created, including the router which is the final dependency for all Phase 02 frontend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-queue-system/02-CONTEXT.md
@.planning/phases/02-queue-system/02-RESEARCH.md
@.planning/phases/02-queue-system/02-UI-SPEC.md
@.planning/phases/02-queue-system/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useAntrianSocket hook + CountdownEstimasi + StatusAntrian + BatalkanAntrianDialog + KonfirmasiAntrianPage</name>
  <files>
    frontend/src/hooks/useAntrianSocket.ts
    frontend/src/components/antrian/CountdownEstimasi.tsx
    frontend/src/components/antrian/StatusAntrian.tsx
    frontend/src/components/antrian/BatalkanAntrianDialog.tsx
    frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx
  </files>
  <read_first>
    - frontend/src/lib/socket.ts (the socket singleton — autoConnect: false; created in 02-01)
    - frontend/src/hooks/useCountdownEstimasi.ts (computeCountdown function + CountdownInput interface — from 02-04)
    - frontend/src/hooks/useSesiAvailability.ts (useAmbilAntrian mutation, useBatalkanAntrian mutation — from 02-04)
    - frontend/src/stores/useAntrianStore.ts (selectedSlotId, selectedBalitaId, reset — from 02-04)
    - frontend/src/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter — for BatalkanAntrianDialog)
    - frontend/src/components/ui/alert.tsx (Alert, AlertDescription — for error states)
    - frontend/src/components/ui/badge.tsx (Badge — for status display)
    - .planning/phases/02-queue-system/02-UI-SPEC.md (Screen 3 Konfirmasi: summary card, RadioGroup balita, estimasi row, error states; Screen 4 Tiket: status section, countdown section; Destructive Actions: BatalkanAntrianDialog copy; Interaction Contracts: Real-Time Countdown Logic)
    - .planning/phases/02-queue-system/02-CONTEXT.md (D-03 countdown formula, D-05 queue:update events, D-06 batalkan rules)
    - .planning/phases/02-queue-system/02-RESEARCH.md (Pattern 2 Socket.IO room management — useAntrianSocket full implementation)
  </read_first>
  <action>
    Step A — Create frontend/src/hooks/useAntrianSocket.ts:
    - Props: (slotId: string, antrianId: string)
    - Import socket from '@/lib/socket'
    - Import useQueryClient from '@tanstack/react-query'
    - Interface QueueUpdate: { nomorAktif: number; durasiRataAktual: number | null; antrianList: Array<{ id: string; nomorUrut: number; statusAntrian: string }> }
    - Export type QueueUpdate for consumers (TiketAntrianPage needs to read the update)
    - Use useState<QueueUpdate | null>(null) to hold the latest queue state
    - useEffect with dependencies [slotId, antrianId, queryClient]:
      a. socket.connect()
      b. socket.emit('queue:join', { slotId, antrianId })
      c. socket.on('queue:update', (data: QueueUpdate) => { setQueueState(data); void queryClient.invalidateQueries({ queryKey: ['antrian', antrianId] }) })
      d. Return cleanup: () => { socket.off('queue:update'); socket.disconnect() }
    - socket.on('connect_error', ...) → log to console in dev; do not throw
    - Return { queueState } — TiketAntrianPage uses queueState.nomorAktif and queueState.durasiRataAktual to recompute countdown
    - RULE: Do NOT call socket.leave() on browser tab hide — per 02-UI-SPEC.md Executor Notes #5. Only disconnect on component unmount (in useEffect cleanup)

    Step B — Create frontend/src/components/antrian/CountdownEstimasi.tsx:
    Props: { nomorUrut: number; estimasiDurasiMenit: number; durasiRataAktual: number | null; nomorAktif: number }
    - Import computeCountdown from '@/hooks/useCountdownEstimasi'
    - Compute minutesLeft = computeCountdown({ nomorUrut, nomorAktif, estimasiDurasiMenit, durasiRataAktual })
    - Render a border rounded-lg p-4 text-center region with aria-live="polite" (screen reader announces updates)
    - "Estimasi dipanggil dalam" label: caption size (text-xs), text-gray-500
    - Countdown figure: "±{Math.round(minutesLeft)} menit" in heading size (text-xl font-bold); text-amber-600 when minutesLeft < 5, else text-foreground
    - "±" prefix is MANDATORY — never display without it (QUEUE-03, CLAUDE.md)
    - Disclaimer below: "Estimasi waktu tunggu. Dapat berubah sesuai kondisi pelayanan." (text-xs text-gray-400 italic)
    - Disclaimer is MANDATORY — never omit per 02-UI-SPEC.md

    Step C — Create frontend/src/components/antrian/StatusAntrian.tsx:
    Props: { status: 'menunggu' | 'dipanggil' | 'selesai' | 'ditangguhkan' | 'tidak_hadir' | 'dibatalkan' }
    - Status color map (inline const):
      - menunggu: 'bg-blue-50 text-blue-600 border-blue-200'
      - dipanggil: 'bg-amber-50 text-amber-600 border-amber-200'
      - selesai: 'bg-green-50 text-green-600 border-green-200'
      - dibatalkan: 'bg-gray-100 text-gray-500 border-gray-200'
      - (others): 'bg-gray-100 text-gray-500 border-gray-200'
    - Status display label map: menunggu → 'MENUNGGU', dipanggil → 'DIPANGGIL', selesai → 'SELESAI', dibatalkan → 'DIBATALKAN'
    - Render: `<span className="text-xs text-gray-500">Status:</span>` + Badge with border className from map, role="status"

    Step D — Create frontend/src/components/antrian/BatalkanAntrianDialog.tsx:
    Props: { antrianId: string; onBatalkanSuccess: () => void }
    - Uses useBatalkanAntrian mutation (from useAntrianSocket or useSesiAvailability — import from '@/hooks/useSesiAvailability')
    - Trigger: Button variant="outline" text-red-600 className text "Batalkan Antrian" (only rendered externally by TiketAntrianPage when statusAntrian === 'menunggu')
    - The dialog uses useState(false) for open state; trigger button opens it
    - Dialog title: "Batalkan Antrian?" (DialogTitle)
    - Dialog body: "Nomor antrian Anda akan dilepas dan tidak bisa dikembalikan." (body text)
    - Dialog footer: "Tidak, Kembali" ghost button (closes dialog) | "Ya, Batalkan" Button variant="destructive" (calls mutation)
    - On mutation success: close dialog, call onBatalkanSuccess(), show toast "Antrian berhasil dibatalkan."
    - On mutation pending: disable buttons, show Loader2 animate-spin on confirm button

    Step E — Create frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx:
    - Get selectedSlotId from useAntrianStore
    - If no selectedSlotId, redirect back to /citizen/antrian/pilih-sesi
    - Fetch sesi details via useSesiAvailability(jadwalId) — need jadwalId; get from location.state or deduce from the sesi data. Note: selectedSlotId is a SlotSesi id, not jadwalId. Options: (a) store jadwalId in useAntrianStore alongside slotId, (b) add getSlotById hook. Simplest: fetch GET /api/sesi?jadwalId=... but we only have slotId. Better: call GET /api/antrian (or a dedicated slot endpoint). Cleanest solution: add jadwalId to useAntrianStore (update useAntrianStore.ts to include selectedJadwalId and setSelectedJadwalId), set it in PilihSesiPage when slotId is set.
    - Fetch citizen's balita list via GET /api/balita (from child module) — if endpoint doesn't exist yet, use GET /api/users/me/balita or check what Phase 01 provides. If no balita endpoint exists, show the "Belum ada balita" alert with placeholder CTA.
    - Summary card (bg-green-50, rounded-lg, p-4): posyandu name, tanggal (DD/MM/YYYY), sesi label, estimasi "±{N} menit"
    - Estimasi row value: "±{nomorUrut × estimasiDurasiMenit} menit" — but we don't know nomorUrut yet (only known after POST). Show "±{Math.round(estimasiDurasiMenit)} – ±{estimasiDurasiMenit × kuota} menit" range OR show just "±{estimasiDurasiMenit} menit" as minimum. Per 02-UI-SPEC.md: show the computed estimate assuming worst-case nomorUrut = terisi+1: estimasiMenit = (terisi+1) × estimasiDurasiMenit
    - Disclaimer: "Estimasi dapat berubah sesuai kondisi pelayanan" (text-xs text-gray-400 italic) — MANDATORY
    - Pilih balita: RadioGroup with one RadioGroupItem per balita; disable CTA until balita selected
    - useAmbilAntrian mutation: on success → store antrianId, call reset() on useAntrianStore, navigate to '/citizen/antrian/tiket/' + antrianId
    - Error states:
      - 409 SLOT_PENUH: Alert variant="destructive" "Sesi ini sudah penuh saat Anda mengkonfirmasi. Silakan pilih sesi lain." + "Ganti Sesi" ghost Button navigating back
      - 409 SUDAH_DAFTAR: Alert variant="destructive" "Balita ini sudah terdaftar di sesi yang sama. Pilih balita lain atau sesi berbeda."
      - Generic: standard destructive alert
    - Figma frame 5:2902 — executor MUST pull this frame before implementing
  </action>
  <verify>
    <automated>grep -c "socket\.connect\|socket\.disconnect" /d/Semester\ 4/PSI/sispos/frontend/src/hooks/useAntrianSocket.ts</automated>
  </verify>
  <acceptance_criteria>
    - useAntrianSocket.ts calls socket.connect() and socket.emit('queue:join') in useEffect
    - useAntrianSocket.ts cleanup returns socket.off('queue:update') AND socket.disconnect()
    - CountdownEstimasi.tsx countdown text always starts with "±" (grep for "±" in file)
    - CountdownEstimasi.tsx aria-live="polite" on countdown region
    - BatalkanAntrianDialog.tsx "Ya, Batalkan" button uses variant="destructive"
    - KonfirmasiAntrianPage.tsx handles 409 SLOT_PENUH with Alert containing "Ganti Sesi" ghost button
    - KonfirmasiAntrianPage.tsx calls useAntrianStore().reset() after successful ambilAntrian
    - No TypeScript errors in the five new files
  </acceptance_criteria>
  <done>Socket hook connects/disconnects correctly; CountdownEstimasi always shows ± prefix; BatalkanAntrianDialog has destructive confirm; KonfirmasiAntrianPage handles 409 race condition with recovery CTA.</done>
</task>

<task type="auto">
  <name>Task 2: TiketAntrianPage + CitizenDashboardPage + router wiring + PuskesmasDashboardPage nav link</name>
  <files>
    frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx
    frontend/src/pages/citizen/CitizenDashboardPage.tsx
    frontend/src/pages/PuskesmasDashboardPage.tsx
    frontend/src/router/index.tsx
  </files>
  <read_first>
    - frontend/src/router/index.tsx (current file — add 5 new lazy routes; preserve all existing routes)
    - frontend/src/router/ProtectedRoute.tsx (updated in 02-04 — use allowedRoles prop for citizen and puskesmas routes)
    - frontend/src/hooks/useAntrianSocket.ts (useAntrianSocket — just created in Task 1)
    - frontend/src/components/antrian/CountdownEstimasi.tsx (just created in Task 1)
    - frontend/src/components/antrian/StatusAntrian.tsx (just created in Task 1)
    - frontend/src/components/antrian/BatalkanAntrianDialog.tsx (just created in Task 1)
    - frontend/src/stores/useAuthStore.ts (user.namaLengkap for greeting)
    - .planning/phases/02-queue-system/02-UI-SPEC.md (Screen 4 TiketAntrianPage full spec; Screen 7 CitizenDashboardPage; Navigation Flow; Copywriting Contract)
    - .planning/phases/02-queue-system/02-CONTEXT.md (D-06 batalkan only when menunggu; D-07 puskesmas/jadwal route)
  </read_first>
  <action>
    Step A — Create frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx:
    - Route param: :antrianId (from useParams)
    - Fetch antrian detail: `useQuery({ queryKey: ['antrian', antrianId], queryFn: () => apiClient.get('/antrian/' + antrianId).then(r => r.data.data) })`
    - Call useAntrianSocket(slotId, antrianId) after antrian data is loaded (slotId from antrian.slotId)
    - The queueState from useAntrianSocket provides nomorAktif and durasiRataAktual for countdown recalculation
    - Layout:
      - Sticky header (top-0 z-10 bg-white border-b px-4 py-3): "SISPOS" left (text-primary font-bold) + "Tiket Antrian" center (text-sm font-bold)
      - Section 1 (text-center): posyandu name + address + date + sesi label
      - Section 2 (text-center py-6 bg-green-50 rounded-xl): "NOMOR ANTRIAN ANDA" (text-xs uppercase tracking-wider text-gray-400) + number (text-5xl font-bold leading-none) ZERO-PADDED 2 digits: String(antrian.nomorUrut).padStart(2, '0'). aria-label="Nomor antrian Anda: {padded}"
      - Section 3 (border rounded-lg p-4 text-center): CountdownEstimasi component when statusAntrian === 'menunggu'; pulsing amber Badge "Silakan menuju meja pelayanan!" when dipanggil; CheckCircle + "Pelayanan selesai. Terima kasih!" when selesai
      - Section 4: StatusAntrian component
      - Section 5: MessageSquare icon (text-green-600 size 14) + "Notifikasi akan dikirim ke WhatsApp Anda" (caption text-gray-500)
      - Section 6: "Batalkan Antrian" BatalkanAntrianDialog trigger — ONLY rendered when statusAntrian === 'menunggu'; hidden when dipanggil/selesai/dibatalkan
      - Section 7: "Kembali ke Dashboard" Button variant="ghost" full-width min-h-[44px] → /citizen/dashboard
    - On queueState update (from useAntrianSocket): the countdown automatically recomputes because CountdownEstimasi receives updated nomorAktif and durasiRataAktual from queueState
    - Socket disconnect: show Alert "Koneksi terputus. Memuat ulang data..." (non-dismissible; socket.io-client has autoReconnect by default)
    - Loading state: Skeleton layout for each section proportional to content height
    - Figma frame 5:3116 — executor MUST pull this frame before implementing
    - BatalkanAntrianDialog onBatalkanSuccess: invalidate ['antrian', antrianId] query to refresh status

    Step B — Create frontend/src/pages/citizen/CitizenDashboardPage.tsx (replacing placeholder at frontend/src/pages/CitizenDashboardPage.tsx):
    - NEW FILE at: frontend/src/pages/citizen/CitizenDashboardPage.tsx (correct path per RESEARCH.md structure)
    - Fetch today's active antrian: `useQuery({ queryKey: ['antrian', 'saya'], queryFn: () => apiClient.get('/antrian/saya').then(r => r.data.data) })`
    - Get user from useAuthStore: namaLengkap, extract first name (split by space, take [0])
    - Layout (mobile-first, max-width 400px centered, bg-gray-50, min-h-screen pb-8):
      - Top greeting card (bg-white border-b px-4 py-4): "Selamat datang, {namaDepan}" (text-xl font-bold) + "Posyandu {namaPosyandu}" or "Belum memilih Posyandu" (caption text-gray-500)
      - Section A — if antrian active (statusAntrian 'menunggu' | 'dipanggil'):
        Card (bg-white rounded-xl shadow-sm p-4 mx-4 mt-4):
        "ANTRIAN AKTIF HARI INI" (caption uppercase tracking-wider text-gray-400)
        StatusAntrian badge
        "Nomor {padded nomorUrut}" (text-2xl font-bold) — note text-2xl here, not text-5xl (card summary, not tiket)
        "{namaPosyandu} · {jamMulai} WIB" (caption text-gray-500)
        "Estimasi ±{computeCountdown} menit" (caption text-gray-500)
        "Lihat Tiket" Button variant="default" min-h-[44px] full-width → navigate('/citizen/antrian/tiket/' + antrian.id)
      - Section B — if no active antrian:
        Card (bg-green-50 rounded-xl p-6 mx-4 mt-4 text-center):
        CalendarPlus icon (size 40 text-primary)
        "Belum ada antrian hari ini" (text-sm text-gray-500)
        "Ambil antrian Posyandu sekarang" (text-sm text-gray-500)
        "Ambil Antrian" Button variant="default" min-h-[44px] → navigate('/citizen/antrian/pilih-tanggal')
    - Loading state: Skeleton 120px tall mx-4 mt-4 rounded-xl

    Step C — Update frontend/src/pages/PuskesmasDashboardPage.tsx:
    - Keep the existing placeholder content (Phase 4 will replace it)
    - Add a temporary navigation link to /puskesmas/jadwal at the bottom of the card:
      - Import Link from 'react-router-dom'
      - Add after the existing content: a styled link or Button: "Manajemen Jadwal" → /puskesmas/jadwal
      - This is temporary until Phase 4 builds the full sidebar/navbar (per CONTEXT.md deferred)

    Step D — Update frontend/src/router/index.tsx:
    - ADD lazy imports for the 5 new citizen pages and 1 puskesmas page:
      const PilihTanggalPage = lazy(() => import('@/pages/citizen/antrian/PilihTanggalPage'))
      const PilihSesiPage = lazy(() => import('@/pages/citizen/antrian/PilihSesiPage'))
      const KonfirmasiAntrianPage = lazy(() => import('@/pages/citizen/antrian/KonfirmasiAntrianPage'))
      const TiketAntrianPage = lazy(() => import('@/pages/citizen/antrian/TiketAntrianPage'))
      const ManajemenJadwalPage = lazy(() => import('@/pages/puskesmas/jadwal/ManajemenJadwalPage'))
    - UPDATE the CitizenDashboardPage import to the new path:
      const CitizenDashboardPage = lazy(() => import('@/pages/citizen/CitizenDashboardPage'))
    - ADD 5 new routes inside the Routes block:
      - /citizen/antrian/pilih-tanggal → ProtectedRoute allowedRoles={['citizen']} → PilihTanggalPage
      - /citizen/antrian/pilih-sesi → ProtectedRoute allowedRoles={['citizen']} → PilihSesiPage
      - /citizen/antrian/konfirmasi → ProtectedRoute allowedRoles={['citizen']} → KonfirmasiAntrianPage
      - /citizen/antrian/tiket/:antrianId → ProtectedRoute allowedRoles={['citizen']} → TiketAntrianPage
      - /puskesmas/jadwal → ProtectedRoute allowedRoles={['puskesmas']} → ManajemenJadwalPage
    - UPDATE existing /citizen/dashboard route to also include allowedRoles={['citizen']}
    - UPDATE existing /puskesmas/dashboard route to also include allowedRoles={['puskesmas']}
    - UPDATE existing /kader/dashboard route to also include allowedRoles={['kader', 'ketua_kader']}
    - Keep all existing routes unchanged (login, register, OTP, lokasi, 404)
    - Also DELETE the OLD placeholder CitizenDashboardPage file at frontend/src/pages/CitizenDashboardPage.tsx OR keep it as a re-export: `export { default } from './citizen/CitizenDashboardPage'` to avoid breaking the import if used elsewhere
  </action>
  <verify>
    <automated>grep -c "citizen/antrian/tiket\|puskesmas/jadwal" /d/Semester\ 4/PSI/sispos/frontend/src/router/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TiketAntrianPage.tsx renders nomorUrut with padStart(2, '0') (zero-padded 2 digits)
    - TiketAntrianPage.tsx renders "Batalkan Antrian" trigger ONLY when statusAntrian === 'menunggu' (hidden for dipanggil/selesai)
    - TiketAntrianPage.tsx imports and calls useAntrianSocket
    - CountdownEstimasi is rendered with nomorAktif, estimasiDurasiMenit, durasiRataAktual props
    - CitizenDashboardPage.tsx at the NEW path frontend/src/pages/citizen/CitizenDashboardPage.tsx (not the old location)
    - CitizenDashboardPage.tsx renders "ANTRIAN AKTIF HARI INI" card when GET /api/antrian/saya returns an antrian
    - router/index.tsx contains routes for all 4 citizen antrian paths and /puskesmas/jadwal
    - router/index.tsx uses allowedRoles on all dashboard and new routes
    - `curl http://localhost/citizen/antrian/tiket/test` returns 200 HTML (route exists, not 404 from nginx)
  </acceptance_criteria>
  <done>TiketAntrianPage with realtime countdown and cancel dialog; CitizenDashboardPage replaces placeholder; all Phase 02 routes registered; PuskesmasDashboardPage has temp nav link to jadwal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 02 antrian system: Puskesmas can create jadwal, citizens can take antrian, tiket shows realtime countdown, cancel flow works, WA notifications are enqueued via BullMQ.
  </what-built>
  <how-to-verify>
    Run through all 5 Phase 02 success criteria:

    SUCCESS CRITERION 1 — Jadwal creation:
    1. Login as puskesmas: demo@puskesmas-mergangsan.go.id / Demo1234!
    2. Navigate to http://localhost/puskesmas/jadwal
    3. Click "Buat Jadwal Baru" → fill posyandu, tanggal tomorrow, estimasi 7 menit
    4. Click "Buat Jadwal" → toast "Jadwal berhasil dibuat. 3 sesi ter-generate otomatis."
    5. Open Prisma Studio (docker compose exec sispos-backend npx prisma studio) → SlotSesi table → 3 records with kuota=8, terisi=0
    EXPECTED: Pass if 3 slots exist with kuota=8

    SUCCESS CRITERION 2 — Race condition guard:
    1. Login as citizen: NIK 3471012345670001 / Demo1234!
    2. Set the slot terisi = kuota - 1 in DB (Prisma Studio: edit terisi = 7)
    3. Open 2 browser tabs, both at http://localhost/citizen/antrian/konfirmasi with same slotId
    4. Select same balita in both, click "Ambil Antrian" simultaneously in both tabs
    EXPECTED: Exactly 1 tab shows antrian tiket, other tab shows 409 SLOT_PENUH alert with "Ganti Sesi" button

    SUCCESS CRITERION 3 — Nomor antrian + estimasi:
    1. On the winning tab from test 2 → check TiketAntrianPage
    2. Queue number should be 2-digit padded (e.g. "08" not "8")
    3. Countdown shows "±{N} menit" format — confirm "±" is present and no absolute clock time
    EXPECTED: Pass if "08" (or similar 2-digit number) and "±" prefix visible

    SUCCESS CRITERION 4 — Realtime countdown (manual):
    1. Open TiketAntrianPage in Tab A (citizen logged in)
    2. In another terminal: manually emit a Socket.IO update to room sesi:{slotId} (or have a second citizen in same sesi take/cancel antrian)
    3. Observe Tab A: countdown figure updates WITHOUT browser refresh
    EXPECTED: Pass if countdown changes in Tab A without any refresh action

    SUCCESS CRITERION 5 — WA BullMQ:
    After completing test 2, run: docker compose logs sispos-backend | grep antrian_whatsapp
    EXPECTED: At least 1 log line showing the BullMQ job was enqueued (not necessarily delivered if FONNTE_API_KEY is test key)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass. Or describe which criteria failed and include error details.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citizen browser → TiketAntrianPage GET /api/antrian/:id | Ownership verified on server |
| citizen browser → Socket.IO queue:join event | slotId from client; server joins to room (no DB write) |
| citizen browser → PATCH /api/antrian/:id/batalkan (via BatalkanAntrianDialog) | Ownership verified on server |
| router/index.tsx ProtectedRoute | Client-side role check (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24 | Elevation of Privilege | Citizen accesses /puskesmas/jadwal | mitigate | ProtectedRoute allowedRoles={['puskesmas']} + server requireRole('puskesmas') — two layers |
| T-02-25 | Elevation of Privilege | Puskesmas accesses /citizen/antrian/* | mitigate | ProtectedRoute allowedRoles={['citizen']} — redirects to /login |
| T-02-26 | Tampering | TiketAntrianPage: socket slotId manipulation to snoop on another sesi | accept | Joining a sesi room one doesn't belong to returns stale data they can already infer publicly. No PII is exposed in queue:update payload (only nomorUrut, statusAntrian — not balita names). Low risk. |
| T-02-27 | Tampering | Double-tap "Ambil Antrian" on KonfirmasiAntrianPage | mitigate | Backend P2002 unique constraint + SELECT FOR UPDATE handles this. Button disabled on pending mutation. |
| T-02-28 | Repudiation | Cancel antrian — no audit log | accept | AuditLog is scoped to Phase 3 (Meja 2 triggers the first required AuditLog per REQUIREMENTS.md KADER-03). Queue cancel is low-stakes for Phase 2 academic context. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages; all imports from existing packages |
</threat_model>

<verification>
The human-verify checkpoint in Task 3 covers all 5 Phase 02 success criteria. Run it only after Tasks 1 and 2 are committed.

Additional automated checks:
- `grep -c "padStart(2" /d/Semester\ 4/PSI/sispos/frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx` → must be >= 1 (zero-padding)
- `grep -c "±" /d/Semester\ 4/PSI/sispos/frontend/src/components/antrian/CountdownEstimasi.tsx` → must be >= 1 (± prefix)
- `grep -c "allowedRoles" /d/Semester\ 4/PSI/sispos/frontend/src/router/index.tsx` → must be >= 3 (citizen+puskesmas+kader routes)
</verification>

<success_criteria>
- TiketAntrianPage renders realtime countdown with ± prefix, updated from Socket.IO queue:update events
- nomorUrut always shown as 2-digit zero-padded
- BatalkanAntrianDialog only visible when statusAntrian === 'menunggu'
- CitizenDashboardPage shows antrian card or CTA depending on GET /api/antrian/saya response
- All 5 new routes in router/index.tsx with ProtectedRoute allowedRoles
- Human verify checkpoint: all 5 Phase 02 success criteria pass
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| useAntrianSocket | frontend/src/hooks/useAntrianSocket.ts | React hook | Socket.IO connect/join/leave + queueState |
| QueueUpdate | frontend/src/hooks/useAntrianSocket.ts | TypeScript interface | { nomorAktif, durasiRataAktual, antrianList } |
| CountdownEstimasi | frontend/src/components/antrian/CountdownEstimasi.tsx | React component | "±N menit" countdown with aria-live, amber warning < 5 min |
| StatusAntrian | frontend/src/components/antrian/StatusAntrian.tsx | React component | Status badge with semantic colors |
| BatalkanAntrianDialog | frontend/src/components/antrian/BatalkanAntrianDialog.tsx | React component | Destructive confirm dialog for cancel flow (D-06) |
| KonfirmasiAntrianPage | frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx | React page | Screen 3: summary + balita picker + race condition 409 handling |
| TiketAntrianPage | frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx | React page | Screen 4: tiket with realtime countdown, status, cancel |
| CitizenDashboardPage | frontend/src/pages/citizen/CitizenDashboardPage.tsx | React page | Replaces placeholder: shows active antrian card or Ambil CTA |
| PuskesmasDashboardPage (updated) | frontend/src/pages/PuskesmasDashboardPage.tsx | React page | Adds temp nav link to /puskesmas/jadwal (D-07) |
| Routes | frontend/src/router/index.tsx | React Router config | /citizen/antrian/{pilih-tanggal,pilih-sesi,konfirmasi,tiket/:id} + /puskesmas/jadwal |

<output>
Create `.planning/phases/02-queue-system/02-07-SUMMARY.md` when done
</output>

---
phase: 02-queue-system
plan: "06"
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - frontend/src/components/jadwal/JadwalCard.tsx
  - frontend/src/components/jadwal/JadwalTable.tsx
  - frontend/src/components/jadwal/BuatJadwalDialog.tsx
  - frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx
autonomous: true
requirements:
  - QUEUE-01
must_haves:
  truths:
    - "ManajemenJadwalPage renders JadwalTable on desktop (sm:block) and JadwalCard list on mobile (sm:hidden)"
    - "BuatJadwalDialog renders slot preview: floor(60 / estimasiDurasiMenit) for 3 sessions in real-time as estimasiDurasiMenit changes"
    - "BuatJadwalDialog disables future dates in tanggalPelaksanaan calendar that already have a jadwal for the same posyandu"
    - "BuatJadwalDialog: 'Buat Jadwal' button submits form, closes dialog on success, shows toast 'Jadwal berhasil dibuat. 3 sesi ter-generate otomatis.'"
    - "JadwalTable status badge uses color tokens: AKTIF=bg-green-50 text-green-600, DRAFT=bg-gray-100 text-gray-500, DIKUNCI=bg-blue-50 text-blue-600"
    - "POST /api/jadwal 409 JADWAL_SUDAH_ADA shows field-level error on tanggal picker (not generic alert)"
  artifacts:
    - path: "frontend/src/components/jadwal/JadwalCard.tsx"
      provides: "Mobile card view for jadwal list item"
      exports: ["JadwalCard"]
    - path: "frontend/src/components/jadwal/JadwalTable.tsx"
      provides: "Desktop table view for jadwal list"
      exports: ["JadwalTable"]
    - path: "frontend/src/components/jadwal/BuatJadwalDialog.tsx"
      provides: "Modal form for creating jadwal"
      exports: ["BuatJadwalDialog"]
    - path: "frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx"
      provides: "Puskesmas jadwal management page (Screen 5 + 6)"
  key_links:
    - from: "frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx"
      to: "frontend/src/hooks/useJadwalList.ts"
      via: "useJadwalList()"
      pattern: "useJadwalList"
    - from: "frontend/src/components/jadwal/BuatJadwalDialog.tsx"
      to: "frontend/src/lib/validations/jadwal.schema.ts"
      via: "CreateJadwalFESchema + react-hook-form resolver"
      pattern: "CreateJadwalFESchema"
---

<objective>
Wave 4b — Puskesmas jadwal management UI: ManajemenJadwalPage + 3 components. This plan runs in parallel with Plan 02-05 (citizen pages 1-2) — zero file overlap.

Screens implemented: Screen 5 (ManajemenJadwalPage — table/card list) and Screen 6 (BuatJadwalDialog — inline modal form).

Decision D-07: ManajemenJadwalPage is a standalone page at /puskesmas/jadwal — NOT a tab in puskesmas dashboard. The router route is wired in Plan 02-07 since that plan handles all final route registration.

Design rules from 02-UI-SPEC.md:
- Responsive layout: `<Table>` on sm: breakpoint (640px+), card list on mobile
- Status badge colors: AKTIF=green, DRAFT=gray, DIKUNCI=blue (per phase 2 status color tokens)
- Slot preview in BuatJadwalDialog is client-side only: floor(60 / estimasi) × 3 sesi
- tanggalPelaksanaan Calendar: tomorrow is earliest allowed date

Purpose: QUEUE-01 frontend portion — Puskesmas can create and view jadwal through this UI.

Output: JadwalCard, JadwalTable, BuatJadwalDialog, ManajemenJadwalPage (4 files, no router changes yet).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-queue-system/02-CONTEXT.md
@.planning/phases/02-queue-system/02-RESEARCH.md
@.planning/phases/02-queue-system/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: JadwalCard + JadwalTable components (responsive jadwal list)</name>
  <files>
    frontend/src/components/jadwal/JadwalCard.tsx
    frontend/src/components/jadwal/JadwalTable.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/card.tsx (Card, CardContent)
    - frontend/src/components/ui/table.tsx (Table, TableHeader, TableRow, TableHead, TableBody, TableCell — installed in 02-01)
    - frontend/src/components/ui/badge.tsx (Badge — for status badges)
    - frontend/src/components/ui/button.tsx (Button for Lihat Detail)
    - frontend/src/components/ui/skeleton.tsx (Skeleton for loading)
    - .planning/phases/02-queue-system/02-UI-SPEC.md (Screen 5 ManajemenJadwal: table columns, status badges, color tokens; copywriting contract puskesmas section)
    - .planning/phases/02-queue-system/02-RESEARCH.md (API Endpoints — GET /api/jadwal response shape)
  </read_first>
  <action>
    Define a shared JadwalItem interface (in a co-located types comment or inline): { id: string; tanggalPelaksanaan: string; posyandu: { namaPosyandu: string }; slotSesi: Array<{ id: string; kuota: number; terisi: number }>; statusJadwal: 'draft' | 'aktif' | 'terkunci' | 'selesai' | 'dibatalkan'; estimasiDurasiMenit: number }

    STATUS_COLOR_MAP object (define once and import in both components):
    - Use a const object mapping statusJadwal → className string:
      - draft: 'bg-gray-100 text-gray-500 border-gray-200'
      - aktif: 'bg-green-50 text-green-600 border-green-200'
      - terkunci: 'bg-blue-50 text-blue-600 border-blue-200'
      - selesai: 'bg-green-50 text-green-600 border-green-200'
      - dibatalkan: 'bg-gray-100 text-gray-500 border-gray-200'
    - Status display labels: draft → 'DRAFT', aktif → 'AKTIF', terkunci → 'DIKUNCI', selesai → 'SELESAI', dibatalkan → 'DIBATALKAN'

    Step A — Create frontend/src/components/jadwal/JadwalCard.tsx:
    Props: { jadwal: JadwalItem }
    - Card layout (white bg, rounded-lg, border, p-4):
      - Top row: tanggalPelaksanaan formatted as DD/MM/YYYY + status Badge using STATUS_COLOR_MAP
      - Body: posyandu.namaPosyandu in font-bold; "Estimasi {jadwal.estimasiDurasiMenit} menit/orang" in caption
      - Slots row: "3 sesi · {kuota} kuota/sesi" where kuota = slotSesi[0]?.kuota ?? '-'
      - Footer: "Lihat Detail" Button variant="ghost" size="sm" (placeholder — Phase 3 will wire this to /puskesmas/jadwal/:id)
    - Format tanggalPelaksanaan: parse the ISO date string and format as DD/MM/YYYY using toLocaleDateString('id-ID', { day: '2-digit', month: '2-digit', year: 'numeric' })

    Step B — Create frontend/src/components/jadwal/JadwalTable.tsx:
    Props: { jadwals: JadwalItem[]; isLoading: boolean }
    - On isLoading: render 3× TableRow with Skeleton cells
    - Table columns: Tanggal | Posyandu | Sesi | Kuota | Status | Aksi
    - Each data row: formatted tanggalPelaksanaan | namaPosyandu | "3 sesi" | "{kuota}/sesi" | Badge with STATUS_COLOR_MAP | "Lihat Detail" Button variant="ghost" size="sm"
    - Use shadcn Table, TableHeader, TableBody, TableHead, TableRow, TableCell components
    - Table wrapper: `<div className="rounded-md border overflow-hidden">`
  </action>
  <verify>
    <automated>grep -c "STATUS_COLOR_MAP\|statusJadwal" /d/Semester\ 4/PSI/sispos/frontend/src/components/jadwal/JadwalTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - JadwalCard.tsx renders a Card with tanggalPelaksanaan formatted as DD/MM/YYYY (not ISO string)
    - JadwalTable.tsx uses STATUS_COLOR_MAP (or equivalent) to derive badge class from statusJadwal value
    - JadwalTable.tsx renders Skeleton rows when isLoading is true
    - 'AKTIF' status badge uses bg-green-50 text-green-600 className
    - 'DRAFT' status badge uses bg-gray-100 text-gray-500 className
    - 'DIKUNCI' status badge uses bg-blue-50 text-blue-600 className
    - No TypeScript errors in either file
  </acceptance_criteria>
  <done>JadwalCard (mobile) and JadwalTable (desktop) render jadwal data with correct status color tokens and DD/MM/YYYY date formatting.</done>
</task>

<task type="auto">
  <name>Task 2: BuatJadwalDialog + ManajemenJadwalPage (Screens 5 + 6)</name>
  <files>
    frontend/src/components/jadwal/BuatJadwalDialog.tsx
    frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter — shadcn)
    - frontend/src/components/ui/calendar.tsx (Calendar — shadcn; check onSelect prop type: (date?: Date) => void or (date: Date | undefined) => void)
    - frontend/src/components/ui/select.tsx (Select, SelectTrigger, SelectValue, SelectContent, SelectItem)
    - frontend/src/components/ui/form.tsx (Form, FormField, FormItem, FormLabel, FormMessage — shadcn RHF integration)
    - frontend/src/components/ui/input.tsx (Input)
    - frontend/src/lib/validations/jadwal.schema.ts (CreateJadwalFESchema — from 02-04)
    - frontend/src/hooks/useJadwalList.ts (useJadwalList for the table data, useJadwalTersedia not used here)
    - .planning/phases/02-queue-system/02-RESEARCH.md (Pattern 5 TanStack Query — useMutation for POST /api/jadwal; Code Examples: previewSlots function)
    - .planning/phases/02-queue-system/02-UI-SPEC.md (Screen 6 BuatJadwalDialog: form fields, slot preview, footer buttons, success toast, error states; Screen 5 ManajemenJadwalPage: header, table, empty state)
    - .planning/phases/02-queue-system/02-CONTEXT.md (D-07 standalone page; D-08 posyandu dropdown puskesmas-scoped)
  </read_first>
  <action>
    Step A — Create frontend/src/components/jadwal/BuatJadwalDialog.tsx:
    Props: { open: boolean; onOpenChange: (open: boolean) => void; posyanduList: Array<{ id: string; namaPosyandu: string }> }
    - Use React Hook Form: `useForm<CreateJadwalFEInput>({ resolver: zodResolver(CreateJadwalFESchema), defaultValues: { estimasiDurasiMenit: 7 } })`
    - Import zodResolver from '@hookform/resolvers/zod'
    - Form fields in order per 02-UI-SPEC.md Screen 6:
      1. posyanduId: Select with options from posyanduList prop (per D-08: list filtered by backend)
      2. tanggalPelaksanaan: Calendar (shadcn) with disabled prop: `(date) => date <= new Date() || date < new Date()` — disable today and past; tomorrow is earliest. Also disable dates that already have a jadwal for the selected posyandu (fetch jadwalList and check tanggalPelaksanaan matches)
      3. estimasiDurasiMenit: Input type="number" min="5" max="30" step="1"
    - Slot preview section (shown when estimasiDurasiMenit is valid and >= 5):
      - Compute kuota = Math.floor(60 / estimasiDurasiMenit) using watch('estimasiDurasiMenit')
      - Render 3 rows in bg-green-50 p-3 rounded-md caption size:
        "Sesi 1 · 08:00 – 09:00 · {kuota} kuota"
        "Sesi 2 · 09:00 – 10:00 · {kuota} kuota"
        "Sesi 3 · 10:00 – 11:00 · {kuota} kuota"
      - Preview label: "Preview sesi ({estimasi} menit → kuota {kuota}/sesi):"
    - TanStack Query mutation for POST /api/jadwal:
      - `useMutation({ mutationFn: (data: CreateJadwalFEInput) => apiClient.post('/jadwal', { ...data, tanggalPelaksanaan: format(data.tanggalPelaksanaan, 'yyyy-MM-dd') }).then(r => r.data.data), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['jadwal', 'list'] }); onOpenChange(false); toast success }, onError: (error) => { handle 409 JADWAL_SUDAH_ADA by setError('tanggalPelaksanaan', { message: '...' }); else setFormError } })`
      - date-fns is likely already installed (check frontend/package.json); if not, use `data.tanggalPelaksanaan.toISOString().split('T')[0]` to format as YYYY-MM-DD
    - Dialog footer: "Tutup" Button variant="ghost" onClick={() => onOpenChange(false)} | "Buat Jadwal" Button type="submit" with Loader2 animate-spin when pending
    - 409 JADWAL_SUDAH_ADA error: call setError('tanggalPelaksanaan', { message: 'Jadwal untuk posyandu ini pada tanggal tersebut sudah ada.' })
    - Toast (shadcn toast or useToast hook): success → "Jadwal berhasil dibuat. 3 sesi ter-generate otomatis."
    - Use the Toaster component from shadcn — check if it's already in App.tsx from Phase 01; if not, add `<Toaster />` to App.tsx or main layout

    Step B — Create frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx:
    - Import useJadwalList from '@/hooks/useJadwalList' — get { data: jadwals, isLoading }
    - Also call GET /api/posyandu (via apiClient.get or a useQuery hook inline) to populate BuatJadwalDialog posyanduList prop
    - Header section: "Manajemen Jadwal" heading (text-xl font-bold) + "Atur jadwal pelayanan Posyandu" subtitle (text-gray-500)
    - Primary action button: "Buat Jadwal Baru" (top-right on sm:, full-width below header on mobile) — onClick opens BuatJadwalDialog via useState(false)
    - Responsive content: `<div className="block sm:hidden">` for card list (JadwalCard mapped), `<div className="hidden sm:block">` for JadwalTable
    - Empty state (when jadwals.length === 0 and !isLoading): CalendarOff icon (Lucide, size 48, text-gray-300) centered + "Belum ada jadwal Posyandu" heading + "Buat jadwal pertama untuk memulai pelayanan." body + "Buat Jadwal Baru" primary button
    - Loading state: 3× Skeleton blocks (use JadwalTable isLoading prop or manual skeletons)
    - Page: max-width 900px centered, bg-gray-50, min-h-screen, px-4 py-6
    - This page will be routed to /puskesmas/jadwal in Plan 07 (D-07)
    - Figma frame 5:15526 — executor MUST pull this frame before implementing
  </action>
  <verify>
    <automated>grep -c "kuota\|previewSlot\|floor" /d/Semester\ 4/PSI/sispos/frontend/src/components/jadwal/BuatJadwalDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BuatJadwalDialog.tsx renders a slot preview section that computes kuota = Math.floor(60 / estimasiDurasiMenit)
    - BuatJadwalDialog.tsx tanggalPelaksanaan Calendar has disabled past dates (today and before)
    - BuatJadwalDialog.tsx on 409 JADWAL_SUDAH_ADA sets tanggalPelaksanaan field error (not a generic alert)
    - BuatJadwalDialog.tsx on success: closes dialog, invalidates ['jadwal', 'list'] query, shows success toast
    - ManajemenJadwalPage.tsx renders JadwalCard in mobile (block sm:hidden) and JadwalTable in desktop (hidden sm:block)
    - ManajemenJadwalPage.tsx shows empty state with CalendarOff icon when no jadwals
    - No TypeScript errors in the two new files
  </acceptance_criteria>
  <done>BuatJadwalDialog form with real-time slot preview, Calendar date picker, 409 field error handling; ManajemenJadwalPage with responsive table/card layout and empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| puskesmas browser → POST /api/jadwal (via BuatJadwalDialog) | Form data includes posyanduId; server verifies ownership |
| puskesmas browser → GET /api/jadwal (via useJadwalList) | Server filters by puskesmasId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Tampering | BuatJadwalDialog: posyanduId from dropdown | accept | Server verifies posyandu.puskesmasId === req.user.userId in jadwal.service.ts. Client-side dropdown is pre-filtered (D-08) for UX; server is authoritative. |
| T-02-22 | Elevation of Privilege | Citizen accesses /puskesmas/jadwal | mitigate | ProtectedRoute with allowedRoles={['puskesmas']} (wired in Plan 07); server requireRole('puskesmas') on all jadwal write endpoints |
| T-02-23 | Tampering | estimasiDurasiMenit set outside min/max range via DevTools | mitigate | CreateJadwalFESchema validates min 5 max 30 client-side; backend CreateJadwalSchema validates same range server-side |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages; shadcn components already installed in 02-01 |
</threat_model>

<verification>
Manual walkthrough after this plan completes (without router yet — navigate directly in browser):
1. Login as puskesmas
2. Navigate directly to /puskesmas/jadwal (route not in router.tsx yet — test by importing ManajemenJadwalPage in a temp route)
3. Empty state shows: CalendarOff icon + "Belum ada jadwal Posyandu" heading
4. Click "Buat Jadwal Baru" → BuatJadwalDialog opens
5. Select posyandu → calendar appears; past dates are greyed out
6. Select tomorrow's date, enter estimasiDurasiMenit 7 → preview shows kuota 8 for 3 sesi
7. Click "Buat Jadwal" → 201 response, dialog closes, toast appears, jadwal appears in table/card list
8. Re-open dialog, select same posyandu + same date → 409 → field error on tanggal picker
</verification>

<success_criteria>
- BuatJadwalDialog shows real-time slot preview (floor(60/estimasi) × 3)
- BuatJadwalDialog Calendar disables past dates and dates with existing jadwal
- POST /api/jadwal success → dialog closes, success toast, table refreshes
- POST /api/jadwal 409 → tanggalPelaksanaan field error (not generic alert)
- ManajemenJadwalPage responsive: card list mobile, table desktop (sm: breakpoint)
- Status badges use correct color tokens from 02-UI-SPEC.md
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| JadwalCard | frontend/src/components/jadwal/JadwalCard.tsx | React component | Mobile card for jadwal list item with DD/MM/YYYY date |
| JadwalTable | frontend/src/components/jadwal/JadwalTable.tsx | React component | Desktop table with status badges and Skeleton loading |
| BuatJadwalDialog | frontend/src/components/jadwal/BuatJadwalDialog.tsx | React component | Modal form: posyandu select, date picker, duration input, slot preview |
| ManajemenJadwalPage | frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx | React page | Screen 5+6: jadwal list + create dialog (D-07 standalone page) |

<output>
Create `.planning/phases/02-queue-system/02-06-SUMMARY.md` when done
</output>

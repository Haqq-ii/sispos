---
phase: 08-ui-figma-alignment
plan: 12
type: execute
wave: 12
depends_on: [08-11]
files_modified:
  - frontend/src/pages/kader/meja/Meja2Page.tsx
  - frontend/src/pages/kader/meja/Meja3Page.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Meja2Page numpad digit buttons (0-9), decimal, and backspace all have onClick handlers"
    - "Meja2Page BB/TB toggle button switches active measurement input"
    - "Meja2Page Konfirmasi Biologis gate (Dialog) shows for BB > 30kg before submitting"
    - "Meja3Page tanda klinis checkboxes have onChange handlers wired to form state"
    - "Meja3Page Simpan Tanda Klinis button calls the tanda klinis save mutation"
    - "Meja3Page Lanjut ke Meja 4 button navigates with state"
  artifacts:
    - path: "frontend/src/pages/kader/meja/Meja2Page.tsx"
      provides: "Figma-aligned numpad with BB/TB toggle and biologic gate"
      contains: "numpad"
    - path: "frontend/src/pages/kader/meja/Meja3Page.tsx"
      provides: "Z-Score chart display + tanda klinis checkboxes"
      contains: "ZScoreChart"
  key_links:
    - from: "Meja2Page numpad digit"
      to: "BB/TB display value"
      via: "handleNumpad function updating bbStr or tbStr state"
      pattern: "handleNumpad"
    - from: "Meja3Page Simpan"
      to: "PATCH /api/growth/tanda-klinis or equivalent"
      via: "tandaKlinisMutation.mutate"
      pattern: "tandaKlinisMutation"
---

<objective>
Wave 8.12: Meja 2 and Meja 3 Figma alignment.

Meja2Page has a custom numeric keypad for BB (berat badan) and TB (tinggi badan) measurement. The Biological Validation gate shows a Dialog if BB > 30kg before submitting. These are FULL status but need visual alignment to Figma 5:9717 and 5:9865.

Meja3Page shows the Z-Score chart from the pemeriksaan result plus tanda klinis checkboxes for the kader to record clinical signs. FULL status, needs alignment to Figma 2001:6897 and 2001:7160.

Both pages already have TukarMejaModal wired from 08-07. This plan only adjusts visual layout.

Purpose: Meja 2 and 3 are the core health measurement steps. Visual accuracy matters for demo and training.

Output: Meja2Page and Meja3Page JSX updated to match Figma frames.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meja2Page Figma alignment (frames 5:9717, 5:9865)</name>
  <files>frontend/src/pages/kader/meja/Meja2Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja2Page.tsx` — full file; note: bbStr/tbStr state, handleNumpad function, activeMeasure toggle (bb/tb), biologic Dialog trigger (BB > 30), doSubmit/handleSimpan, useCreatePemeriksaan hook, antrian selector, SyncPendingBadge, TukarMejaModal (from 08-07)
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, native checkbox pattern note
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:9717" (Meja 2 list state). Then get_design_context same params. Also get_screenshot nodeId "5:9865" (numpad input state). Study both carefully.

    HEADER alignment:
    - bg-[#008236] header with MEJA 2 title, "Pengukuran BB/TB" subtitle, Tukar Meja button (onClick setShowTukarMeja(true) from 08-07), SyncPendingBadge.
    - Antrian selector at top: show selected balita name prominently (text-[#1e2939] font-bold) with a tap/click to open antrian list for that sesi. If Figma shows a dropdown-style selector for balita, align to that.

    NUMPAD display alignment:
    - BB/TB toggle: two tab buttons "BB (kg)" and "TB (cm)" — active tab: bg-[#008236] text-white rounded-[14px]; inactive: bg-white border border-[#e5e7eb] text-[#364153]. onClick on each: setActiveMeasure('bb') or setActiveMeasure('tb'). Both must have onClick.
    - Value display: large number showing bbStr or tbStr depending on activeMeasure. text-[#1e2939] font-bold text-4xl text-center with unit suffix "kg" or "cm" text-[#99a1af] text-sm.
    - Numpad grid: 4 rows — [1,2,3], [4,5,6], [7,8,9], [.,0,⌫]. Each digit button: bg-white border border-[#e5e7eb] rounded-2xl h-14 text-[#1e2939] font-bold text-xl shadow-sm active:bg-[#f0fdf4]. onClick: handleNumpad(digit). Decimal '.': same style, onClick: handleNumpad('.'). Backspace '⌫': bg-[#fef2f2] border border-[#ffc9c9] rounded-2xl h-14, onClick: handleNumpad('back'). All 12 numpad buttons must have onClick.

    SIMPAN/LANJUT button:
    - "Simpan & Lanjut ke Meja 3" button: bg-[#008236] text-white rounded-[14px] py-3.5 w-full. Verify onClick calls doSubmit or handleSimpan. Disabled when bb or tb is 0/empty or isPending.

    KONFIRMASI BIOLOGIS Dialog:
    - Triggered when BB > 30 — verify Dialog opens with "Konfirmasi Data Biologis" title, description about unusual BB value, Cancel (continue editing) and Confirm (proceed with submit) buttons.
    - Dialog Cancel: onClick closes dialog, returns to editing. Dialog Confirm: onClick calls doSubmit with force=true or skips the validation guard.
    - Verify shadcn Dialog is used (not native confirm()).

    Z-Score result card (after submission): bg-white border rounded-2xl showing WAZ/HAZ/WHZ values and statusGizi badge. This is rendered by existing logic — only align styling.

    Do NOT change: useCreatePemeriksaan hook, handleNumpad logic, biologic validation (BB>30 threshold), navigate on success, offline branch, TukarMejaModal.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja2Page.tsx contains "onClick" on BB toggle button setting activeMeasure to 'bb'
    - Meja2Page.tsx contains "onClick" on TB toggle button setting activeMeasure to 'tb'
    - Meja2Page.tsx contains "handleNumpad" onClick on each numpad digit button (verify at least '1', '5', '0', 'back')
    - Meja2Page.tsx contains "handleNumpad('back')" on the backspace numpad button
    - Meja2Page.tsx contains "doSubmit" or "handleSimpan" on the Simpan button onClick
    - Meja2Page.tsx contains "disabled" condition on Simpan button
    - Meja2Page.tsx contains "Konfirmasi" Dialog with Cancel and Confirm buttons (biologic gate)
    - Meja2Page.tsx contains "bg-[#008236]" on Simpan button
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja2Page visually aligned to Figma 5:9717 and 5:9865. All 12 numpad buttons have onClick. BB/TB toggle works. Biologic gate Dialog has Cancel and Confirm. Simpan wired to mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Meja3Page Figma alignment (frames 2001:6897, 2001:7160)</name>
  <files>frontend/src/pages/kader/meja/Meja3Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja3Page.tsx` — full file; note: ZScoreChart usage, tanda klinis checkboxes (native input type="checkbox" per decision 2026-07-02), tandaKlinisMutation, navigate to Meja 4, Tukar Meja button (from 08-07), balita info display
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 3.2 Pattern 3 (native checkbox — @radix-ui/react-checkbox NOT installed)
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:6897". Then get_design_context same params. Also get_screenshot nodeId "2001:7160". Study both states (normal and with klinis checked).

    HEADER alignment:
    - bg-[#008236] with MEJA 3 title, "Analisis Z-Score" subtitle, Tukar Meja button (onClick setShowTukarMeja(true) from 08-07).
    - Balita name + selected antrian info displayed under header.

    Z-SCORE CHART SECTION:
    - Section label "GRAFIK PERTUMBUHAN" text-[#99a1af] text-xs font-semibold tracking-wider.
    - ZScoreChart component wrapped in bg-white border border-[#f3f4f6] rounded-2xl p-4 shadow-sm.
    - Z-Score result badges (WAZ, HAZ, WHZ values) from pemeriksaan data — each as bg-[#f3f4f6] rounded-xl px-3 py-1.5 inline-flex items-center gap-1.
    - statusGizi badge with appropriate color: use getStatusStyle function or equivalent.

    TANDA KLINIS SECTION:
    - Section label "TANDA KLINIS" text-[#99a1af] text-xs font-semibold tracking-wider.
    - List of checkboxes: each tanda klinis item as a div flex items-center gap-3 py-2.5 border-b border-[#f3f4f6] last:border-0.
    - Native checkbox: `<input type="checkbox" checked={value} onChange={(e) => onChange(e.target.checked)} className="w-4 h-4 rounded accent-[#008236] cursor-pointer flex-shrink-0" />` (per codebase pattern — @radix-ui/react-checkbox NOT available).
    - Label text-[#1e2939] text-sm for each tanda klinis item.
    - All checkboxes must have onChange handler wired to react-hook-form or local state. Verify each onChange is present.

    SIMPAN TANDA KLINIS button:
    - bg-[#008236] text-white rounded-[14px] py-3.5 w-full.
    - onClick: tandaKlinisMutation.mutate(formData) or equivalent. Disabled when isPending.
    - Verify it exists and has the onClick.

    LANJUT KE MEJA 4 button:
    - "Lanjut ke Meja 4" or "Selesai Meja 3" button.
    - onClick: navigate('/kader/meja/4', { state: { antrianId, balitaId, namaBalita, pemeriksaanId } }).
    - Verify this button exists and has the navigate call.
    - Style: bg-[#1447e6] text-white or per Figma color for the navigation CTA.

    Do NOT change: ZScoreChart data source, tanda klinis form schema, tandaKlinisMutation logic, navigate state passing, TukarMejaModal from 08-07.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja3Page.tsx contains "ZScoreChart" component render
    - Meja3Page.tsx contains "type=\"checkbox\"" (native checkbox for tanda klinis — not radix)
    - Meja3Page.tsx contains "onChange" handler on each tanda klinis checkbox (wired to form/state)
    - Meja3Page.tsx contains "accent-[#008236]" on checkbox className (native checkbox green accent)
    - Meja3Page.tsx contains "tandaKlinisMutation" reference on Simpan button onClick
    - Meja3Page.tsx contains "disabled" condition on Simpan button
    - Meja3Page.tsx contains "navigate" call to '/kader/meja/4' on Lanjut ke Meja 4 button
    - Meja3Page.tsx contains "setShowTukarMeja(true)" on Tukar Meja button (from 08-07)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja3Page visually aligned to Figma 2001:6897 and 2001:7160. ZScoreChart renders. All tanda klinis checkboxes have onChange. Simpan calls mutation. Lanjut navigates to Meja 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader → POST /api/growth/pemeriksaan | Kader submits BB/TB measurement (scoped to posyandu via JWT) |
| Kader → PATCH tanda klinis endpoint | Kader records clinical signs per balita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12-01 | Tampering | Numpad allows arbitrary BB/TB values | accept | Backend validates reasonable ranges; biologic gate for BB > 30kg adds UX warning; WHO Z-Score calculation uses validated table |
| T-08-12-02 | Tampering | Tanda klinis checkboxes client-side only | accept | Server validates pemeriksaanId belongs to kader's posyandu before accepting update; IDOR guard from Phase 3 |
| T-08-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 12 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /kader/meja/2 — numpad renders 12 buttons (0-9, decimal, backspace), BB/TB toggle switches display, Simpan button visible
3. Browser: enter BB > 30 and tap Simpan — Dialog opens with Konfirmasi Biologis
4. Browser: /kader/meja/3 — ZScoreChart visible, tanda klinis checkboxes clickable, Simpan and Lanjut ke Meja 4 buttons visible
</verification>

<success_criteria>
- UI-02: Meja2Page aligned to Figma 5:9717 and 5:9865 — numpad, BB/TB toggle, biologic Dialog
- UI-02: Meja3Page aligned to Figma 2001:6897 and 2001:7160 — ZScoreChart, native checkboxes, simpan + navigate buttons
- All button handlers verified functional
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-12-SUMMARY.md` when done.
</output>

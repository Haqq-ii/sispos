---
phase: 08-ui-figma-alignment
plan: 13
type: execute
wave: 13
depends_on: [08-12]
files_modified:
  - frontend/src/pages/kader/meja/Meja4Page.tsx
  - frontend/src/pages/kader/meja/Meja5Page.tsx
autonomous: true
requirements: [UI-02, UI-03]
must_haves:
  truths:
    - "Meja4Page mic button is w-16 h-16 rounded-full circle: bg-[#008236] when ready, bg-[#e7000b] when recording"
    - "Meja4Page auto-stop 45s timer shows countdown text during recording"
    - "Meja4Page Simpan Catatan button calls the catatan save mutation"
    - "Meja5Page Selesai Pelayanan button calls selesaiMutation (updates durasiRataAktual + Socket.IO)"
    - "Meja5Page Tambah Imunisasi form has Simpan button wired to tambahImunisasiMutation"
    - "Meja5Page Lihat Riwayat / imunisasi list toggle shows fetched imunisasi data"
  artifacts:
    - path: "frontend/src/pages/kader/meja/Meja4Page.tsx"
      provides: "Mic circle button with recording state + STT integration"
      contains: "rounded-full"
    - path: "frontend/src/pages/kader/meja/Meja5Page.tsx"
      provides: "Imunisasi list + tambah form + Selesai Pelayanan"
      contains: "selesaiMutation"
  key_links:
    - from: "Meja4Page mic button"
      to: "handleToggleRecording"
      via: "onClick toggles isRecording state and starts/stops Google STT"
      pattern: "handleToggleRecording"
    - from: "Meja5Page Selesai Pelayanan button"
      to: "PATCH /api/antrian/:id/selesai"
      via: "selesaiMutation.mutate + durasiRataAktual update + Socket.IO broadcast"
      pattern: "selesaiMutation"
---

<objective>
Wave 8.13: Meja 4 and Meja 5 Figma alignment — the final two steps of the kader 5-meja flow.

Meja4Page (Konsultasi & Penyuluhan) has a voice recording mic button, 45s auto-stop timer, STT textarea, and AI early warning integration. The mic button must be a w-16 h-16 rounded-full circle (not a wide border button) per Figma 2001:7403 and 2001:7601.

Meja5Page (Imunisasi) shows imunisasi riwayat for the current balita, a Tambah Imunisasi form, and the critical "Selesai Pelayanan" button that updates durasiRataAktual and broadcasts via Socket.IO — this is the core mechanic keeping countdown estimates accurate for citizens.

Both pages already have TukarMejaModal wired from 08-07.

Purpose: Meja 4 and 5 complete the 5-meja flow. Meja 5 Selesai is the most critical action in the entire system.

Output: Meja4Page mic circle aligned. Meja5Page imunisasi list + Selesai Pelayanan verified working.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meja4Page Figma alignment — mic circle + STT + simpan (frames 2001:7403, 2001:7601)</name>
  <files>frontend/src/pages/kader/meja/Meja4Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja4Page.tsx` — full file; note: handleToggleRecording, isRecording state, timeLeft 45s countdown, useVoiceRecorder hook, catatanText state/textarea, handleSimpanCatatan, AI early warning integration, Tooltip for offline state, TukarMejaModal (from 08-07)
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4: "Meja 4 mic: bg-[#008236] ready / bg-[#e7000b] recording" decision from STATE.md
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:7403" (Meja 4 ready state). Then get_design_context same params. Also get_screenshot nodeId "2001:7601" (recording state). Study the mic circle design — it should be a centered rounded-full circle, NOT a wide border button.

    HEADER alignment:
    - bg-[#008236] with MEJA 4 title, "Konsultasi & Penyuluhan" subtitle, Tukar Meja button (setShowTukarMeja(true) from 08-07).
    - Balita name prominently displayed.

    MIC BUTTON alignment (STATE.md decision 2026-07-05):
    - The mic button must be: `<button className="w-16 h-16 rounded-full flex items-center justify-center shadow-lg transition-colors {isRecording ? 'bg-[#e7000b]' : 'bg-[#008236]'}" onClick={handleToggleRecording} disabled={!isOnline}>` with Mic icon size={28} className="text-white" inside.
    - Center the mic button: wrap in `<div className="flex flex-col items-center gap-4 py-6">`.
    - Below button: if isRecording show countdown timer `<p className="text-[#e7000b] font-bold text-xl">{timeLeft}s</p>` and "Merekam..." text; if !isRecording show "Ketuk untuk merekam" text-[#99a1af] text-sm.
    - If offline: Tooltip wraps the button showing "Perekaman tidak tersedia saat offline" on hover. Disabled state visual.

    STT TEXTAREA alignment:
    - Section label "CATATAN KONSULTASI" text-[#99a1af] text-xs font-semibold tracking-wider.
    - Native `<textarea>` (per decision 2026-07-02 — @radix-ui/react-textarea not available): className="w-full bg-[#f9fafb] border border-[#e5e7eb] rounded-2xl px-4 py-3 text-sm text-[#1e2939] resize-none focus:outline-none focus:ring-2 focus:ring-[#008236]" rows={5} value={catatanText} onChange={(e) => setCatatanText(e.target.value)} placeholder="Catatan konsultasi akan muncul di sini setelah perekaman..."
    - Verify onChange sets catatanText state.

    SIMPAN CATATAN button:
    - "Simpan & Lanjut ke Meja 5" button: bg-[#008236] text-white rounded-[14px] py-3.5 w-full.
    - onClick: handleSimpanCatatan (saves catatan + triggers AI early warning + navigates to Meja 5).
    - disabled when isPending or (catatan empty AND no significant justification from Figma — leave as-is).
    - Verify handleSimpanCatatan is wired.

    AI EARLY WARNING result card (if rendered): bg-[#fff7ed] border border-[#fed7aa] rounded-2xl p-4 for warning state; bg-[#f0fdf4] border border-[#b9f8cf] for normal state. Content: AI recommendation text (from rekomendasiAi, decrypted).

    Do NOT change: handleToggleRecording logic, useVoiceRecorder hook, AI early warning mutation, navigate state, offline Tooltip, TukarMejaModal from 08-07.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja4Page.tsx contains "w-16 h-16 rounded-full" on the mic button className
    - Meja4Page.tsx contains "bg-[#e7000b]" in the mic button className (recording state color)
    - Meja4Page.tsx contains "bg-[#008236]" in the mic button className (ready state color)
    - Meja4Page.tsx contains "onClick={handleToggleRecording}" on the mic button
    - Meja4Page.tsx contains "timeLeft" display (45s countdown timer rendered)
    - Meja4Page.tsx contains "onChange" on the catatan textarea (editable)
    - Meja4Page.tsx contains "handleSimpanCatatan" on Simpan button onClick
    - Meja4Page.tsx contains "setShowTukarMeja(true)" on Tukar Meja button (from 08-07)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja4Page mic button is a centered w-16 h-16 rounded-full circle with correct green/red state colors. STT textarea has onChange. Simpan calls handleSimpanCatatan.</done>
</task>

<task type="auto">
  <name>Task 2: Meja5Page Figma alignment — imunisasi list + Selesai Pelayanan (frames 5:11942, 5:12149, 5:12594)</name>
  <files>frontend/src/pages/kader/meja/Meja5Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja5Page.tsx` — full file; note: imunisasi useQuery, tambahImunisasiMutation, selesaiMutation, showTambahForm state toggle, VAKSIN_OPTIONS array, handleSelesai (durasiRataAktual + navigate), SyncPendingBadge, offline branch, TukarMejaModal (from 08-07)
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens; orange accent #e17100 for Meja 5 header per design tokens table
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:11942" (Meja 5 main). Then get_design_context same params. Also get_screenshot nodeId "5:12149" (imunisasi list view) and "5:12594" (tambah imunisasi form). Study all three states.

    HEADER alignment:
    - Meja 5 header: bg-[#e17100] (orange accent per RESEARCH.md design tokens, NOT green) px-4 pt-10 pb-4.
    - Title: "MEJA 5 — Imunisasi" text-white font-bold text-sm. Subtitle "Catat Riwayat Vaksin" text-[rgba(255,255,255,0.8)] text-xs.
    - Tukar Meja button: bg-[rgba(0,0,0,0.15)] rounded-xl text-white text-xs (orange header so button uses dark overlay, not green).
    - SyncPendingBadge preserved.

    IMUNISASI RIWAYAT SECTION:
    - Section label "RIWAYAT IMUNISASI" text-[#99a1af] text-xs font-semibold tracking-wider.
    - Each imunisasi item: bg-white border border-[#f3f4f6] rounded-[14px] px-4 py-3 flex items-center gap-3.
      - Syringe icon text-[#e17100].
      - namaVaksin text-[#1e2939] font-semibold text-sm; "Dosis ke-{dosisKe}" text-[#99a1af] text-xs; tanggalInjeksi formatted as DD/MM/YYYY.
    - Empty state: "Belum ada riwayat imunisasi." text-[#99a1af] text-sm text-center py-4.
    - isLoadingImunisasi: Skeleton component.

    TAMBAH IMUNISASI:
    - "Tambah Imunisasi" button: border border-[#e17100] text-[#e17100] rounded-[14px] py-2.5 px-4 font-semibold text-sm. Plus icon. onClick: setShowTambahForm(true). Verify onClick present.
    - When showTambahForm: collapsible form within the same card (or slide-up panel per Figma):
      - Vaksin Select: shadcn `<Select>` with VAKSIN_OPTIONS items. value={namaVaksin} onValueChange={setNamaVaksin}. Verify onValueChange present.
      - Dosis input: `<input type="number" min={1} max={4}` value={dosisKe} onChange={setDosisKe}. Verify onChange present.
      - Tanggal input (optional): date input defaulting to today.
      - Simpan button: bg-[#e17100] text-white rounded-[14px] py-2.5 onClick: tambahImunisasiMutation.mutate({ balitaId, namaVaksin, dosisKe, tanggalInjeksi }). Disabled when namaVaksin empty or isPending. Verify onClick.
      - Batal button: onClick setShowTambahForm(false). Verify onClick.

    SELESAI PELAYANAN button (CRITICAL):
    - "Selesai Pelayanan" button: bg-[#008236] text-white font-bold py-4 rounded-2xl w-full text-base. CheckCircle icon size={20}.
    - onClick: handleSelesai or selesaiMutation.mutate (updates durasiRataAktual + Socket.IO broadcast + navigate to /kader/rekap).
    - Disabled when selesaiMutation.isPending.
    - This button MUST call selesaiMutation — verify the onClick is wired correctly. This is the most critical button in the entire kader flow.

    Do NOT change: selesaiMutation logic (durasiRataAktual formula), offline branch, handleSelesai navigate, imunisasi useQuery, TukarMejaModal from 08-07.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja5Page.tsx contains "bg-[#e17100]" on header (orange accent for Meja 5)
    - Meja5Page.tsx contains "onClick" on "Tambah Imunisasi" button that sets showTambahForm true
    - Meja5Page.tsx contains "onValueChange" on vaksin Select (dropdown selection handler)
    - Meja5Page.tsx contains "tambahImunisasiMutation" on Simpan imunisasi button onClick
    - Meja5Page.tsx contains "disabled" condition on Simpan imunisasi button
    - Meja5Page.tsx contains "onClick" on Batal (tambah form) that sets showTambahForm false
    - Meja5Page.tsx contains "selesaiMutation" or "handleSelesai" on Selesai Pelayanan button onClick
    - Meja5Page.tsx contains "disabled" condition on Selesai Pelayanan button referencing isPending
    - Meja5Page.tsx contains "bg-[#008236]" on Selesai Pelayanan button
    - Meja5Page.tsx contains "setShowTukarMeja(true)" on Tukar Meja button (from 08-07)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja5Page aligned to Figma 5:11942, 5:12149, 5:12594. Orange header. Tambah Imunisasi form has all handlers. Selesai Pelayanan button verified to call selesaiMutation (durasiRataAktual + Socket.IO broadcast).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader → PATCH /api/antrian/:id/selesai | Kader marks antrian selesai, triggers durasiRataAktual update and Socket.IO broadcast to citizen |
| Kader → POST /api/imunisasi | Kader adds imunisasi record (must be scoped to posyandu) |
| Kader → POST /api/voice/transcribe | STT audio sent to backend for Google Cloud STT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13-01 | Tampering | Selesai Pelayanan updates durasiRataAktual globally for jadwal | accept | durasiRataAktual is a moving average — any outlier is smoothed out; scoped to jadwal/slotId owned by the kader's posyandu |
| T-08-13-02 | Information Disclosure | catatanKonsultasi and rekomendasiAi (Meja 4) | mitigate | These columns are encrypted at rest per UU PDP (APP_ENCRYPTION_KEY) from Phase 3; encryption is backend responsibility — no change needed here |
| T-08-13-03 | Denial of Service | 45s STT recording uploads audio to backend | accept | Backend voice.service.ts has graceful degradation (try-catch); if STT fails, textarea is still editable manually |
| T-08-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 13 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /kader/meja/4 — mic button is circular (rounded-full), green in ready state; click toggles to red recording state with countdown timer
3. Browser: /kader/meja/5 — orange header (#e17100), imunisasi riwayat list (or empty state), Tambah Imunisasi button visible
4. Browser: click Tambah Imunisasi — form expands with vaksin Select, Simpan (orange), Batal buttons
5. Browser: Selesai Pelayanan button visible at bottom, green, calls mutation on click
6. Confirm Socket.IO broadcast: after Selesai clicked, check browser Network tab for socket event emission
</verification>

<success_criteria>
- UI-02: Meja4Page mic circle aligned to Figma 2001:7403/7601 — correct rounded-full, green/red colors
- UI-02: Meja5Page aligned to Figma 5:11942/12149/12594 — orange header, imunisasi list, tambah form
- UI-03: Selesai Pelayanan wired to selesaiMutation (durasiRataAktual + Socket.IO broadcast to citizens) — core system mechanic verified
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-13-SUMMARY.md` when done.
</output>

---
phase: 08-ui-figma-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [UI-03]
must_haves:
  truths:
    - "npx prisma db seed runs to completion with zero Error: lines in output"
    - "Login Citizen NIK 3471012345670001 / Demo1234! shows antrian aktif nomorUrut 3"
    - "Login Kader HP 081234560001 / PIN 123456 shows 4 sesi today in KaderDashboardPage"
    - "Login Puskesmas demo@puskesmas-mergangsan.go.id / Demo1234! shows stats in dashboard"
    - "ChatAssistantPage returns Bahasa Indonesia answer for a gizi question"
    - "ChatAssistantPage triggers daftar_antrian tool only after explicit citizen konfirmasi"
    - "ChatAssistantPage rate-limit at 20 messages/day returns appropriate Bahasa Indonesia error"
  artifacts:
    - path: ".planning/phases/08-ui-figma-alignment/08-01-SUMMARY.md"
      provides: "Wave 1 verification outcome + any seed failures or AI chatbot issues"
  key_links:
    - from: "docker compose exec sispos-backend"
      to: "npx prisma db seed"
      via: "prisma/seed.ts orchestrator → seedMassal → seedDemo → seedToday"
      pattern: "seed.ts"
    - from: "ChatAssistantPage"
      to: "POST /api/ai/chat/assistant"
      via: "apiClient.post in ChatAssistantPage.tsx"
      pattern: "chat/assistant"
---

<objective>
Wave 8.1: Execute the two blocking verification checkpoints (07-03 and 04-04) that were completed at implementation level but never received human sign-off. No source code is modified; this wave is pure verification.

Purpose: Phase 8 cannot claim the application is demo-ready until (a) seed data fills the DB with realistic demo accounts, and (b) the AI chatbot pendaftaran flow is manually confirmed to work end-to-end including explicit confirmation gate and rate limiting.

Output: 08-01-SUMMARY.md with pass/fail status for each verification criterion, plus any error output from the seed run for debugging.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run seed pipeline and capture output</name>
  <files>prisma/seed.ts, prisma/seed.demo.ts, prisma/seed.today.ts</files>

  <read_first>
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 5.4 (Plan 07-03 status, risk assessment, expected verification steps)
    - `.planning/STATE.md` — decisions section; BCRYPT_ROUNDS=10 used in seed.demo.ts
    - `prisma/seed.ts` — verify orchestrator calls seedDemo + seedToday at the end
    - `prisma/seed.today.ts` — confirm 4 sesi (08:00, 09:00, 10:00, 11:00) with durasiRataAktual=10
    - `prisma/seed.demo.ts` — confirm seedDemo creates Sari Dewi balita at nomorUrut=3
  </read_first>

  <action>
    Run the seed pipeline inside the running sispos-backend container: execute `docker compose exec sispos-backend npx prisma db seed` from the project root. Capture the complete stdout/stderr. Do NOT proceed if the command returns a non-zero exit code — report the full error output in the summary and halt.

    After successful seed, run a quick DB sanity check: execute `docker compose exec sispos-backend npx prisma db execute --file -` with a SQL snippet counting rows in key tables: `SELECT COUNT(*) AS balita_count FROM balita; SELECT COUNT(*) AS antrian_count FROM antrian WHERE "createdAt"::date = CURRENT_DATE;`

    Expected: balita_count > 100, antrian_count >= 4 (the 4 sesi for Dewi + others today).

    Report findings: seed output tail (last 30 lines), DB count results, any "Error:" lines found in output.
  </action>

  <verify>
    <automated>docker compose exec sispos-backend npx prisma db seed 2>&amp;1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Seed command exits with code 0 (no unhandled Promise rejection, no "Error:" in output)
    - Output contains "Seeding complete" or equivalent completion message from seed.ts
    - DB row count shows balita_count > 100
    - DB row count shows antrian_count > 0 for today's date
    - No orphaned foreign key errors in the output
  </acceptance_criteria>

  <done>Seed pipeline ran to completion with zero errors; DB row counts confirm data is present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 5 login scenarios in browser</name>

  <read_first>
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 5.4, verification checklist
    - `.planning/STATE.md` — demo account credentials (citizen NIK, kader HP/PIN, puskesmas email/password)
  </read_first>

  <what-built>Task 1 ran npx prisma db seed which populated the database with 375+ balita, 3 puskesmas, 15 posyandu, 12-month pemeriksaan history, demo accounts, and 4 antrian slots for today.</what-built>

  <how-to-verify>
    Open http://localhost in a browser. Verify all 5 scenarios:

    Scenario 1 — Citizen login:
    - Navigate to http://localhost/login
    - Enter NIK: 3471012345670001, password: Demo1234!
    - Expected: redirected to /citizen/dashboard showing greeting with namaLengkap
    - Expected: antrian aktif card visible with nomorUrut=3 and posyandu name

    Scenario 2 — Citizen balita count:
    - While logged in as citizen, navigate to /citizen/tumbuh-kembang
    - Expected: Riwayat tab shows at least 1 pemeriksaan record (BB, TB, Z-Score values, not "--")

    Scenario 3 — Kader login:
    - Logout, navigate to /login
    - Enter HP: 081234560001, PIN: 123456 (6 digit)
    - Expected: redirected to /kader/dashboard showing antrian or jadwal hari ini
    - Expected: at least 1 jadwal card showing today's date with 4 sesi

    Scenario 4 — Puskesmas login:
    - Logout, navigate to /login
    - Enter email: demo@puskesmas-mergangsan.go.id, password: Demo1234!
    - Expected: redirected to /puskesmas/dashboard showing stats (balita count, posyandu count)

    Scenario 5 — Dashboard data not empty:
    - On puskesmas dashboard, navigate to Peta Stunting (/puskesmas/peta-stunting)
    - Expected: map renders with at least 1 CircleMarker (posyandu with lat/lng from seed)
  </how-to-verify>

  <resume-signal>Type "seed-verified" if all 5 scenarios pass. If any fail, describe which scenario failed and the error/behavior observed.</resume-signal>

  <acceptance_criteria>
    - Citizen login redirects to /citizen/dashboard without white screen or 401
    - KaderDashboardPage shows at least 1 jadwal entry for today
    - PuskesmasDashboardPage shows numeric stats (non-zero counts)
    - No "Gagal memuat data" error toasts on any of the 3 dashboards
    - Citizen TumbuhKembangPage riwayat tab shows at least 1 real record (not "Belum ada data pemeriksaan")
  </acceptance_criteria>

  <done>All 5 login scenarios confirmed by human; seed data is correct and demo-ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify AI chatbot — gizi + pendaftaran + rate limit</name>

  <read_first>
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 5.3 (Plan 04-04, ChatAssistantPage architecture)
    - `.planning/STATE.md` — AI chatbot decisions (parallel_tool_calls:false, confirmation gate, rate limit 20/day WIB)
    - `CLAUDE.md` — AI chatbot constraints (temperature 0.4 for pendaftaran, 0.6 for gizi, guardrail, rate limit)
  </read_first>

  <what-built>Backend ai-pendaftaran.service.ts and ai-assistant.service.ts exist with unified /api/ai/chat/assistant endpoint. ChatAssistantPage.tsx calls this endpoint. The endpoint handles both gizi questions (GPT-4o temp 0.6, guardrail) and pendaftaran function calling (5 tools, parallel_tool_calls:false, confirmation gate).</what-built>

  <how-to-verify>
    Login as citizen (NIK 3471012345670001 / Demo1234!). Navigate to /citizen/chat-assistant.

    Test A — Gizi question (AI-01):
    - Type: "Apa makanan yang baik untuk balita usia 6 bulan?"
    - Expected: Response in Bahasa Indonesia about infant nutrition. No English text.
    - Expected: Response does NOT mention antrian or registering.

    Test B — Off-topic rejection (AI-01 guardrail):
    - Type: "Siapa presiden Indonesia sekarang?"
    - Expected: Polite refusal in Bahasa Indonesia, no answer about politics.

    Test C — Pendaftaran flow (AI-02):
    - Type: "Saya mau daftar antrian besok"
    - Expected: AI calls get_jadwal_tersedia, shows available jadwal options, asks which to choose.
    - Pick a slot from the options shown.
    - Expected: AI calls get_profil_balita, shows balita profile summary, asks for konfirmasi sebelum mendaftar.
    - Type: "ya, daftarkan" (explicit confirmation)
    - Expected: AI calls daftar_antrian, antrian terdaftar → confirmation message with nomorUrut.

    Test D — Rate limit (AI-01):
    - This scenario only applies if 20+ messages have been sent today.
    - If rate limit is hit, expected: "Maaf, Anda telah mencapai batas 20 pesan hari ini" or similar in Bahasa Indonesia.
    - If not yet at limit, skip and note "rate limit not tested — insufficient messages today".

    Test E — OPENAI_API_KEY check:
    - Before testing, confirm: `docker compose exec sispos-backend sh -c 'echo $OPENAI_API_KEY'`
    - If empty: document as "AI features non-functional — OPENAI_API_KEY not configured" and skip Tests A-D.
    - Notify the developer to configure OPENAI_API_KEY in the Docker environment.
  </how-to-verify>

  <resume-signal>Type "ai-verified: [A-pass/fail, B-pass/fail, C-pass/fail, D-skipped/pass/fail, E-key-present/missing]" to resume. Example: "ai-verified: A-pass, B-pass, C-pass, D-skipped, E-key-present"</resume-signal>

  <acceptance_criteria>
    - Test A: Response is in Bahasa Indonesia and contains food/nutrition content relevant to balita
    - Test B: Response refuses with polite Bahasa Indonesia message; no political answer given
    - Test C: daftar_antrian tool is NOT called before human types explicit confirmation ("ya"/"konfirmasi"); antrian appears in DB after confirmation
    - Test D: if triggered, rate limit message is in Bahasa Indonesia (not an unhandled 429 JSON error)
    - Test E: OPENAI_API_KEY is present OR developer is notified to configure it
  </acceptance_criteria>

  <done>AI chatbot gizi guardrail, pendaftaran flow with explicit confirmation gate, and rate limiting all verified in-browser against the running application. Results documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → sispos-backend container | Docker exec runs prisma db seed as privileged operation inside the container |
| Browser → /api/ai/chat/assistant | Citizen JWT required; request body contains chat message string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | Demo seed credentials (NIK/PIN/email) | accept | Academic project; credentials are intentionally public for demo purposes; not a production system |
| T-08-01-02 | Spoofing | AI chatbot daftar_antrian confirmation gate | accept | parallel_tool_calls:false already implemented in ai-pendaftaran.service.ts per decisions log 2026-07-03; verification task confirms it works |
| T-08-01-03 | Denial of Service | Rate limit 20 msg/day per citizen | accept | Redis-based rate limit already implemented; Test D verifies it |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; seed run only executes existing seed.ts scripts |
</threat_model>

<verification>
All three tasks must pass before Wave 8.2 begins:
1. `docker compose exec sispos-backend npx prisma db seed` exits 0 — automated
2. 5 login scenarios pass in browser — human checkpoint
3. AI chatbot gizi + pendaftaran + rate limit pass — human checkpoint

If Task 1 fails (seed error): debug seed.ts output, identify failing seedMassal/seedDemo/seedToday step, fix before proceeding.
If Task 2 fails (login fails): check if Task 1 actually succeeded (check DB counts), verify JWT cookie is being set correctly.
If Task 3 fails (AI not responding): check `docker compose exec sispos-backend sh -c 'echo $OPENAI_API_KEY'`; if empty, configure it before re-running.
</verification>

<success_criteria>
- npx prisma db seed runs to completion without errors (SC-4 prerequisite: data for visual population)
- 5 login scenarios pass (SC-4: AI chatbot + realtime features require real data in DB)
- AI chatbot responds in Bahasa Indonesia for gizi questions (SC-4c: AI Chatbot respon dalam Bahasa Indonesia)
- Confirmation gate prevents daftar_antrian without explicit citizen input (AI-02, AI-03 requirements)
</success_criteria>

## Artifacts This Phase Produces

- `.planning/phases/08-ui-figma-alignment/08-01-SUMMARY.md` — seed output, login verification results, AI chatbot test results

<output>
Create `.planning/phases/08-ui-figma-alignment/08-01-SUMMARY.md` when done.
</output>

---
phase: 08-ui-figma-alignment
plan: 09
type: execute
wave: 9
depends_on: [08-08]
files_modified:
  - frontend/src/pages/citizen/antrian/PilihTanggalPage.tsx
  - frontend/src/pages/citizen/antrian/PilihSesiPage.tsx
  - frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx
  - frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "PilihTanggalPage: calendar day tap selects date, Lanjutkan button navigates to PilihSesi"
    - "PilihSesiPage: sesi card tap selects slot, Lanjutkan navigates to KonfirmasiAntrian"
    - "KonfirmasiAntrianPage: balita radio select, Daftar Sekarang calls ambilAntrianMutation, Batal navigates back"
    - "TiketAntrianPage: BatalkanAntrianDialog button present for menunggu status, countdown shows with ± prefix"
  artifacts:
    - path: "frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx"
      provides: "Tiket with realtime countdown and batalkan dialog"
      contains: "BatalkanAntrianDialog"
  key_links:
    - from: "KonfirmasiAntrianPage Daftar Sekarang"
      to: "POST /api/antrian/ambil"
      via: "ambilAntrianMutation.mutate or useAmbilAntrian hook"
      pattern: "ambilAntrian"
    - from: "TiketAntrianPage"
      to: "Socket.IO sesi:{slotId} room"
      via: "useAntrianSocket hook"
      pattern: "useAntrianSocket"
---

<objective>
Wave 8.9: Citizen antrian flow Figma alignment — 4 screens from date picker to tiket.

These 4 screens form the core citizen value proposition (queue registration). All are FULL status but need visual alignment against Figma and button handler verification. The Socket.IO realtime countdown on TiketAntrianPage is the most critical element — it must show the ± prefix and update without page refresh.

Purpose: The antrian flow is the primary citizen interaction. Any visual gap or broken button erodes trust in the system.

Output: PilihTanggal, PilihSesi, KonfirmasiAntrian, TiketAntrian aligned to Figma with all buttons verified.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PilihTanggalPage + PilihSesiPage Figma alignment</name>
  <files>
    frontend/src/pages/citizen/antrian/PilihTanggalPage.tsx,
    frontend/src/pages/citizen/antrian/PilihSesiPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/citizen/antrian/PilihTanggalPage.tsx` — full file; note AntrianKalender component usage, selectedDate state, Lanjutkan button onClick, Back button, useAntrianStore calls
    - `frontend/src/pages/citizen/antrian/PilihSesiPage.tsx` — full file; note sesi card tap handler (setSelectedSlotId), Lanjutkan onClick, Back button, useAntrianStore
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:2473" (PilihTanggal). Then get_design_context same params. Also get_screenshot nodeId "5:2767" (PilihSesi). Study both frames.

    PILIH TANGGAL PAGE — visual alignment:
    - Header: green bg-[#008236] px-4 pt-10 pb-6 — title "Pilih Tanggal", back button (ChevronLeft) with onClick={() => navigate(-1) or navigate('/citizen/dashboard')}.
    - Calendar: AntrianKalender renders react-day-picker — verify it's inside a bg-white rounded-2xl card with proper shadow.
    - Selected date chip: show selected date as "DD/MM/YYYY" in a highlighted chip below calendar when selectedDate is not null.
    - Lanjutkan button: bg-[#008236] text-white rounded-[14px] py-3.5 w-full. onClick must call navigate('/citizen/antrian/sesi') only if selectedDate is not null. Verify disabled={!selectedDate} attribute is present.
    - Location note (if Figma shows it): MapPin icon + posyandu name from store.
    Do NOT change AntrianKalender component logic, useJadwalTersedia hook, or useAntrianStore.setSelectedDate calls.

    PILIH SESI PAGE — visual alignment:
    - Header: green bg-[#008236] with "Pilih Sesi" title and back ChevronLeft.
    - Sesi cards: each sesi shown as bg-white rounded-2xl card with sesi time, kuota/sisa display, radio or border highlight when selected.
    - Card onClick: must call setSelectedSlotId(sesi.id) from useAntrianStore (or equivalent store setter). Selected card gets border-[#008236] or bg-[#f0fdf4] highlight.
    - Kuota display: "N sisa" shown per card. Cards at 0 kuota shown as disabled/grayed with no onClick.
    - Lanjutkan button: bg-[#008236] disabled={!selectedSlotId}, onClick navigates to /citizen/antrian/konfirmasi passing jadwalId in navigate state.
    Preserve: useSesiAvailability hook, all existing store calls, jadwalId in navigate state.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - PilihTanggalPage.tsx contains onClick on the Back button (ChevronLeft)
    - PilihTanggalPage.tsx contains "disabled={!selectedDate}" on Lanjutkan button (or equivalent disabled condition)
    - PilihTanggalPage.tsx contains "navigate" call on Lanjutkan button to antrian/sesi route
    - PilihTanggalPage.tsx contains "bg-[#008236]" on Lanjutkan button
    - PilihSesiPage.tsx contains onClick on sesi cards that calls setSelectedSlotId or store setter
    - PilihSesiPage.tsx contains selected-state styling (border-[#008236] or bg-[#f0fdf4]) conditional on selected card
    - PilihSesiPage.tsx contains "disabled={!selectedSlotId}" on Lanjutkan button
    - PilihSesiPage.tsx contains "navigate" call on Lanjutkan to konfirmasi route
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>PilihTanggalPage and PilihSesiPage visually aligned to Figma. All buttons have handlers. Disabled states enforced on Lanjutkan buttons.</done>
</task>

<task type="auto">
  <name>Task 2: KonfirmasiAntrianPage + TiketAntrianPage alignment</name>
  <files>
    frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx,
    frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/citizen/antrian/KonfirmasiAntrianPage.tsx` — full file; note RadioGroup balita selector, Daftar Sekarang onClick (ambilAntrianMutation or useAmbilAntrian), Batal onClick, 409 SLOT_PENUH error display
    - `frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx` — full file; note CountdownEstimasi component, BatalkanAntrianDialog button, useAntrianSocket, nomorUrut zero-padded display, disconnect alert
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 5.1 Socket.IO verification notes
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:3010" (KonfirmasiAntrian). Then get_design_context same params. Also get_screenshot nodeId "5:3197" (TiketAntrian). Study both frames carefully — TiketAntrian has the most visual complexity.

    KONFIRMASI ANTRIAN PAGE — visual alignment:
    - Header: green bg-[#008236] with "Konfirmasi Antrian" title and back ChevronLeft with onClick={() => navigate(-1)}.
    - Summary card: bg-white rounded-2xl showing posyandu name, tanggal, sesi time — all from loaded sesi data. MapPin, Calendar, Clock Lucide icons per Figma.
    - Balita selector: shadcn RadioGroup with RadioGroupItem per balita. Selected balita shows highlighted border. If no balita returned, show "Belum ada balita terdaftar" with link to add balita.
    - Daftar Sekarang button: bg-[#008236] text-white rounded-[14px] py-3.5 w-full. onClick triggers ambilAntrianMutation.mutate or equivalent. disabled={!selectedBalitaId || isPending}. Verify 409 SLOT_PENUH shows Alert with "Ganti Sesi" ghost button that calls navigate(-1). Verify 409 SUDAH_DAFTAR shows Alert with descriptive message.
    - Batal button (or back link): onClick={() => navigate(-1)} — must be present.
    Do NOT change: useAmbilAntrian hook, race condition guard logic, store reset on success.

    TIKET ANTRIAN PAGE — critical alignment (this is the core value prop):
    - Tiket card: bg-white rounded-2xl shadow-md. Nomor antrian zero-padded: String(nomorUrut).padStart(2,'0') in large font. Posyandu name, sesi time.
    - CountdownEstimasi component: verify it renders with the ± prefix ("±15 menit" format). Verify disclaimer text "Estimasi waktu, bukan janji" visible below countdown.
    - Status badge: uses StatusAntrian component. Badge color changes with statusAntrian value.
    - WA notice: "Notifikasi akan dikirim ke WhatsApp Anda" text visible.
    - Batalkan Antrian button: only rendered when statusAntrian === 'menunggu'. onClick opens BatalkanAntrianDialog. Verify BatalkanAntrianDialog has its own Cancel and Batalkan buttons.
    - Socket disconnect alert: when socketStatus === 'disconnected' show a non-dismissible Alert "Koneksi realtime terputus. Data mungkin tidak terkini."
    - Bagikan/share button (if in Figma): onClick calls navigator.share or window.print — only add if Figma shows it.
    Do NOT change: useAntrianSocket hook, CountdownEstimasi logic, BatalkanAntrianDialog mutation, socketStatus state machine.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - KonfirmasiAntrianPage.tsx contains onClick on Back/Batal button calling navigate(-1)
    - KonfirmasiAntrianPage.tsx contains "ambilAntrian" mutation reference on Daftar Sekarang onClick
    - KonfirmasiAntrianPage.tsx contains "disabled" condition on Daftar Sekarang button referencing selectedBalitaId
    - KonfirmasiAntrianPage.tsx contains "SLOT_PENUH" string (409 error handling)
    - KonfirmasiAntrianPage.tsx contains "bg-[#008236]" on Daftar Sekarang button
    - TiketAntrianPage.tsx contains "BatalkanAntrianDialog" component render
    - TiketAntrianPage.tsx contains "statusAntrian === 'menunggu'" condition guarding BatalkanAntrianDialog
    - TiketAntrianPage.tsx contains "CountdownEstimasi" component render
    - TiketAntrianPage.tsx contains "padStart(2, '0')" for nomorUrut zero-padding
    - TiketAntrianPage.tsx contains "useAntrianSocket" hook call (realtime preserved)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>KonfirmasiAntrianPage and TiketAntrianPage visually aligned to Figma. All buttons verified. CountdownEstimasi with ± prefix and BatalkanAntrianDialog both present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/antrian/ambil | Citizen submits antrian request (race-condition guarded from Phase 2) |
| Browser ↔ Socket.IO sesi:{slotId} | Realtime countdown updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Tampering | Lanjutkan enabled without selectedDate/selectedSlotId | mitigate | Disabled condition enforced on button + Navigate guard: page guards on selectedDate/selectedSlotId in store |
| T-08-09-02 | Denial of Service | Socket.IO disconnect leaves citizen with stale countdown | accept | Disconnect alert rendered (non-dismissible); citizen can manually refresh; stale data is non-critical (countdown is estimate not promise) |
| T-08-09-03 | Information Disclosure | nomorUrut exposed in TiketAntrian | accept | nomorUrut is by design public within a session; citizen needs it to identify themselves at posyandu |
| T-08-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 9 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /citizen/antrian/tanggal — calendar renders, Lanjutkan disabled without selection
3. Browser: /citizen/antrian/sesi — sesi cards with kuota, Lanjutkan disabled without selection
4. Browser: /citizen/antrian/konfirmasi — balita radio group, Daftar Sekarang button, Batal link
5. Browser: /citizen/antrian/:id — tiket shows nomorUrut zero-padded, CountdownEstimasi with ± prefix, BatalkanAntrianDialog button if status=menunggu
6. Open DevTools Network tab: confirm Socket.IO WS connection established on TiketAntrianPage
</verification>

<success_criteria>
- UI-01: Citizen antrian flow (4 screens) visually aligned to Figma
- UI-03: Realtime countdown (±prefix) verified working; Socket.IO connection active on TiketAntrianPage
- Every button has a real onClick handler — no decorative buttons
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-09-SUMMARY.md` when done.
</output>

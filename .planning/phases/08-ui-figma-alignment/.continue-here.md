---
context: phase
phase: 08-ui-figma-alignment
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-07-05T06:06:48.114Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through actual failure this session.

- [ ] CONSTRAINT: FIGMA-MCP-REQUIRED — Executor agents used RESEARCH.md design tokens instead of pulling actual Figma screenshots. Result: structural layout completely wrong despite "alignment" commits. Every screen implementation MUST start with `get_screenshot` + `get_design_context` from Figma MCP before writing any code.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Token-only alignment | Executor applied hex colors and border-radius from RESEARCH.md without viewing actual Figma frames. Login background wrong (white instead of mint), CitizenDashboard has green header that Figma doesn't have, KaderDashboard missing entire chart section. | blocking | MUST call `mcp__plugin_figma_figma__get_screenshot` for the MOBILE frame of every screen BEFORE writing any JSX. Do not trust RESEARCH.md design tokens for layout/structure — only for color values. |
| Wrong frame IDs | RESEARCH.md listed frame IDs that mix desktop and mobile, and some were component IDs not screen IDs. | advisory | Use frame IDs from memory file `feedback-figma-pixel-perfect.md` which were verified by browsing Figma metadata. Always use mobile frames (e.g., `5:5731` not `5:5654` for Login). |

<current_state>
Phase 08 Wave 5 (plan 08-05) is at human-verify checkpoint:
- Task 1 DONE: FamilyAccountPage balita list + ProfilSayaPage polish (commit 4aaf69e)
- Task 2 BLOCKED: QA pass pending — user found UI doesn't match Figma at all (not just 401s)

Root cause of block: Waves 2-4 executor agents made surface-level color changes without structural Figma alignment. The user confirmed ALL screens need pixel-perfect Figma implementation.

Additionally, the Kader meja lock feature is missing:
- Currently kader can navigate freely between mejas
- Entering a meja should lock the UI to that meja
- Tukar Meja button exists in Figma but has no working handler
- Switching meja requires PIN from ketua kader
</current_state>

<completed_work>
Wave 1 (08-01): Seed pipeline OK (413 balita), 2 backend bugs fixed (growth/riwayat endpoint + dashboard stats)
Wave 2 (08-02): getCitizenGrowthRiwayat added, TumbuhKembangPage ZScoreChart implemented, citizen screen token tweaks
Wave 3 (08-03): LockScreenPage green header, KaderDashboard/Meja2/Meja4 token adjustments
Wave 4 (08-04): PetaStunting mobile-first refactor, PuskesmasDashboard stats body, ManajemenPengguna green header
Wave 5 Task 1 (08-05): FamilyAccountPage balita list from /api/balita, ProfilSayaPage polished (commit 4aaf69e)
</completed_work>

<remaining_work>
IMMEDIATE:
- 08-05 Task 2: QA checkpoint — can be resolved by either: (A) approving with known gaps, or (B) deferring QA to after redesign

MAJOR (next session priority):
1. Pixel-perfect Figma redesign — ALL screens:
   - Login (mobile `5:5731`): mint bg `#f0fdf4`, demo buttons, footer
   - CitizenDashboard (`5:2029`): NO green header; notification cards; layanan cepat grid
   - KaderDashboard (`27:2531`): stats row, Tren Status Gizi bar chart, donut chart, risiko stunting table
   - All Meja screens (see frame IDs in memory)
   - Puskesmas screens (re-check against `5:13232`, `27:4655`)

2. Kader Meja Lock feature (new):
   - Enter meja → LockScreen-like lock to that meja
   - "Tukar Meja" button in header → open PIN modal
   - PIN must be verified against ketua kader PIN via backend
   - Failed PIN attempts should be logged
   - Backend endpoint: POST /api/kader/verify-ketua-pin (or similar)
</remaining_work>

<decisions_made>
- 401 console errors during QA are expected — axios interceptor handles refresh-then-retry (withCredentials: true)
- Wave 2-4 commits are kept (design tokens are correct foundation, structure needs addition)
- Kader meja lock is a confirmed required feature, not optional
- Figma file key: 4DIazKntakgAGXBDYefjbD (verified, all frames accessible via MCP)
</decisions_made>

<blockers>
- 08-05 Task 2 QA not approved — user wants pixel-perfect UI first, then QA
- Phase 8 scope expanded significantly: structural redesign + new kader meja lock feature
</blockers>

## Required Reading (in order)
1. `C:/Users/ASUS/.claude/projects/D--Semester-4-PSI-sispos/memory/project-phase8-gaps.md` — confirmed gaps with Figma frame IDs
2. `C:/Users/ASUS/.claude/projects/D--Semester-4-PSI-sispos/memory/feedback-figma-pixel-perfect.md` — pixel-perfect rule + mobile frame ID list
3. `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — existing research (use for context, NOT as layout authority)
4. `.planning/STATE.md` — current phase state

## Critical Anti-Patterns (do NOT repeat these)
- **DO NOT** implement any screen UI from RESEARCH.md tokens alone → Always call `get_screenshot` (mobile frame) first
- **DO NOT** add green headers to screens that don't have them in Figma → CitizenDashboard has NO green header
- **DO NOT** trust executor summaries claiming "aligned to Figma" without screenshot verification

## Infrastructure State
- Docker: sispos-backend, sispos-frontend, sispos-db, sispos-redis, sispos-nginx all UP
- Backend: http://localhost/api — healthy
- Frontend: http://localhost — healthy
- DB: 413 balita seeded, demo accounts ready
- Demo accounts: Citizen NIK 3471012345670001/Demo1234!, Kader 081234560001/123456, Puskesmas demo@puskesmas-mergangsan.go.id/Demo1234!

<context>
User confirmed: ALL screens must be pixel-perfect to Figma — text size, spacing, components, layout structure — not just color tokens. Waves 2-4 used wrong approach (token-only). Next session should start fresh with proper Figma MCP usage for every screen.

Two deliverables for next session:
1. Pixel-perfect UI redesign (start with Login → CitizenDashboard → KaderDashboard — highest visibility)
2. Kader meja lock feature (enter meja = locked, tukar meja = ketua kader PIN required)

Figma metadata was pulled this session and all mobile frame IDs are saved in memory.
The continuation agent for 08-05 is still live (agentId: a0c8ce3d95079d355) but the plan's scope has changed — recommend closing it and re-planning.
</context>

<next_action>
Start with: Read memory/project-phase8-gaps.md, then:
Option A — Re-plan Phase 8: `/gsd-plan-phase 08` with instruction to pull Figma screenshots for each screen
Option B — Create Phase 9 "pixel-perfect-ui": `/gsd-plan-phase 09` with scope = pixel-perfect redesign + kader meja lock

Either way: the FIRST TASK of any plan must be `get_screenshot` from Figma MCP for the mobile frame of that screen.
</next_action>

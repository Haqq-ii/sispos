---
phase: 08-ui-figma-alignment
plan: 10
type: execute
wave: 10
depends_on: []
files_modified:
  - frontend/src/pages/citizen/TumbuhKembangPage.tsx
  - frontend/src/pages/citizen/ChatAssistantPage.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "TumbuhKembangPage Grafik tab renders ZScoreChart with real riwayat data (not a placeholder)"
    - "TumbuhKembangPage Imunisasi tab renders fetched imunisasi list (not a placeholder)"
    - "ChatAssistantPage Send button calls the chat mutation"
    - "ChatAssistantPage quick suggestion chips have onClick that fills input and submits"
  artifacts:
    - path: "frontend/src/pages/citizen/TumbuhKembangPage.tsx"
      provides: "3 functional tabs: Grafik (ZScoreChart), Riwayat (list), Imunisasi (list)"
      contains: "ZScoreChart"
  key_links:
    - from: "TumbuhKembangPage Grafik tab"
      to: "ZScoreChart component"
      via: "map riwayat data to ZScoreDataPoint array"
      pattern: "ZScoreChart"
    - from: "ChatAssistantPage Send button"
      to: "POST /api/ai/chat/assistant"
      via: "chatMutation.mutate"
      pattern: "chatMutation"
---

<objective>
Wave 8.10: Citizen supplementary screens — TumbuhKembangPage grafik/imunisasi tabs and ChatAssistantPage alignment.

TumbuhKembangPage currently has "Riwayat" tab functional but "Grafik" and "Imunisasi" tabs showing "Fitur Segera Hadir" placeholders. ZScoreChart component already exists from Meja3Page. The riwayat data already contains zScoreBbU, zScoreTbU fields that can feed ZScoreChart. The imunisasi endpoint exists from Phase 3. Both placeholder tabs must show real content.

ChatAssistantPage is CHECKPOINT status — the unified gizi+pendaftaran AI chat. Visual alignment and button handler verification.

Purpose: TumbuhKembang is visible in the demo flow. A placeholder is worse than no feature. ChatAssistant is the main AI entry point for citizens.

Output: TumbuhKembangPage with 3 functional tabs. ChatAssistantPage visually aligned with all buttons verified.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TumbuhKembangPage — implement Grafik tab + Imunisasi tab</name>
  <files>frontend/src/pages/citizen/TumbuhKembangPage.tsx</files>

  <read_first>
    - `frontend/src/pages/citizen/TumbuhKembangPage.tsx` — full file (~200 lines); note: RiwayatRecord interface (has zScoreBbU, zScoreTbU fields), current Grafik tab (placeholder), current Imunisasi tab (placeholder), the useQuery for riwayat data, existing ZScoreChart import and ZScoreDataPoint type
    - `frontend/src/pages/kader/meja/Meja3Page.tsx` — lines 1-60: exact ZScoreChart import path, ZScoreDataPoint interface shape { tanggal, bbU, tbU, bbTb? }
    - `frontend/src/components/kader/ZScoreChart.tsx` — full file: confirm ZScoreDataPoint fields and component props
    - `backend/src/modules/immunization/` — check if there is a GET /api/imunisasi endpoint that accepts balitaId and returns imunisasi history for citizen
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:2336" (TumbuhKembang grafik tab, or search for the correct frame if 2001:2336 returns wrong content). Study the Grafik and Imunisasi tab layouts.

    GRAFIK TAB — replace placeholder with real chart:
    1. Map riwayat data (already fetched) to ZScoreDataPoint array:
       `const grafik: ZScoreDataPoint[] = (riwayatData ?? []).map(r => ({ tanggal: r.tanggalPemeriksaan ?? r.createdAt, bbU: r.zScoreBbU ?? 0, tbU: r.zScoreTbU ?? 0, bbTb: r.zScoreBbTb ?? undefined })).sort((a,b) => new Date(a.tanggal).getTime() - new Date(b.tanggal).getTime())`
    2. In the Grafik tab content, replace the placeholder div with:
       - If grafik.length === 0: show `<p className="text-[#99a1af] text-sm text-center py-8">Belum ada data pemeriksaan untuk ditampilkan.</p>`
       - If grafik.length > 0: wrap `<ZScoreChart data={grafik} />` in `<div className="bg-white border border-[#f3f4f6] rounded-2xl p-4 shadow-sm">` with a title `<p className="text-[#99a1af] text-xs font-semibold tracking-wider mb-3">GRAFIK Z-SCORE</p>`
    3. No new imports needed — ZScoreChart is already imported.

    IMUNISASI TAB — replace placeholder with fetched list:
    1. Add a new useQuery for imunisasi: queryKey ['imunisasi', 'citizen', balitaId], staleTime 60_000.
       queryFn: check the imunisasi endpoint — try GET /api/imunisasi (the citizen's balita imunisasi). Read the immunization module routes to confirm the exact endpoint path. If endpoint is GET /api/imunisasi with no params (auto-scoped to citizen's first balita via JWT), use that. If it requires balitaId, get balitaId from useAuthStore or from the riwayat data (the balita of the citizen's first child).
       Define ImunisasiItem interface: { id: string, namaVaksin: string, dosisKe: number, tanggalInjeksi: string, keterangan?: string | null }.
    2. In the Imunisasi tab content, replace placeholder with:
       - isLoadingImunisasi: `<Skeleton className="h-24 rounded-2xl" />`
       - imunisasiList.length === 0: `<p className="text-[#99a1af] text-sm text-center py-8">Belum ada riwayat imunisasi.</p>`
       - imunisasiList.length > 0: map each item to a card: bg-white border border-[#f3f4f6] rounded-2xl px-4 py-3 showing namaVaksin (text-[#1e2939] font-semibold text-sm), "Dosis ke-{dosisKe}" (text-[#99a1af] text-xs), and tanggalInjeksi formatted as DD/MM/YYYY.
    3. Add the imunisasi useQuery to the existing queries section.

    Do NOT change: the Riwayat tab (already functional), the green header stats (BB, TB, Z-Score from LatestStats), the formatDate helper, the getStatusStyle helper, or the existing riwayat useQuery. Only add grafik mapping and imunisasi query.

    TypeScript: add ZScoreDataPoint and ImunisasiItem interfaces with explicit types. No implicit any.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - TumbuhKembangPage.tsx contains "ZScoreChart" component render in the Grafik tab (not in a placeholder div)
    - TumbuhKembangPage.tsx contains "zScoreBbU" in the grafik mapping (using real riwayat data)
    - TumbuhKembangPage.tsx does NOT contain "Segera Hadir" text in the Grafik or Imunisasi tabs (placeholders removed)
    - TumbuhKembangPage.tsx contains "imunisasi" in a useQuery queryKey (imunisasi data fetched)
    - TumbuhKembangPage.tsx contains "namaVaksin" property access in JSX (imunisasi item rendered)
    - TumbuhKembangPage.tsx still contains "riwayatData" reference (Riwayat tab preserved)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>TumbuhKembangPage has 3 functional tabs: Grafik (ZScoreChart from riwayat data), Riwayat (existing list), Imunisasi (fetched from API). No placeholder text remains.</done>
</task>

<task type="auto">
  <name>Task 2: ChatAssistantPage visual alignment + button audit</name>
  <files>frontend/src/pages/citizen/ChatAssistantPage.tsx</files>

  <read_first>
    - `frontend/src/pages/citizen/ChatAssistantPage.tsx` — full file; note: chatMutation, messages state, input state, QUICK_SUGGESTIONS array, Send button onClick, suggestion chip onClick, ArrowLeft back link, auto-scroll ref
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:7969" (ChatAssistant). Then get_design_context same params. Study the layout.

    Visual alignment — adjust JSX/className only; preserve ALL chat logic:
    - Root: min-h-screen flex flex-col bg-[#f9fafb].
    - Header: bg-[#008236] px-4 pt-10 pb-4. Contains: ArrowLeft back link → /citizen/dashboard with text-white. Bot or Sparkles icon. Title "AI Assistant" text-white font-bold. Subtitle "Konsultasi gizi & pendaftaran antrian" text-[#7bf1a8] text-xs.
    - Messages area: flex-1 overflow-y-auto px-4 py-3 space-y-3. User bubble: bg-[#008236] text-white rounded-2xl rounded-br-sm px-4 py-2.5 self-end max-w-[85%]. Assistant bubble: bg-white border border-[#f3f4f6] rounded-2xl rounded-bl-sm px-4 py-2.5 self-start max-w-[85%] text-[#1e2939] text-sm whitespace-pre-wrap.
    - Quick suggestions: horizontal scroll row of chips. Each chip: bg-white border border-[#e5e7eb] rounded-full px-3 py-1.5 text-[#364153] text-xs font-medium. onClick must fill input with the suggestion text AND submit (call chatMutation directly or trigger handleSend). Verify QUICK_SUGGESTIONS chips have onClick that calls chatMutation.mutate or handleSend with the suggestion text.
    - Input area: bg-white border-t border-[#f3f4f6] px-3 py-3 flex items-end gap-2. Input field: flex-1 bg-[#f9fafb] border border-[#e5e7eb] rounded-[14px] px-4 py-2.5 text-sm resize-none (use textarea for multiline). Send button: bg-[#008236] rounded-[14px] p-2.5 disabled when input.trim()=='' or chatMutation.isPending. SendHorizonal icon text-white size 18. onClick calls handleSend.

    Verify these handlers are working (not decoration):
    - ArrowLeft/back: Link or navigate to /citizen/dashboard
    - Each QUICK_SUGGESTIONS chip: onClick fills and submits that suggestion
    - Send button: onClick calls chatMutation.mutate({ message: input, history: messages })
    - Input: onChange updates input state, onKeyDown Enter (not Shift+Enter) triggers send

    If any of the above handlers are missing or broken, add them. Do NOT change: chatMutation mutationFn (POST /api/ai/chat/assistant), messages state management, auto-scroll useEffect, toast on error.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - ChatAssistantPage.tsx contains "onClick" on the ArrowLeft or back element linking to citizen/dashboard
    - ChatAssistantPage.tsx contains "onClick" on QUICK_SUGGESTIONS chip map (suggestion chips are clickable)
    - ChatAssistantPage.tsx contains "chatMutation.mutate" or "handleSend" called from suggestion chip onClick
    - ChatAssistantPage.tsx contains "disabled" condition on Send button referencing chatMutation.isPending
    - ChatAssistantPage.tsx contains "SendHorizonal" or equivalent send icon in Send button
    - ChatAssistantPage.tsx contains "whitespace-pre-wrap" on assistant message bubble (preserves bullet points)
    - ChatAssistantPage.tsx contains "bg-[#008236]" on Send button
    - ChatAssistantPage.tsx still contains "chatMutation" useMutation (API call preserved)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>ChatAssistantPage visually aligned to Figma 2001:7969. All buttons have working handlers. Quick suggestion chips submit messages. Send button calls chat mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/imunisasi | Citizen fetches their balita imunisasi history |
| Browser → POST /api/ai/chat/assistant | Citizen sends message to AI (rate limited 20/day) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Information Disclosure (IDOR) | GET /api/imunisasi returns citizen's balita data | accept | Endpoint already secured from Phase 3 via authMiddleware + citizen JWT; scoped to JWT wargaId; no change to backend |
| T-08-10-02 | Denial of Service | AI chat rate limit exceeded | accept | Rate limit 20 msg/day already enforced server-side (Phase 4); frontend shows toast on 429 response |
| T-08-10-03 | Tampering | Quick suggestion chips could inject malicious prompt | accept | All suggestions are hardcoded strings in QUICK_SUGGESTIONS array; not user-controlled; AI has system-level guardrails |
| T-08-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 10 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /citizen/tumbuh-kembang → Grafik tab shows ZScoreChart (or empty state if no data), not placeholder
3. Browser: /citizen/tumbuh-kembang → Imunisasi tab shows list (or empty state), not "Segera Hadir"
4. Browser: /citizen/chat-assistant → quick suggestion chips clickable, Send button enabled when text entered, assistant bubble renders with whitespace-pre-wrap
</verification>

<success_criteria>
- UI-01: TumbuhKembangPage Grafik + Imunisasi tabs show real content (no placeholder)
- UI-01: ChatAssistantPage visually aligned to Figma 2001:7969
- UI-03: ZScoreChart renders from real riwayat data; AI chat sends to /api/ai/chat/assistant
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-10-SUMMARY.md` when done.
</output>

---
phase: 08-ui-figma-alignment
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - frontend/src/pages/citizen/FamilyAccountPage.tsx
  - frontend/src/pages/citizen/ProfilSayaPage.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "FamilyAccountPage shows a list of the citizen's registered balita (fetched from GET /api/balita) instead of Fitur Segera Hadir placeholder"
    - "ProfilSayaPage shows citizen profile info from useAuthStore with proper Figma-aligned card layout"
    - "docker compose exec sispos-frontend npm run lint exits 0 (no TypeScript errors in entire frontend)"
    - "All 37 routes accessible in browser without React error boundary triggering"
    - "No red console errors on any citizen, kader, or puskesmas page"
  artifacts:
    - path: "frontend/src/pages/citizen/FamilyAccountPage.tsx"
      provides: "Balita list fetched from /api/balita replacing Fitur Segera Hadir"
      contains: "useQuery"
    - path: "frontend/src/pages/citizen/ProfilSayaPage.tsx"
      provides: "Profile info from useAuthStore with card layout"
  key_links:
    - from: "FamilyAccountPage.tsx"
      to: "GET /api/balita"
      via: "useQuery queryKey: ['balita', 'saya']"
      pattern: "api/balita"
---

<objective>
Wave 8.5: Final polish and QA. Implement meaningful content for FamilyAccountPage (currently "Fitur Segera Hadir" placeholder) and improve ProfilSayaPage layout. Run TypeScript compilation audit. Complete a human-verified full QA pass across all routes for all three roles to confirm SC-5 (no console errors, no crashes).

Purpose: The application must be demo-ready. FamilyAccountPage is visible in the citizen navigation — showing "Fitur Segera Hadir" during a demo is a notable gap. ProfilSayaPage shows user info but lacks the Figma card structure. The final human-verify checkpoint is the gatekeeping test for SC-5.

Output: FamilyAccountPage with real balita list; ProfilSayaPage polished; full QA pass signed off.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FamilyAccountPage balita list + ProfilSayaPage polish + TypeScript audit</name>
  <files>
    frontend/src/pages/citizen/FamilyAccountPage.tsx,
    frontend/src/pages/citizen/ProfilSayaPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/citizen/FamilyAccountPage.tsx` — current placeholder implementation
    - `frontend/src/pages/citizen/ProfilSayaPage.tsx` — current basic implementation
    - `frontend/src/pages/citizen/CitizenDashboardPage.tsx` — lines 30-60 for the existing AntrianAktif type and Balita data pattern used in dashboard
    - `backend/src/modules/child/child.routes.ts` — GET /api/balita returns citizen's balita list (requireRole citizen)
    - `backend/src/modules/child/child.controller.ts` — getBalitaSayaHandler return shape
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 3.2 Patterns 1-6, section 4 Tier 3 (FamilyAccountPage: Segera Hadir — note as planned feature in demo)
    - `frontend/src/stores/useAuthStore.ts` — what fields are available: namaLengkap, role, userId (for ProfilSayaPage)
  </read_first>

  <action>
    IMPORTANT: FamilyAccountPage is listed as Tier 3 (nice-to-have) in RESEARCH.md — implement a minimal but real balita list, not a full add/edit feature. The goal is to replace the "Fitur Segera Hadir" with actual data.

    FamilyAccountPage implementation:
    1. Add useQuery to fetch GET /api/balita: `queryKey: ['balita', 'saya'], queryFn: () => apiClient.get('/balita').then(r => r.data.data as BalitaItem[])`. Define `BalitaItem` interface with at minimum: `id: string, namaLengkap: string, tanggalLahir: string, jenisKelamin: string`.
    2. Add a green header: bg-[#008236] px-4 pt-10 pb-5 with title "Keluarga Saya" (text-white font-bold text-xl) and subtitle "Daftar balita terdaftar" (text-[#b9f8cf] text-xs).
    3. Add a back button (Link to /citizen/dashboard) in the header using ArrowLeft icon from lucide-react in `bg-[rgba(0,166,62,0.5)] rounded-[14px] p-2` pattern (same as TumbuhKembangPage).
    4. Body: px-4 mt-4 space-y-3 with:
       - isLoading: `<Skeleton className="h-[80px] rounded-2xl" />` (3 skeletons)
       - Empty: card with "Belum ada balita terdaftar. Hubungi petugas Posyandu untuk mendaftarkan balita Anda."
       - balita list: each card is `bg-white border border-[#f3f4f6] rounded-2xl shadow-sm px-4 py-4 flex items-center gap-3`; a circular avatar with first letter of namaLengkap in `w-10 h-10 rounded-full bg-[#dcfce7] flex items-center justify-center text-[#008236] font-bold text-sm`; name in text-[#1e2939] font-semibold text-sm; tanggalLahir formatted as DD/MM/YYYY in text-[#99a1af] text-xs; jenisKelamin in a badge bg-[#f3f4f6] text-[#364153] rounded-[10px] px-2 text-xs.
    5. Do NOT implement add/edit/delete balita functionality — out of scope per RESEARCH.md Tier 4 exclusions.

    ProfilSayaPage polish:
    1. Pull Figma context: search fileKey `4DIazKntakgAGXBDYefjbD` for frame named "Profil" or "Profil Saya".
    2. Add a green header (same pattern as FamilyAccountPage) with title "Profil Saya" and subtitle from useAuthStore namaLengkap.
    3. Profile info card: bg-white border border-[#f3f4f6] rounded-2xl shadow-sm px-4 py-5 mx-4 mt-4. Show: namaLengkap, role badge (text-[#008236] bg-[#dcfce7] rounded-[10px] px-2 py-0.5 text-xs for "Citizen"), and any other fields from useAuthStore.
    4. Do NOT implement edit profile form — out of scope.

    TypeScript audit:
    After FamilyAccountPage and ProfilSayaPage changes, run `docker compose exec sispos-frontend npm run lint`. If any errors appear in OTHER files (not modified in this wave), do NOT modify those files — only document the errors in the SUMMARY. Fix only errors in FamilyAccountPage.tsx and ProfilSayaPage.tsx.

    Also run `docker compose exec sispos-backend npx tsc --noEmit` to confirm backend TypeScript is clean after the growth endpoint added in 08-02.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -10 &amp;&amp; docker compose exec sispos-backend npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - FamilyAccountPage.tsx does NOT contain "Fitur Segera Hadir" string
    - FamilyAccountPage.tsx contains `useQuery` import and a query calling `/balita` endpoint
    - FamilyAccountPage.tsx contains `bg-[#008236]` in header section
    - ProfilSayaPage.tsx contains `bg-[#008236]` in header section
    - ProfilSayaPage.tsx contains `useAuthStore` import and usage
    - `docker compose exec sispos-frontend npm run lint` exits 0 (no TypeScript errors in FamilyAccountPage.tsx and ProfilSayaPage.tsx specifically)
    - `docker compose exec sispos-backend npx tsc --noEmit` exits 0 (backend TypeScript clean after 08-02 growth endpoint)
    - Browser at http://localhost/citizen/family-account (logged in as citizen with demo seed data) shows at least 1 balita card (Budi Santoso or Sari Dewi from seed data)
  </acceptance_criteria>

  <done>FamilyAccountPage shows real balita list from /api/balita. ProfilSayaPage has green header + profile card. TypeScript compilation clean in both frontend and backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final QA — all routes, console errors, visual check all 3 roles</name>

  <read_first>
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 6.2 Acceptance Criteria per Success Criterion, section 6.3 Wave-by-Wave Gate
    - `.planning/ROADMAP.md` — Phase 8 Success Criteria SC-1 through SC-5
  </read_first>

  <what-built>
    Waves 8.1–8.5 completed: seed pipeline verified, AI chatbot verified, TumbuhKembangPage grafik tab implemented, all citizen/kader/puskesmas screens aligned to Figma, FamilyAccountPage balita list implemented, ProfilSayaPage polished.
  </what-built>

  <how-to-verify>
    Open http://localhost in a browser. Open DevTools Console (F12). Systematically navigate to each route while checking console for red errors.

    Citizen routes (login as NIK 3471012345670001 / Demo1234!):
    - /login — no errors
    - /register — form renders, no errors (verify visual match Figma frames 4:87, 4:142)
    - /citizen/dashboard — antrian aktif card visible, no errors
    - /citizen/antrian/pilih-tanggal — calendar renders, no errors
    - /citizen/antrian/pilih-sesi — (requires navigating from pilih-tanggal with valid jadwalId state)
    - /citizen/antrian/tiket — (requires active antrian; check with demo citizen)
    - /citizen/tumbuh-kembang — click "Grafik" tab → ZScoreChart renders (not placeholder), no errors
    - /citizen/chat-assistant — chat input visible, no errors
    - /citizen/family-account — balita list visible (Budi Santoso card), no errors
    - /citizen/profil — profile card visible, no errors

    Kader routes (login as HP 081234560001 / PIN 123456):
    - /kader/dashboard — jadwal cards visible, no errors
    - /kader/pelayanan — no errors
    - /kader/lock-screen — green header + card meja buttons visible, no errors
    - /kader/meja/1 — hadir/tangguhkan list, no errors
    - /kader/meja/2 — numpad visible, no errors
    - /kader/meja/3 — ZScoreChart renders (from Meja 3), no errors
    - /kader/meja/4 — STT button and AI warning section visible, no errors
    - /kader/meja/5 — selesai button visible, no errors
    - /kader/rekap — rekap page visible, no errors

    Puskesmas routes (login as demo@puskesmas-mergangsan.go.id / Demo1234!):
    - /puskesmas/dashboard — stats grid (2x2) visible, no errors
    - /puskesmas/peta-stunting — map renders (full-width, green header), no errors; change bulan filter → map updates without "Map container already initialized" error
    - /puskesmas/manajemen-pengguna — kader list visible, no errors
    - /puskesmas/jadwal — jadwal table visible, no errors
    - /puskesmas/laporan — laporan page with Excel/PDF download buttons, no errors

    SC-4 functional checks:
    - SC-4a: On TiketAntrianPage with active antrian — countdown shows ± prefix and "estimasi" disclaimer (confirm text matches CLAUDE.md mandate)
    - SC-4b: TumbuhKembangPage Grafik tab — recharts LineChart with BB/U TB/U BB/TB lines visible
    - SC-4c: ChatAssistantPage — ask "apa manfaat ASI?" → response in Bahasa Indonesia (confirm no English text)

  </how-to-verify>

  <resume-signal>Type "qa-approved" if all routes pass and SC-4a/b/c are confirmed. Or describe specific routes that failed with error messages.</resume-signal>

  <acceptance_criteria>
    - Zero red console errors across all citizen, kader, and puskesmas routes listed above
    - TumbuhKembangPage Grafik tab shows ZScoreChart (recharts LineChart element), not a placeholder string
    - TiketAntrianPage countdown text contains "±" prefix (confirming estimasi label requirement)
    - ChatAssistantPage response is entirely in Bahasa Indonesia for a gizi question
    - PetaStuntingPage bulan filter change does NOT produce "Map container is already initialized" error
    - LockScreenPage header is green (not white with border-b)
    - FamilyAccountPage shows at least 1 balita card with namaLengkap visible
    - All listed routes load without triggering React error boundary (no "Something went wrong" screen)
  </acceptance_criteria>

  <done>Full QA pass completed. All routes accessible without crash. No console errors. SC-1 through SC-5 confirmed by human tester. Phase 8 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/balita | Citizen-only endpoint (requireRole citizen); already secured from Phase 2 |
| Human tester → all routes | Final QA; no auth changes; verifying existing security controls work correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | FamilyAccountPage GET /api/balita | accept | Endpoint already protected by requireRole('citizen') from child.routes.ts (Phase 2); no new auth surface; citizen sees only their own balita (WHERE wargaId = req.user.userId in getBalitaSayaHandler) |
| T-08-05-02 | Information Disclosure | ProfilSayaPage useAuthStore data | accept | useAuthStore only holds data from JWT decode (namaLengkap, role, userId — no sensitive fields); no backend call exposes new data |
| T-08-05-03 | Spoofing | QA login with demo credentials | accept | Demo credentials are intentionally public; QA testing uses demo accounts; production would use different credentials |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all UI uses existing lucide-react, shadcn/ui, recharts (already in package.json) |
</threat_model>

<verification>
Final Phase 8 verification gate (all must pass before declaring Phase 8 complete):
1. `docker compose exec sispos-frontend npm run lint` exits 0 — frontend TypeScript clean
2. `docker compose exec sispos-backend npx tsc --noEmit` exits 0 — backend TypeScript clean
3. Human QA pass: all 22 routes listed in Task 2 load without React error boundary
4. Human QA pass: zero red console errors on any page
5. Human QA pass: SC-4a (countdown ± prefix), SC-4b (ZScoreChart), SC-4c (Bahasa Indonesia AI response) all confirmed
6. Human QA pass: LockScreenPage green header confirmed
7. Human QA pass: PetaStuntingPage bulan filter change — no map re-init error
</verification>

<success_criteria>
- SC-1: All Citizen screens (Login, Register, Dashboard, Antrian, Chat, TumbuhKembang, FamilyAccount, Profil) match Figma — verified in QA pass
- SC-2: All Kader screens (Dashboard, LockScreen, Meja 1-5, Rekap) match Figma — verified in QA pass
- SC-3: All Puskesmas screens (Dashboard, Peta Stunting, Manajemen Pengguna, Laporan) match Figma — verified in QA pass
- SC-4: Countdown realtime accurate (Socket.IO), Z-Score chart readable, AI Chatbot in Bahasa Indonesia — all verified
- SC-5: No console errors on any page; all routes accessible without crash — verified in QA pass
- UI-01: Citizen screens aligned (complete)
- UI-02: Kader + Puskesmas screens aligned (complete)
- UI-03: Functional features working end-to-end (complete)
</success_criteria>

## Artifacts This Phase Produces

- Updated `frontend/src/pages/citizen/FamilyAccountPage.tsx` — real balita list from /api/balita
- Updated `frontend/src/pages/citizen/ProfilSayaPage.tsx` — green header + profile card
- `.planning/phases/08-ui-figma-alignment/08-05-SUMMARY.md` — final QA results, Phase 8 completion declaration

<output>
Create `.planning/phases/08-ui-figma-alignment/08-05-SUMMARY.md` when done.
</output>

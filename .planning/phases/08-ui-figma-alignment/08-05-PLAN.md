---
phase: 08-ui-figma-alignment
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - frontend/src/pages/auth/LoginPage.tsx
  - frontend/src/pages/citizen/CitizenDashboardPage.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "LoginPage root container uses bg-[#f0fdf4] (mint green), not bg-[#f9fafb]"
    - "LoginPage has a 'Demo Akun Cepat' section with 3 buttons that auto-fill credentials and submit"
    - "LoginPage footer shows SISPOS v1.0 and UU PDP compliance text"
    - "CitizenDashboardPage does NOT have bg-[#008236] as the outermost top-level page header"
    - "CitizenDashboardPage greeting ('Selamat datang') appears on white or transparent background"
  artifacts:
    - path: "frontend/src/pages/auth/LoginPage.tsx"
      provides: "Login page with mint bg, SISPOS logo, demo quick-fill buttons, legal footer"
      contains: "bg-[#f0fdf4]"
    - path: "frontend/src/pages/citizen/CitizenDashboardPage.tsx"
      provides: "Citizen dashboard with white-bg greeting section instead of green header"
  key_links:
    - from: "LoginPage demo buttons"
      to: "loginMutation.mutate"
      via: "onClick calls mutation directly with hardcoded demo credentials"
      pattern: "loginMutation\\.mutate"
---

<objective>
Wave 8.5 (Phase 2 of replanning): Pixel-perfect redesign of Login page and Citizen Dashboard page to match Figma frame specifications.

LoginPage (Figma 5:5731): The current page uses a plain white background and a circular icon badge. Figma shows a mint-green (#f0fdf4) full-page background, a green rounded-square SISPOS logo block, a white login card with role-detection chip, a "Demo Akun Cepat" quick-fill section with 3 demo credential buttons, and a legal footer. None of these exist in the current implementation.

CitizenDashboardPage (Figma 5:2029): The current page has a green (#008236) header at the top — this is WRONG per Figma. The Figma frame shows NO green header; instead the page starts with a white/light greeting section with the user's name and a notification bell, followed by a user chip row. All existing business logic (antrian query, computeCountdown, socket, navigation) must be preserved — only the JSX layout structure changes.

Purpose: Login is the entry point seen by every user. Citizen Dashboard is the main landing page for citizens. Both must match Figma exactly for demo quality.

Output: LoginPage with mint background + demo buttons + footer. CitizenDashboardPage with white greeting header (green header removed).
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginPage pixel-perfect redesign (Figma 5:5731)</name>
  <files>frontend/src/pages/auth/LoginPage.tsx</files>

  <read_first>
    - `frontend/src/pages/auth/LoginPage.tsx` — full current implementation (170 lines); note: all mutation logic, KaderLockScreen overlay, error handling, detectRole, and showRegisterLink logic must be preserved
    - `frontend/src/components/auth/LoginForm.tsx` — what props it accepts (onSubmit, isLoading, error, gagalLogin) so you don't break the interface
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 3.2 patterns
    - CLAUDE.md — demo account credentials: NIK 3471012345670001/Demo1234!, Kader 081234560001/123456, Puskesmas demo@puskesmas-mergangsan.go.id/Demo1234!
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:5731". Then call mcp__plugin_figma_figma__get_design_context with the same file key and node ID. Study the returned visual and design spec before writing any JSX.

    From the confirmed UI gaps (planning context), the Figma frame 5:5731 shows:
    - Full-page mint background: #f0fdf4
    - SISPOS logo block at top: green rounded-square icon (bg-[#008236] rounded-2xl, ~64x64px) containing ShieldCheck size 36 text-white, then "SISPOS" in text-[#008236] font-bold text-2xl, then "Sistem Informasi Posyandu" in text-[#99a1af] text-sm, then a MapPin icon + "Posyandu Digital Indonesia" text in text-[#008236] text-xs (all centered in a column)
    - White login card: bg-white rounded-2xl border border-[#f3f4f6] shadow-md px-6 py-6; heading "Masuk ke Akun" text-[#1e2939] font-bold text-lg mb-1; role detection chip (show the result of detectRole(identifier) as a badge if identifier is non-empty); then the LoginForm component
    - Demo Akun Cepat section (below card): label "Demo Akun Cepat" text-[#99a1af] text-xs font-semibold uppercase tracking-wider mb-2 text-center; three buttons in a column (or 1-col grid) each bg-white border border-[#f3f4f6] rounded-[14px] py-2.5 px-4 text-sm font-semibold text-[#364153] shadow-sm
    - Legal footer at very bottom: text-center text-[#99a1af] text-[10px] leading-relaxed content "SISPOS v1.0 · Dilindungi UU PDP No. 27/2022 · TLS 1.3 + AES-256"

    Implementation steps:

    1. Change root div className from "min-h-screen flex items-center justify-center bg-[#f9fafb] px-4" to "min-h-screen flex flex-col items-center justify-center bg-[#f0fdf4] px-4 py-8".

    2. Replace the current logo+wordmark section with: a centered column containing (a) a div w-16 h-16 bg-[#008236] rounded-2xl flex items-center justify-center mb-3 with ShieldCheck size={36} className="text-white" inside; (b) h1 text-[#008236] font-bold text-2xl; (c) p "Sistem Informasi Posyandu" text-[#99a1af] text-sm mt-0.5; (d) a div flex items-center gap-1 justify-center mt-1 with MapPin size={12} className="text-[#008236]" and span "Posyandu Digital Indonesia" text-[#008236] text-xs.

    3. Inside the white card, add a heading "Masuk ke Akun" as h2 text-[#1e2939] font-bold text-lg mb-1 before the LoginForm. Below the heading, if identifier is non-empty, show a role chip: a span with content showing detectRole(identifier) in a badge bg-[#dcfce7] text-[#008236] text-xs rounded-[10px] px-2 py-0.5 mb-3 inline-block. Use the existing detectRole(identifier) logic already in the file.

    4. Add Demo Akun Cepat section. Place it between the white card and the register link paragraph. Add a state variable `isDemoLoading: boolean` initialized to false. Create a handleDemoLogin function that accepts (identifier: string, password: string) and calls loginMutation.mutate({ identifier, password }) directly. Three demo buttons:
       - "Demo Warga" → calls handleDemoLogin("3471012345670001", "Demo1234!")
       - "Demo Kader" → calls handleDemoLogin("081234560001", "123456")
       - "Demo Puskesmas" → calls handleDemoLogin("demo@puskesmas-mergangsan.go.id", "Demo1234!")
       Each button is disabled when loginMutation.isPending. Style each button: bg-white border border-[#f3f4f6] rounded-[14px] py-2.5 px-4 w-full text-sm font-semibold text-[#364153] shadow-sm hover:bg-[#f0fdf4] transition-colors. Wrap all three in a div space-y-2. Add the section label "Demo Akun Cepat" as p text-[#99a1af] text-xs font-semibold tracking-wider text-center mb-2 above the buttons.

    5. Add legal footer at the very bottom of the container (after register link): p text-center text-[#99a1af] text-[10px] mt-4 leading-relaxed content "SISPOS v1.0 · Dilindungi UU PDP No. 27/2022 · TLS 1.3 + AES-256".

    6. Add MapPin to the lucide-react imports at the top of the file alongside the existing ShieldCheck import.

    DO NOT change: KaderLockScreen overlay logic, loginMutation logic, onSuccess role redirect, onError error handling, handleSubmit, handleUnlock, terkunciSampai state, gagalLogin state, identifier state, showRegisterLink logic, or the detectRole import. Only add/change JSX structure and className values.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - LoginPage.tsx contains "bg-[#f0fdf4]" in the root container className
    - LoginPage.tsx contains "Demo Akun Cepat" string
    - LoginPage.tsx contains "3471012345670001" (demo citizen NIK)
    - LoginPage.tsx contains "081234560001" (demo kader phone)
    - LoginPage.tsx contains "demo@puskesmas-mergangsan.go.id" (demo puskesmas email)
    - LoginPage.tsx contains "UU PDP No. 27/2022" in the footer section
    - LoginPage.tsx contains "loginMutation.mutate" (demo buttons call same mutation)
    - LoginPage.tsx contains "MapPin" in the lucide-react import
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>LoginPage has mint green background, SISPOS rounded-square logo, role-detection chip in card, 3 demo quick-fill buttons, and UU PDP legal footer. All existing login functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: CitizenDashboardPage structural redesign (Figma 5:2029)</name>
  <files>frontend/src/pages/citizen/CitizenDashboardPage.tsx</files>

  <read_first>
    - `frontend/src/pages/citizen/CitizenDashboardPage.tsx` — full current implementation (313 lines); note: the green header section is lines ~108-139; all business logic below that (antrian query, computeCountdown, isAntrian, Section A, Section B, Layanan Cepat, Profil Balita, Tips Gizi) must be preserved intact
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:2029". Then call mcp__plugin_figma_figma__get_design_context with the same file key and node ID. Study the layout carefully before writing any JSX.

    From the confirmed UI gaps (planning context), the Figma frame 5:2029 shows:
    - NO green header — the page starts on a white or #f9fafb background
    - Top section (white background): greeting text "Selamat datang," small text-[#99a1af] text-xs, then user namaLengkap in text-[#1e2939] font-bold text-xl; notification bell button on the far right of this top row
    - User chip row below the greeting: an avatar (initials circle w-8 h-8 rounded-full bg-[#dcfce7] text-[#008236] font-bold text-sm) + user namaLengkap + optional location/RT text, all in a horizontal pill-style chip
    - Below user chip: the antrian card (Section A or Section B depending on whether antrian exists)
    - Below antrian: LAYANAN CEPAT 2×2 grid (already implemented)
    - Below: PROFIL BALITA card (already implemented)
    - Below: TIPS GIZI card (already implemented)

    Implementation steps:

    1. REMOVE the entire green header div (currently lines ~108-139): the div with className containing "bg-[#008236]" that wraps the greeting, notification bell, and child chips. Delete this entire block.

    2. ADD a new white top section replacing it. Structure: div className="bg-white px-4 pt-10 pb-4 border-b border-[#f3f4f6]" containing:
       - A flex justify-between items-start row:
         - Left: div with p "Selamat datang," text-[#99a1af] text-xs and h1 with user?.namaLengkap text-[#1e2939] font-bold text-xl leading-tight
         - Right: the notification Bell button, restyled to bg-[#f3f4f6] rounded-[14px] p-2 (remove green background; use a light gray background instead); keep the red badge dot and aria-label

    3. ADD a user chip row below the greeting row inside the same top section. Structure: div className="mt-3 flex items-center gap-2" containing:
       - Avatar: div w-8 h-8 rounded-full bg-[#dcfce7] flex items-center justify-center flex-shrink-0, with span text-[#008236] font-bold text-sm showing the first letter of namaLengkap (use same pattern as ProfilSayaPage: user?.namaLengkap?.[0]?.toUpperCase() ?? 'W')
       - Name chip: span className="bg-[#f0fdf4] text-[#008236] text-xs font-semibold px-2.5 py-1 rounded-full" showing user?.namaLengkap ?? 'Warga'

    4. Keep ALL existing sections below the header exactly as they are: the isLoading Skeleton, Section A (antrian aktif blue card), Section B (no antrian white card), LAYANAN CEPAT grid, PROFIL BALITA card, TIPS GIZI banner, and the closing mb-6 padding.

    5. The root div className remains "min-h-full bg-[#f9fafb]" — no change.

    DO NOT change: useQuery, computeCountdown, isAntrian logic, posyanduNama, jamDisplay, statusLabel, estimasiMenit calculations, Section A antrian card content, Section B CTA button navigation, Layanan Cepat links, Profil Balita card, Tips Gizi banner, or any imports except if you need to add User from lucide-react (optional).
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - CitizenDashboardPage.tsx does NOT contain a standalone "bg-[#008236] px-4" div as the first child of the root container (green header removed)
    - CitizenDashboardPage.tsx contains "Selamat datang" text label
    - CitizenDashboardPage.tsx contains "bg-white px-4 pt-10" (or similar) as the new top section (no green background on the greeting)
    - CitizenDashboardPage.tsx still contains "useQuery" import (antrian query preserved)
    - CitizenDashboardPage.tsx still contains "computeCountdown" import (countdown logic preserved)
    - CitizenDashboardPage.tsx still contains "Ambil Antrian" CTA button text (Section B preserved)
    - CitizenDashboardPage.tsx still contains "LAYANAN CEPAT" section label (grid preserved)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>CitizenDashboardPage has white background greeting section at top (green header removed), user name + avatar chip row, and all existing antrian/navigation/data-fetching functionality preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo buttons → loginMutation | Demo credentials are hardcoded in frontend; they trigger the same auth endpoint as regular login |
| Browser → GET /api/antrian/saya | Citizen endpoint already secured from prior phases; no new surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | Demo credentials in LoginPage.tsx | accept | Demo credentials are intentionally public (CLAUDE.md § Seed Data); used only for development/demo environment; not production secrets |
| T-08-05-02 | Tampering | handleDemoLogin bypasses LoginForm validation | accept | handleDemoLogin calls loginMutation.mutate directly — same mutation as LoginForm; server-side validation is identical; no auth bypass occurs |
| T-08-05-03 | Information Disclosure | CitizenDashboard greeting shows namaLengkap | accept | namaLengkap from JWT payload (already in useAuthStore from prior auth); no new data exposure; user is already authenticated |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; MapPin is from lucide-react already in package.json |
</threat_model>

<verification>
Wave 5 verification gate:
1. `docker compose exec sispos-frontend npm run lint` exits 0 — no TypeScript errors in modified files
2. Browser: http://localhost/login — page background is mint green (not white/gray)
3. Browser: three "Demo Akun Cepat" buttons visible below login card
4. Browser: clicking "Demo Warga" auto-submits login and navigates to /citizen/dashboard
5. Browser: /citizen/dashboard — page starts with white/light background greeting (no green header at top)
6. Browser: user chip row visible below greeting on /citizen/dashboard
7. Browser: antrian card, Layanan Cepat grid, Profil Balita still visible on /citizen/dashboard
</verification>

<success_criteria>
- UI-01 (partial): LoginPage matches Figma 5:5731 — mint background, SISPOS logo, demo buttons, footer
- UI-01 (partial): CitizenDashboardPage matches Figma 5:2029 — white greeting header (green header removed), user chip
- No TypeScript errors introduced in either file
- All existing citizen dashboard functionality (antrian query, countdown, navigation) preserved
</success_criteria>

## Artifacts This Phase Produces

- Updated `frontend/src/pages/auth/LoginPage.tsx` — mint background, SISPOS logo block, demo quick-fill buttons, UU PDP footer
- Updated `frontend/src/pages/citizen/CitizenDashboardPage.tsx` — white greeting section replaces green header; user chip added; all business logic preserved

<output>
Create `.planning/phases/08-ui-figma-alignment/08-05-SUMMARY.md` when done.
</output>

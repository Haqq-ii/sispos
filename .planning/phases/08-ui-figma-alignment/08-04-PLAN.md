---
phase: 08-ui-figma-alignment
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - frontend/src/pages/puskesmas/PetaStuntingPage.tsx
  - frontend/src/pages/puskesmas/PuskesmasDashboardPage.tsx
  - frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "PetaStuntingPage uses mobile-first layout (no max-w-5xl mx-auto wrapper) with full-width map"
    - "PetaStuntingPage green header matches design system (bg-[#008236]) instead of current plain text h1"
    - "MapContainer has NO key prop (decisions log 2026-07-03 — prevents Map container already initialized error)"
    - "PuskesmasDashboardPage stats grid uses bg-white rounded-2xl cards matching Figma frame 5:13232"
    - "ManajemenPenggunaPage kader list cards use established card container pattern"
  artifacts:
    - path: "frontend/src/pages/puskesmas/PetaStuntingPage.tsx"
      provides: "Mobile-first Peta Stunting layout with green header"
      contains: "bg-[#008236]"
    - path: "frontend/src/pages/puskesmas/PuskesmasDashboardPage.tsx"
      provides: "Stats grid aligned to Figma 5:13232"
    - path: "frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx"
      provides: "Kader list with Figma-aligned card style"
  key_links:
    - from: "PetaStuntingPage.tsx"
      to: "GET /api/dashboard/stunting"
      via: "usePuskesmasStunting(bulan) useQuery"
      pattern: "dashboard/stunting"
    - from: "MapContainer (react-leaflet)"
      to: "OpenStreetMap tiles"
      via: "TileLayer url https://{s}.tile.openstreetmap.org"
      pattern: "openstreetmap"
---

<objective>
Wave 8.4: Align all Puskesmas screens to Figma file 4DIazKntakgAGXBDYefjbD. The highest-priority change is PetaStuntingPage — currently has a `p-6 max-w-5xl mx-auto` desktop wrapper that conflicts with the mobile-first PWA design. The map should be full-width with a proper green header. Second priority is PuskesmasDashboardPage stats grid and ManajemenPenggunaPage alignment.

Purpose: UI-02 covers Puskesmas screens. The peta stunting is a core Phase 4 deliverable — DSS-01 — and must look professional for the demo. The desktop-only max-width wrapper is immediately obvious as out-of-place in a mobile-first PWA.

Output: PetaStuntingPage mobile-first with green header; PuskesmasDashboardPage and ManajemenPenggunaPage Figma-aligned.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PetaStuntingPage mobile-first refactor</name>
  <files>frontend/src/pages/puskesmas/PetaStuntingPage.tsx</files>

  <read_first>
    - `frontend/src/pages/puskesmas/PetaStuntingPage.tsx` — full current implementation; note `p-6 max-w-5xl mx-auto` wrapper and plain text h1 header
    - `.planning/STATE.md` — decisions 2026-07-03: "MapContainer tanpa key prop" and "react-leaflet@4.2.1 pinned (v5 requires React 19)"
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 3.3 Risk 5 (Map container re-init), section 1.4 design tokens, section 3.2 Pattern 1 (green header), Pattern 2 (card)
    - CLAUDE.md — PuskesmasLayout wraps puskesmas pages; check if PetaStuntingPage is inside PuskesmasLayout sidebar (it is, per router/index.tsx)
  </read_first>

  <action>
    Pull Figma design context for the peta stunting screen. Use `get_design_context` with fileKey `4DIazKntakgAGXBDYefjbD`. Search metadata for frame named "Peta Stunting" or "Stunting Map". Pull `get_screenshot` for visual reference.

    Refactor PetaStuntingPage.tsx from desktop-first to mobile-first layout. Functional logic (usePuskesmasStunting hook, bulan state, MapContainer, CircleMarker, Popup, getMarkerColor) must NOT be modified.

    Layout changes:
    1. Remove the outer `p-6 max-w-5xl mx-auto` wrapper — replace with `min-h-full bg-[#f9fafb]` on the root div.
    2. Add a green header section: `bg-[#008236] px-4 pt-6 pb-5` containing:
       - Title: `text-white font-bold text-xl leading-tight` — "Peta Sebaran Stunting"
       - Subtitle: `text-[#b9f8cf] text-xs mt-0.5` — "Data posyandu per bulan"
    3. Bulan filter: move below the header in a `px-4 py-3 bg-white border-b border-[#f3f4f6] flex items-center gap-2` bar. Keep the existing `<input type="month" />` element. Style input with `border border-[#f3f4f6] rounded-[14px] px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#008236]`.
    4. Legenda: move to a `px-4 py-3 bg-white border-b border-[#f3f4f6] flex flex-wrap gap-3` strip below the filter bar.
    5. Map container div: `mx-4 my-3 rounded-2xl overflow-hidden border border-[#f3f4f6] shadow-sm`. Change MapContainer style height from `450px` to `calc(100vh - 240px)` and `min-height: 300px` to be responsive on mobile.
    6. Empty state div: `mx-4 mt-3 py-8 bg-white rounded-2xl border border-[#f3f4f6] text-center` — keep existing text.
    7. Loading overlay: keep unchanged (absolute inset-0 z-[1000]).

    CRITICAL CONSTRAINT: Do NOT add a `key` prop to MapContainer. The comment `CRITICAL: Do NOT add key prop to MapContainer` must remain. This was an explicit decisions log entry (2026-07-03) — violating it causes runtime "Map container is already initialized" error when bulan filter changes.

    After changes: `docker compose exec sispos-frontend npx tsc --noEmit` must exit 0.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npx tsc --noEmit 2>&amp;1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - PetaStuntingPage.tsx does NOT contain `max-w-5xl` or `max-w-5xl mx-auto` class (old desktop wrapper removed)
    - PetaStuntingPage.tsx contains `bg-[#008236]` in the header section (green header added)
    - PetaStuntingPage.tsx does NOT contain `key=` prop on `MapContainer` element (critical — no key on MapContainer)
    - PetaStuntingPage.tsx still contains `usePuskesmasStunting` hook call (data fetching preserved)
    - PetaStuntingPage.tsx still contains `CircleMarker` component usage (map markers preserved)
    - PetaStuntingPage.tsx map container div uses `rounded-2xl` class (card container pattern)
    - `docker compose exec sispos-frontend npx tsc --noEmit` exits 0
    - Browser at http://localhost/puskesmas/peta-stunting: map renders without "Map container is already initialized" console error when changing bulan filter
  </acceptance_criteria>

  <done>PetaStuntingPage has mobile-first layout with green header. max-w-5xl wrapper is gone. MapContainer has no key prop. Bulan filter change does not trigger map container error.</done>
</task>

<task type="auto">
  <name>Task 2: PuskesmasDashboardPage + ManajemenPenggunaPage Figma alignment</name>
  <files>
    frontend/src/pages/puskesmas/PuskesmasDashboardPage.tsx,
    frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/puskesmas/PuskesmasDashboardPage.tsx` — full current implementation; identify stats grid structure and quick action card layout
    - `frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx` — full current implementation; identify kader list card and "Buka Kunci" button styling
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 1.1 (PuskesmasDashboardPage: FULL status with green header and stats 2x2 grid), section 2 Figma frames 5:13232 (Dashboard), 5:14204 (Manajemen Pengguna)
    - `.planning/STATE.md` — decisions 2026-07-03: puskesmasId dari req.user!.userId (JWT) — verify no puskesmasId is passed from frontend (correct: backend extracts from JWT)
  </read_first>

  <action>
    Pull Figma design context for two frames:
    - PuskesmasDashboardPage: nodeId `5:13232` — call `get_design_context` and `get_screenshot`
    - ManajemenPenggunaPage: nodeId `5:14204` — call `get_design_context` and `get_screenshot`

    For PuskesmasDashboardPage:
    Compare the Figma screenshot against the current React component. Apply className changes to match Figma:
    - Stats grid: 2×2 grid of white cards; each card uses `bg-white border border-[#f3f4f6] rounded-2xl shadow-sm p-4`; stat value in `text-[#1e2939] font-bold text-2xl`; label in `text-[#99a1af] text-xs mt-1`.
    - Quick action cards: should use `rounded-2xl border border-[#f3f4f6] bg-white shadow-sm` (not bare gray-50 or similar).
    - Green header: must have `bg-[#008236]` — verify it already uses this (it should per RESEARCH.md FULL status); adjust if it uses Tailwind named colors instead of hex.
    - Do NOT remove the TanStack Query hooks (useQuery for stats data).
    - Do NOT remove Link tags to puskesmas sub-routes.

    For ManajemenPenggunaPage:
    Compare against Figma frame 5:14204. Apply className changes:
    - Kader list item card: `bg-white border border-[#f3f4f6] rounded-2xl shadow-sm px-4 py-4 flex items-center justify-between`.
    - Kader name: `text-[#1e2939] font-semibold text-sm`.
    - Kader HP: `text-[#99a1af] text-xs`.
    - "Buka Kunci" button: use `rounded-[14px] bg-[#008236] text-white text-xs px-3 py-1.5` for the unlock button; `rounded-[14px] border border-[#f3f4f6] text-[#364153] text-xs px-3 py-1.5` for secondary action.
    - Lock status badge: red bg-[#fee2e2] text-[#e7000b] rounded-[10px] px-2 py-0.5 text-xs for "Terkunci"; green bg-[#dcfce7] text-[#008236] for "Aktif".
    - Also check the Reset PIN modal (Figma frame 5:14838) if the component includes a dialog/modal for overrule — align its styling to match.
    - Do NOT remove the masterOverruleMutation call or the AuditLog-triggering behavior.

    After all changes: `docker compose exec sispos-frontend npm run lint` exits 0.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - PuskesmasDashboardPage.tsx stats cards contain `rounded-2xl` class
    - PuskesmasDashboardPage.tsx contains `bg-[#008236]` in header section
    - PuskesmasDashboardPage.tsx contains TanStack Query hook (useQuery or similar data fetching call preserved)
    - ManajemenPenggunaPage.tsx kader list items contain `rounded-2xl` class
    - ManajemenPenggunaPage.tsx "Buka Kunci" button action (masterOverruleMutation or similar mutation) is preserved (grep: `mutate` or `mutation` call present)
    - ManajemenPenggunaPage.tsx lock badge uses red color class for terkunci state (one of: text-[#e7000b], bg-[#fee2e2], text-red-600)
    - `docker compose exec sispos-frontend npm run lint` exits 0
    - Browser at http://localhost/puskesmas/dashboard (logged in as puskesmas): 2x2 stats grid visible, no console errors
    - Browser at http://localhost/puskesmas/manajemen-pengguna: kader list loads with card-style items, no console errors
  </acceptance_criteria>

  <done>PuskesmasDashboardPage stats grid and ManajemenPenggunaPage kader list aligned to Figma. Mutation handlers and data fetching hooks preserved. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Puskesmas browser → GET /api/dashboard/stunting | Puskesmas-only endpoint; JWT required; puskesmasId extracted from JWT server-side (never from query params) |
| Puskesmas browser → POST /api/users/kader/:id/unlock | Master overrule; puskesmas JWT required; AuditLog written server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | PetaStuntingPage aggregate data | accept | Map data is aggregate (posyandu-level breakdown counts, no individual citizen data); GET /dashboard/stunting already secured by puskesmasId from JWT (decisions log 2026-07-03) |
| T-08-04-02 | Elevation of Privilege | ManajemenPenggunaPage "Buka Kunci" | accept | masterOverruleMutation calls POST /api/users/kader/:id/unlock which requires puskesmas JWT server-side; AuditLog MASTER_OVERRULE written per KADER-02 requirement; no frontend-only authority |
| T-08-04-03 | Tampering | PetaStuntingPage MapContainer no-key rule | mitigate | Action explicitly prohibits adding key prop to MapContainer; acceptance criteria verifies no key prop; violation causes runtime error (not a security risk, but demo-breaking) |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; react-leaflet@4.2.1 already pinned in package.json |
</threat_model>

<verification>
Wave 8.4 checks:
1. `docker compose exec sispos-frontend npm run lint` exits 0
2. Browser at /puskesmas/peta-stunting: full-width map with green header, no max-width restriction visible
3. Browser bulan filter change on PetaStuntingPage: no "Map container is already initialized" console error
4. Browser at /puskesmas/dashboard: 2×2 stats grid with white rounded cards
5. Browser at /puskesmas/manajemen-pengguna: kader list shows cards with status badges
</verification>

<success_criteria>
- SC-3 (partial): Puskesmas screens (Dashboard, Peta Stunting, Manajemen Pengguna) visually match Figma
- SC-4a: Countdown realtime unaffected (no puskesmas-side countdown; Socket.IO server-side is independent of this wave)
- UI-02: Puskesmas screen alignment to Figma 4DIazKntakgAGXBDYefjbD
</success_criteria>

## Artifacts This Phase Produces

- Updated `frontend/src/pages/puskesmas/PetaStuntingPage.tsx` — mobile-first, green header
- Updated `frontend/src/pages/puskesmas/PuskesmasDashboardPage.tsx` — Figma-aligned stats grid
- Updated `frontend/src/pages/puskesmas/ManajemenPenggunaPage.tsx` — Figma-aligned kader list
- `.planning/phases/08-ui-figma-alignment/08-04-SUMMARY.md`

<output>
Create `.planning/phases/08-ui-figma-alignment/08-04-SUMMARY.md` when done.
</output>

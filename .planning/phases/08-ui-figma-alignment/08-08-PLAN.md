---
phase: 08-ui-figma-alignment
plan: 08
type: execute
wave: 8
depends_on: []
files_modified:
  - frontend/src/pages/auth/RegisterPage.tsx
  - frontend/src/pages/auth/VerifikasiOtpPage.tsx
  - frontend/src/pages/auth/OnboardingLokasiPage.tsx
  - frontend/src/pages/auth/LokasiSelesaiPage.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "RegisterPage has Back button, eye/password-toggle, and Submit wired to register mutation"
    - "VerifikasiOtpPage has 6 single-digit inputs that auto-advance focus, Resend OTP button, and Verify button"
    - "OnboardingLokasiPage has cascading WilayahSelect dropdowns and Simpan button wired to mutation"
    - "LokasiSelesaiPage has Mulai Gunakan button navigating to /citizen/dashboard"
  artifacts:
    - path: "frontend/src/pages/auth/VerifikasiOtpPage.tsx"
      provides: "6-digit OTP input with auto-advance and resend"
      contains: "autoFocus"
    - path: "frontend/src/pages/auth/LokasiSelesaiPage.tsx"
      provides: "Completion screen with Mulai Gunakan CTA"
      contains: "Mulai Gunakan"
  key_links:
    - from: "RegisterPage Submit"
      to: "POST /api/auth/register"
      via: "registerMutation.mutate"
      pattern: "registerMutation"
    - from: "LokasiSelesaiPage Mulai Gunakan"
      to: "/citizen/dashboard"
      via: "navigate or Link to /citizen/dashboard"
      pattern: "citizen/dashboard"
---

<objective>
Wave 8.8: Auth flow Figma alignment — RegisterPage, VerifikasiOtpPage, OnboardingLokasiPage, LokasiSelesaiPage.

These 4 auth screens complete the citizen registration funnel. All are FULL status (logic implemented) but need visual audit against Figma design and button handler verification. Every button must have a working onClick — no decorative buttons allowed.

Purpose: The registration flow is the first experience for new citizens. Visual gaps against Figma reduce trust.

Output: 4 auth pages visually aligned to Figma with all buttons verified to have real handlers.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegisterPage + VerifikasiOtpPage Figma alignment</name>
  <files>
    frontend/src/pages/auth/RegisterPage.tsx,
    frontend/src/pages/auth/VerifikasiOtpPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/auth/RegisterPage.tsx` — full file; note current layout, imports, mutation logic, Eye/EyeOff toggle, Back button onClick, Submit button onClick
    - `frontend/src/pages/auth/VerifikasiOtpPage.tsx` — full file; note OTP input structure (is it one input or 6 separate?), Resend OTP handler, Verify button handler, Back button
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 3.2 patterns
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:1209" (RegisterPage). Then get_design_context same params. Also get_screenshot nodeId "5:1287" (VerifikasiOtpPage). Study both frames.

    REGISTER PAGE — visual alignment only; preserve all mutation logic:
    - Background: ensure root container uses bg-[#f9fafb] or bg-white (check Figma frame — match exactly).
    - Header: back button (ChevronLeft) at top left linking to /login — verify onClick calls navigate(-1) or Link to="/login".
    - Title section: heading "Daftar Akun Baru" or equivalent per Figma — text-[#1e2939] font-bold.
    - Form card: bg-white rounded-2xl border border-[#f3f4f6] shadow-sm px-6 py-6 — if Figma shows a card, add it; if inline, leave it.
    - Eye/EyeOff password toggle: verify the button has onClick to toggle showPassword state. If missing, add it.
    - Submit button: bg-[#008236] text-white rounded-[14px] py-3 w-full — verify it calls registerMutation.mutate or handleSubmit. Verify disabled when registerMutation.isPending.
    - Align colors/spacing to Figma. Do NOT change form schema, validation logic, registerMutation, or navigation on success.

    OTP PAGE — if VerifikasiOtpPage uses a single `<input>` for 6 digits, refactor to 6 separate `<input type="text" maxLength={1}>` in a flex row with auto-advance logic:
    - Each input: `className="w-10 h-12 text-center text-xl font-bold border border-[#e5e7eb] rounded-xl focus:border-[#008236] focus:outline-none focus:ring-2 focus:ring-[#008236]"`
    - Auto-advance: onChange={(e) => { setValue(idx, e.target.value.slice(-1)); if(e.target.value && idx < 5) refs[idx+1].current?.focus() }}
    - Backspace: onKeyDown={(e) => { if(e.key==='Backspace' && !otpValues[idx] && idx > 0) refs[idx-1].current?.focus() }}
    - Use useRef array for focus management: `const refs = Array.from({length:6}, () => useRef<HTMLInputElement>(null))`
    - Combined OTP value for submission: otpValues.join('')
    - If OTP page already has 6 separate inputs with auto-advance, only align visual to Figma (colors, spacing, border radius).
    - Resend OTP button: verify onClick calls POST /api/auth/resend-otp or equivalent mutation; if handler is missing add it.
    - Verify button: verify onClick calls the OTP verification mutation with combined 6-digit string.
    - Back button: navigate(-1) or Link to="/register".
    - Visual: bg-[#f9fafb] or white background per Figma. Green header or inline title per Figma.
    Do NOT change: existing OTP verification mutation, resend mutation, navigation on success/failure.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - RegisterPage.tsx contains "onClick" on Back button (ChevronLeft / ArrowLeft area)
    - RegisterPage.tsx contains "registerMutation" or "handleSubmit" wired to the Submit button onClick
    - RegisterPage.tsx contains "EyeOff" or "Eye" toggle with onClick handler (password visibility)
    - RegisterPage.tsx contains "bg-[#008236]" on the submit button
    - VerifikasiOtpPage.tsx contains "maxLength={1}" or "maxLength=1" (individual digit inputs)
    - VerifikasiOtpPage.tsx contains "autoFocus" or "refs" for focus management
    - VerifikasiOtpPage.tsx contains "Backspace" key handler for backwards navigation
    - VerifikasiOtpPage.tsx contains onClick handler on Resend OTP button
    - VerifikasiOtpPage.tsx contains onClick handler on Verify button wired to verification mutation
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>RegisterPage and VerifikasiOtpPage visually aligned to Figma. All buttons have real handlers. OTP input uses 6 separate auto-advancing digit inputs.</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingLokasiPage + LokasiSelesaiPage Figma alignment</name>
  <files>
    frontend/src/pages/auth/OnboardingLokasiPage.tsx,
    frontend/src/pages/auth/LokasiSelesaiPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/auth/OnboardingLokasiPage.tsx` — full file; note WilayahSelect cascade dropdowns, Simpan onClick, mutation, navigation on success
    - `frontend/src/pages/auth/LokasiSelesaiPage.tsx` — full file; note the "Mulai Gunakan" button onClick — does it navigate to /citizen/dashboard?
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:1366" (OnboardingLokasi). Then get_design_context same params. Also get_screenshot nodeId "5:1438" (LokasiSelesai). Study both frames.

    ONBOARDING LOKASI PAGE — visual alignment; preserve wilayah cascade logic:
    - Background: match Figma (bg-[#f9fafb] or white).
    - Header section: title "Pilih Lokasi Posyandu" or Figma text, subtitle about selecting nearest posyandu.
    - WilayahSelect cascade: Provinsi → Kabupaten/Kota → Kecamatan → Kelurahan → Posyandu dropdowns must all be visible. Each uses shadcn Select component.
    - Simpan/Lanjut button: bg-[#008236] text-white rounded-[14px] py-3 w-full. Verify onClick calls the location save mutation (POST /api/users/lokasi or similar). Button should be disabled if no posyandu selected. If disabled attribute or condition is missing, add: `disabled={!selectedPosyanduId || isSaving}`.
    - Align colors and spacing to Figma. Do NOT change WilayahSelect cascade logic, API calls, or navigation on success.

    LOKASI SELESAI PAGE — this is a confirmation/success screen after location is saved:
    - Pull Figma frame 5:1438. This screen likely shows a success illustration, text like "Lokasi berhasil disimpan!", and a "Mulai Gunakan SISPOS" or "Masuk ke Dashboard" button.
    - Green background or green accent per Figma.
    - The primary CTA button must navigate to /citizen/dashboard. Verify onClick calls navigate('/citizen/dashboard') or is a Link to="/citizen/dashboard". If the button is missing or decorative, add proper handler.
    - Match Figma layout (illustration, title, subtitle, button) using available Lucide icons (MapPin, CheckCircle, etc.) as the illustration placeholder if Figma shows an image.
    - Style: bg-[#008236] button, text-[#1e2939] title, text-[#99a1af] subtitle.

    For both pages: TypeScript strict mode — no implicit any. Zod v4 pattern: use defaultValues in useForm, not .default() on schema.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - OnboardingLokasiPage.tsx contains onClick or onSubmit on the Simpan/Lanjut button wired to a mutation or handler
    - OnboardingLokasiPage.tsx contains "disabled" condition on the Simpan button (prevents submit without posyandu selection)
    - OnboardingLokasiPage.tsx contains "bg-[#008236]" on the primary button
    - OnboardingLokasiPage.tsx contains WilayahSelect or Select component usage (cascade dropdowns preserved)
    - LokasiSelesaiPage.tsx contains "Mulai Gunakan" or "Masuk ke Dashboard" button text
    - LokasiSelesaiPage.tsx contains "citizen/dashboard" in the button's navigation handler (navigate or Link)
    - LokasiSelesaiPage.tsx contains "bg-[#008236]" on the primary CTA button
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>OnboardingLokasiPage and LokasiSelesaiPage visually aligned to Figma. Simpan has proper disabled state. Mulai Gunakan navigates to /citizen/dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/auth/register | New citizen submits NIK + password (already secured from Phase 1) |
| Browser → POST /api/auth/verify-otp | OTP submitted for phone verification (already secured) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | RegisterPage Submit handler preserved | accept | No changes to registerMutation or schema; only visual changes; existing Zod validation remains |
| T-08-08-02 | Information Disclosure | Password field eye toggle shows plaintext | accept | Toggle is user-initiated; standard UX pattern; password field uses type="password" by default |
| T-08-08-03 | Tampering | OTP resend abuse | accept | Rate limiting is server-side from Phase 1; no changes to backend in this plan |
| T-08-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Wave 8 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /register — Back button, Submit button, eye toggle all visible and clickable
3. Browser: /register/verifikasi — 6 digit inputs, typing digit auto-advances to next, Resend and Verify buttons visible
4. Browser: /register/lokasi — cascade dropdowns visible, Simpan button disabled without selection
5. Browser: /register/lokasi-selesai — Mulai Gunakan button visible, clicking navigates to /citizen/dashboard
</verification>

<success_criteria>
- UI-01: Auth registration flow screens visually aligned to Figma
- Every button in the 4 auth screens has a real onClick handler (no decorative buttons)
- TypeScript strict mode: no errors introduced
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-08-SUMMARY.md` when done.
</output>

---
phase: 08-ui-figma-alignment
plan: 06
type: execute
wave: 6
depends_on: [08-05]
files_modified:
  - backend/src/modules/queue/queue-kader.service.ts
  - backend/src/modules/queue/queue-kader.controller.ts
  - backend/src/modules/queue/queue-kader.routes.ts
  - frontend/src/pages/kader/KaderDashboardPage.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "GET /api/kader/dashboard-stats returns totalBalita, risikoStunting, hadirHariIni scoped to kader's posyandu"
    - "KaderDashboardPage shows stats row from dashboard-stats endpoint (not from today-slots)"
    - "KaderDashboardPage has shadcn Tabs with Ringkasan tab containing recharts BarChart and PieChart"
    - "KaderDashboardPage has PERINGATAN RISIKO STUNTING table in Ringkasan tab"
    - "The new /dashboard-stats endpoint uses kaderId from JWT (not query param) for IDOR guard"
  artifacts:
    - path: "backend/src/modules/queue/queue-kader.routes.ts"
      provides: "GET /kader/dashboard-stats route"
      contains: "dashboard-stats"
    - path: "backend/src/modules/queue/queue-kader.service.ts"
      provides: "getKaderDashboardStats function"
      contains: "getKaderDashboardStats"
    - path: "frontend/src/pages/kader/KaderDashboardPage.tsx"
      provides: "Kader dashboard with Tabs, BarChart, PieChart, risk table"
      contains: "BarChart"
  key_links:
    - from: "KaderDashboardPage.tsx"
      to: "GET /api/kader/dashboard-stats"
      via: "useQuery queryKey ['kader', 'dashboard-stats']"
      pattern: "dashboard-stats"
    - from: "getKaderDashboardStats"
      to: "prisma.kader.findUnique"
      via: "kaderId from parameter (sourced from JWT in handler)"
      pattern: "getKaderDashboardStats"
---

<objective>
Wave 8.6: Kader Dashboard comprehensive redesign to match Figma frame 27:2531.

The current KaderDashboardPage shows today's slot stats (kuota/terisi) inside the green header. Figma 27:2531 shows a completely different structure: a stats row with Total Balita | Risiko Stunting | Hadir Hari Ini in the body (not header), a "Mulai Pelayanan Hari-H" card, and a tabbed section with recharts BarChart (tren gizi 6 bulan) + recharts PieChart (distribusi gizi bulan ini) + a PERINGATAN RISIKO STUNTING table.

This requires a new backend endpoint GET /api/kader/dashboard-stats that returns population-level stats for the kader's posyandu (total balita, risk count, today's attendance, 6-month trend, current month distribution, and top-risk list).

Purpose: The Kader Dashboard is the landing page for all kader users. It must show clinically useful data (stunting risk, nutrition trends) for posyandu management, matching the Figma design exactly.

Output: New backend endpoint returning kader-scoped posyandu statistics. KaderDashboardPage redesigned with tabs + recharts charts + risk table.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend GET /api/kader/dashboard-stats endpoint</name>
  <files>
    backend/src/modules/queue/queue-kader.service.ts,
    backend/src/modules/queue/queue-kader.controller.ts,
    backend/src/modules/queue/queue-kader.routes.ts
  </files>

  <read_first>
    - `backend/src/modules/queue/queue-kader.service.ts` — lines 1-70 for existing imports, logger, pino pattern, prisma/redis usage, and error code conventions
    - `backend/src/modules/queue/queue-kader.controller.ts` — lines 1-55 for ERROR_MAP pattern, handleErr helper, and existing handler structure to match
    - `backend/src/modules/queue/queue-kader.routes.ts` — full file to know where to append the new route
    - `prisma/schema.prisma` — Kader model (posyanduId, isKetua, isAktif), Balita (wargaId), Warga (posyanduUtamaId), Pemeriksaan (statusGizi, tanggalPemeriksaan, zScoreBbU, zScoreTbU, balitaId), Antrian (waktuCheckin, slotId), SlotSesi (jadwalId), Jadwal (posyanduId, tanggalPelaksanaan)
  </read_first>

  <action>
    Add getKaderDashboardStats to queue-kader.service.ts, a handler to queue-kader.controller.ts, and the route to queue-kader.routes.ts.

    SERVICE FUNCTION — add to queue-kader.service.ts after the getSlotAntrian function:

    getKaderDashboardStats(kaderId: string) must perform these queries in sequence or parallel (use Promise.all where safe):

    Step A: Get posyanduId — prisma.kader.findUnique where id = kaderId, select only posyanduId. If null (kader not found), throw an error with code 'KADER_TIDAK_DITEMUKAN'. Use the kaderId from the function parameter (sourced from JWT in handler — IDOR guard).

    Step B: totalBalita — prisma.balita.count where warga.posyanduUtamaId = posyanduId.

    Step C: hadirHariIni — Count antrian that have waktuCheckin IS NOT NULL, where slotSesi.jadwal.posyanduId = posyanduId AND slotSesi.jadwal.tanggalPelaksanaan equals today's date in WIB timezone. Use prisma.antrian.count with nested where: { slotSesi: { jadwal: { posyanduId, tanggalPelaksanaan: { equals: todayDate } } }, waktuCheckin: { not: null } }. Compute todayDate using: new Date(Date.now() + 7 * 60 * 60 * 1000) sliced to YYYY-MM-DD (match the technique in queue-kader.service for WIB timezone dates) then pass as a JavaScript Date object set to UTC midnight of that day.

    Step D: risikoStunting — Use prisma.$queryRaw to count distinct balita in the posyandu whose most recent pemeriksaan has statusGizi IN ('kurang', 'buruk', 'pendek', 'sangat_pendek'). The SQL logic: for each balita (filtered by warga.posyanduUtamaId = posyanduId), find their latest pemeriksaan by tanggalPemeriksaan, then count those with matching statusGizi. Use $queryRaw with tagged template literal and Prisma.sql for safe parameterization.

    Step E: trenGiziBulanan — Use prisma.$queryRaw to get grouped data for the last 6 months. Query: SELECT TO_CHAR(p."tanggalPemeriksaan", 'YYYY-MM') as bulan, p."statusGizi", COUNT(*)::int as jumlah FROM pemeriksaan p JOIN balita b ON p."balitaId" = b.id JOIN warga w ON b."wargaId" = w.id WHERE w."posyanduUtamaId" = posyanduId AND p."tanggalPemeriksaan" >= (NOW() AT TIME ZONE 'Asia/Jakarta' - INTERVAL '5 months')::date AND p."statusGizi" IS NOT NULL GROUP BY bulan, p."statusGizi" ORDER BY bulan. Use Prisma.sql with tagged template for posyanduId parameter. Transform the raw result into an array of { bulan: string, normal: number, kurang: number, buruk: number, pendek: number } where each month row aggregates across statusGizi values.

    Step F: distribusiGiziBulanIni — Similar to Step E but for the current month only (WHERE TO_CHAR(p."tanggalPemeriksaan", 'YYYY-MM') = TO_CHAR(NOW() AT TIME ZONE 'Asia/Jakarta', 'YYYY-MM')). Return { normal, kurang, buruk, pendek } counts.

    Step G: peringatanRisiko — prisma.pemeriksaan.findMany with: where statusGizi IN ['kurang', 'buruk', 'pendek', 'sangat_pendek'] AND balita.warga.posyanduUtamaId = posyanduId, orderBy zScoreBbU asc (nulls last), take 10, include balita.namaBalita. Deduplicate by balitaId (take only latest pemeriksaan per balita if multiple appear). Map result to { balitaId, namaBalita, zScoreBbU, zScoreTbU, statusGizi }.

    Return shape from getKaderDashboardStats:
    { totalBalita: number, risikoStunting: number, hadirHariIni: number, trenGiziBulanan: Array<{ bulan: string, normal: number, kurang: number, buruk: number, pendek: number }>, distribusiGiziBulanIni: { normal: number, kurang: number, buruk: number, pendek: number }, peringatanRisiko: Array<{ balitaId: string, namaBalita: string, zScoreBbU: number | null, zScoreTbU: number | null, statusGizi: string }> }

    CONTROLLER — add to queue-kader.controller.ts:
    Import getKaderDashboardStats from the service. Add getKaderDashboardStatsHandler: get kaderId from req.user!.userId, call getKaderDashboardStats(kaderId), return 200 with { success: true, data }. Catch errors: KADER_TIDAK_DITEMUKAN → 404. Other errors → 500 INTERNAL_ERROR. Do NOT log the kaderId as a sensitive value (it is not — it is from JWT, safe to log at debug level).

    ROUTE — add to queue-kader.routes.ts:
    queueKaderRouter.get('/kader/dashboard-stats', ...kaderAuth, getKaderDashboardStatsHandler)
    Place this line AFTER the existing '/kader/today-slots' route.

    TypeScript: Add explicit types to all variables in the new functions to satisfy strict mode. Import Prisma from '@prisma/client' if needed for Prisma.sql tagged template.
  </action>

  <verify>
    <automated>docker compose exec sispos-backend npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - queue-kader.routes.ts contains "dashboard-stats" in a route definition
    - queue-kader.service.ts contains "getKaderDashboardStats" function export
    - queue-kader.service.ts contains "posyanduUtamaId" (IDOR guard: lookup via kader→posyandu, then filter balita by warga.posyanduUtamaId)
    - queue-kader.service.ts contains "risikoStunting" or "risiko_stunting" as a variable or query alias
    - queue-kader.controller.ts contains "getKaderDashboardStatsHandler" function
    - docker compose exec sispos-backend npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>GET /api/kader/dashboard-stats endpoint returns population stats (totalBalita, risikoStunting, hadirHariIni, trenGiziBulanan, distribusiGiziBulanIni, peringatanRisiko) for the authenticated kader's posyandu. TypeScript strict mode clean.</done>
</task>

<task type="auto">
  <name>Task 2: KaderDashboardPage redesign with tabs and recharts (Figma 27:2531)</name>
  <files>frontend/src/pages/kader/KaderDashboardPage.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/KaderDashboardPage.tsx` — full current implementation (~220 lines); note which imports to keep and which to add
    - `frontend/src/pages/kader/meja/Meja3Page.tsx` — lines 1-40 for existing recharts import pattern (BarChart, PieChart imports, ResponsiveContainer, etc.); note the exact named exports used from recharts
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.3 shadcn components: Tabs IS installed (@radix-ui/react-tabs is in package.json); section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "27:2531". Then call mcp__plugin_figma_figma__get_design_context with the same file key and node ID. Study the layout — particularly the stats row, the tab structure, and the chart dimensions — before writing any JSX.

    From the confirmed UI gaps (planning context), the Figma frame 27:2531 shows:
    - Stats row: 3 stat boxes in a horizontal row — Total Balita | Risiko Stunting | Hadir Hari Ini
    - "Mulai Pelayanan Hari-H" card below stats
    - Tabs: Ringkasan | Data Balita | Absensi
    - Ringkasan tab: TREN STATUS GIZI bar chart (6 bulan) + STATUS GIZI BULAN INI donut chart + PERINGATAN RISIKO STUNTING table

    Implementation steps:

    1. ADD new imports to existing import list:
       - From recharts: BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell (import only what's needed; check Meja3Page.tsx for already-used pattern)
       - From shadcn: import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'

    2. ADD new useQuery for dashboard stats. queryKey: ['kader', 'dashboard-stats']. queryFn: apiClient.get('/kader/dashboard-stats').then(r => r.data.data as KaderDashboardStats). staleTime: 60_000. Define interface KaderDashboardStats matching the backend response shape: { totalBalita: number, risikoStunting: number, hadirHariIni: number, trenGiziBulanan: Array<{ bulan: string, normal: number, kurang: number, buruk: number, pendek: number }>, distribusiGiziBulanIni: { normal: number, kurang: number, buruk: number, pendek: number }, peringatanRisiko: Array<{ balitaId: string, namaBalita: string, zScoreBbU: number | null, zScoreTbU: number | null, statusGizi: string }> }.

    3. KEEP the green header section as-is (bg-[#008236] with kader name, logout button, SyncPendingBadge, install button). The header is correct and should not change.

    4. REMOVE the current stats row from inside the header (the 3 grid boxes for totalKuota/totalTerdaftar/totalAntrian). Move stats OUTSIDE the header, into the body.

    5. In the body section (below header), REPLACE the existing slot-based content with the new Figma-aligned layout:

       STATS ROW: After the header, in the body, add a div px-4 pt-4 grid grid-cols-3 gap-2 showing 3 stat cards using dashboardStats data:
       - "Total Balita": number from dashboardStats.totalBalita; label text-[#99a1af] text-xs
       - "Risiko Stunting": number from dashboardStats.risikoStunting in text-[#e7000b] (red) font-bold; label text-[#99a1af] text-xs
       - "Hadir Hari Ini": number from dashboardStats.hadirHariIni; label text-[#99a1af] text-xs
       Each stat card: bg-white border border-[#f3f4f6] rounded-2xl p-3 text-center shadow-sm. Show Skeleton during isLoading.

       MULAI PELAYANAN HARI-H CARD: Keep the existing todayJadwal slot buttons for navigating to pelayanan. Keep all existing logic for empty jadwal state. Style: bg-white border border-[#f3f4f6] rounded-2xl shadow-sm (existing pattern).

       TABS SECTION: Below the slot cards, add shadcn Tabs with defaultValue="ringkasan":
       - TabsList: flex row with 3 triggers — "Ringkasan", "Data Balita", "Absensi"
       - TabsContent value="ringkasan":
         a. TREN STATUS GIZI section: heading p text-[#99a1af] text-xs font-semibold tracking-wider mb-3 "TREN STATUS GIZI". ResponsiveContainer height={180}, BarChart data={dashboardStats?.trenGiziBulanan ?? []}. Bars: "normal" fill="#16a34a", "kurang" fill="#fbbf24", "buruk" fill="#ef4444", "pendek" fill="#f97316". XAxis dataKey="bulan" tick fontSize=10. No legend (keep compact). Wrapped in bg-white border border-[#f3f4f6] rounded-2xl p-4 shadow-sm.
         b. STATUS GIZI BULAN INI section: heading "STATUS GIZI BULAN INI". ResponsiveContainer height={160}, PieChart. Pie data built from distribusiGiziBulanIni: [{ name: 'Normal', value: distribusiGiziBulanIni.normal, color: '#16a34a' }, { name: 'Kurang', value: distribusiGiziBulanIni.kurang, color: '#fbbf24' }, { name: 'Buruk', value: distribusiGiziBulanIni.buruk, color: '#ef4444' }, { name: 'Pendek', value: distribusiGiziBulanIni.pendek, color: '#f97316' }]. innerRadius=40 outerRadius=70 (donut). Cell for each. Wrapped in bg-white border border-[#f3f4f6] rounded-2xl p-4 shadow-sm.
         c. PERINGATAN RISIKO STUNTING table: heading "PERINGATAN RISIKO STUNTING" in text-[#e7000b] text-xs font-semibold tracking-wider. Table or div-based list. For each item in peringatanRisiko: show namaBalita (text-[#1e2939] text-sm font-semibold), zScoreBbU formatted to 1 decimal (text-[#99a1af] text-xs), and statusGizi as a badge. Empty state: "Tidak ada balita berisiko tinggi" in text-[#99a1af] text-xs text-center py-4. Wrapped in bg-white border border-[#f3f4f6] rounded-2xl p-4 shadow-sm.
       - TabsContent value="data-balita": bg-white border border-[#f3f4f6] rounded-2xl p-4 text-center. p "Data lengkap tersedia di sistem Puskesmas." text-[#99a1af] text-xs.
       - TabsContent value="absensi": bg-white border border-[#f3f4f6] rounded-2xl p-4 text-center. p "Lihat rekap kehadiran di halaman Rekap Harian." text-[#99a1af] text-xs. Link to /kader/rekap.

    6. Keep all existing logic: useActiveMeja, setActiveMeja redirect useEffect, handleLogout, usePwaStore, totalKuota/totalTerdaftar/totalAntrian computations can be removed since they are replaced by dashboardStats.

    7. Adjust the combined isLoading check: isLoading = isLoadingActiveMeja || isLoadingJadwal || isLoadingStats (where isLoadingStats is from the new useQuery).

    IMPORTANT: Do not break the useActiveMeja useEffect that auto-redirects kader to their active meja on mount. This is critical for the lock-screen feature. Keep lines: `if (!isLoadingActiveMeja && activeMejaData) { ... navigate('/kader/meja/1') }` intact.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - KaderDashboardPage.tsx contains "BarChart" import from recharts
    - KaderDashboardPage.tsx contains "PieChart" import from recharts
    - KaderDashboardPage.tsx contains "Tabs" import from '@/components/ui/tabs'
    - KaderDashboardPage.tsx contains "dashboard-stats" in a queryKey array
    - KaderDashboardPage.tsx contains "TREN STATUS GIZI" section label
    - KaderDashboardPage.tsx contains "PERINGATAN RISIKO STUNTING" section label
    - KaderDashboardPage.tsx contains "risikoStunting" property access
    - KaderDashboardPage.tsx still contains the useActiveMeja redirect useEffect (must NOT be removed)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>KaderDashboardPage redesigned with stats row from new /api/kader/dashboard-stats endpoint, shadcn Tabs with Ringkasan/Data Balita/Absensi, recharts BarChart (tren gizi 6 bulan), recharts PieChart (distribusi gizi), and PERINGATAN RISIKO STUNTING table. useActiveMeja redirect preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader JWT → GET /api/kader/dashboard-stats | Authenticated kader accesses posyandu-level statistics |
| Backend → PostgreSQL (prisma.$queryRaw) | Raw SQL queries with parameterized inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure (IDOR) | GET /api/kader/dashboard-stats | mitigate | kaderId taken from req.user!.userId (JWT), NOT from query params; posyanduId looked up from DB via kaderId; all queries scoped to that posyanduId; a kader cannot access another posyandu's stats |
| T-08-06-02 | Tampering (SQL Injection) | prisma.$queryRaw in trenGiziBulanan and risikoStunting queries | mitigate | Use Prisma.sql tagged template literal (not string concatenation) for all $queryRaw calls; posyanduId passed as safe parameter $1 |
| T-08-06-03 | Information Disclosure | peringatanRisiko returns Z-scores + health data | accept | Data is scoped to kader's own posyandu; kader is an authorized healthcare worker with legitimate access to this data; no PDP-sensitive encrypted fields (catatanKonsultasi, rekomendasiAi) are included |
| T-08-06-04 | Spoofing | requireRole('kader', 'ketua_kader') on dashboard-stats | mitigate | kaderAuth middleware (authMiddleware + requireRole) already applied as spread per existing pattern in queue-kader.routes.ts; puskesmas and citizen cannot access this endpoint |
| T-08-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; recharts and @radix-ui/react-tabs already in package.json |
</threat_model>

<verification>
Wave 6 verification gate:
1. `docker compose exec sispos-backend npx tsc --noEmit` exits 0 — backend TypeScript clean
2. `docker compose exec sispos-frontend npm run lint` exits 0 — frontend TypeScript clean
3. Browser: /kader/dashboard — Tabs render (Ringkasan/Data Balita/Absensi)
4. Browser: Ringkasan tab — BarChart and PieChart render (may be empty if no pemeriksaan data for posyandu)
5. Browser: Stats row shows "Total Balita", "Risiko Stunting", "Hadir Hari Ini" labels
6. `curl -s -X GET http://localhost/api/kader/dashboard-stats -H "Cookie: [kader-session-cookie]"` returns 200 with success:true (or 401 without auth)
</verification>

<success_criteria>
- UI-02 (partial): KaderDashboardPage matches Figma 27:2531 — stats row, Mulai Pelayanan card, tabbed section with recharts charts and risk table
- Backend: GET /api/kader/dashboard-stats returns kader-scoped population stats with full IDOR guard
- TypeScript strict mode: no errors in backend or frontend
</success_criteria>

## Artifacts This Phase Produces

- Updated `backend/src/modules/queue/queue-kader.service.ts` — getKaderDashboardStats function
- Updated `backend/src/modules/queue/queue-kader.controller.ts` — getKaderDashboardStatsHandler
- Updated `backend/src/modules/queue/queue-kader.routes.ts` — GET /kader/dashboard-stats route
- Updated `frontend/src/pages/kader/KaderDashboardPage.tsx` — redesigned with Tabs + recharts

<output>
Create `.planning/phases/08-ui-figma-alignment/08-06-SUMMARY.md` when done.
</output>

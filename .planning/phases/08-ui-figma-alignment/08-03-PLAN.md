---
phase: 08-ui-figma-alignment
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - frontend/src/pages/kader/LockScreenPage.tsx
  - frontend/src/pages/kader/KaderDashboardPage.tsx
  - frontend/src/pages/kader/meja/Meja2Page.tsx
  - frontend/src/pages/kader/meja/Meja4Page.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "LockScreenPage shows meja selection UI matching Figma design — richer than the current plain button list"
    - "LockScreenPage header uses bg-[#008236] (not bg-white with text-primary)"
    - "KaderDashboardPage stats row (total balita, antrian aktif, posyandu) visually matches Figma frame 5:9717"
    - "Meja2Page numpad grid uses the same button style as Figma frames 5:9785 and 5:9865"
    - "Meja4Page STT section and AI early warning section are laid out consistently with Figma"
  artifacts:
    - path: "frontend/src/pages/kader/LockScreenPage.tsx"
      provides: "Visually polished meja selector matching Figma design"
      contains: "bg-[#008236]"
    - path: "frontend/src/pages/kader/KaderDashboardPage.tsx"
      provides: "Stats row visually aligned to Figma 5:9717"
    - path: "frontend/src/pages/kader/meja/Meja2Page.tsx"
      provides: "Numpad buttons aligned to Figma 5:9785 / 5:9865"
  key_links:
    - from: "LockScreenPage.tsx (meja button click)"
      to: "PATCH /api/kader/active-meja"
      via: "useMutationSetActiveMeja().mutate"
      pattern: "useMutationSetActiveMeja"
    - from: "Meja2Page.tsx (numpad submit)"
      to: "POST /api/growth/pemeriksaan"
      via: "createPemeriksaanMutation.mutate"
      pattern: "createPemeriksaanMutation"
---

<objective>
Wave 8.3: Align all Kader screens to Figma file 4DIazKntakgAGXBDYefjbD. The highest-priority visual gap is LockScreenPage which currently renders a plain button list with a white header bar — Figma likely shows a green header + richer meja cards. Second priority is KaderDashboardPage stats visual and Meja 2 numpad alignment. This wave covers LockScreenPage, KaderDashboardPage, Meja2Page, and Meja4Page.

Purpose: UI-02 requires all Kader screens to visually match Figma. Phase 7 pause explicitly identified kader dashboard UI as needing fixing. LockScreenPage is the entry point for all meja flows — a poor visual here affects the demo experience of the entire kader journey.

Output: Visually polished kader screens aligned to Figma design tokens and layout structure.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LockScreenPage visual polish — green header + meja cards</name>
  <files>frontend/src/pages/kader/LockScreenPage.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/LockScreenPage.tsx` — full current implementation; note: header is `bg-white border-b px-4 py-3 flex items-center gap-2` — this is the BASIC/plain version that needs upgrade
    - `frontend/src/pages/kader/KaderDashboardPage.tsx` — lines 1-80 for established green header pattern and icon usage in kader context
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 3.2 Pattern 1 (green header), Pattern 2 (card container)
    - CLAUDE.md — UI text in Bahasa Indonesia; Figma MCP mandatory
  </read_first>

  <action>
    Pull Figma design context for the kader lock-screen or meja-selector frame. In Figma file 4DIazKntakgAGXBDYefjbD, use `get_design_context` with fileKey `4DIazKntakgAGXBDYefjbD`. There is no confirmed frame ID for LockScreenPage in the research — search the file metadata by name using `get_metadata` for frames named "Lock Screen", "Pilih Meja", or "Meja Selector". Pull `get_screenshot` for the found frame.

    Based on the Figma design found, refactor LockScreenPage.tsx. The functional logic (slotId from router state, useMutationSetActiveMeja, navigate on success) must NOT be modified — only the JSX and className changes are in scope.

    Minimum changes required based on known gap:
    1. Replace the white border-b header with a green header matching the established kader green header pattern: outer div `bg-[#008236] px-4 pt-10 pb-6`, subtitle `text-[#7bf1a8] text-xs font-medium mb-0.5` saying "Sesi Pelayanan", title `text-white font-bold text-xl leading-tight` saying "Pilih Meja Pelayanan", caption `text-[#b9f8cf] text-xs mt-1` showing the slotId or today's date if slotId unavailable.
    2. Replace `bg-gray-50` on root div with `bg-[#f9fafb]` (established bg token).
    3. For each meja button, update the style to use the card container pattern: `bg-white border border-[#f3f4f6] rounded-2xl shadow-sm px-4 py-4 w-full text-left` — colored accent via a left colored border or a colored meja icon. Remove the per-meja color (`bg-blue-50 border-blue-200` etc) in favor of a unified card style with a colored number badge or icon per meja.
    4. Add meja number badge: a circle `w-9 h-9 rounded-full flex items-center justify-center text-white font-bold text-sm` with background color per meja (Meja 1: bg-blue-500, Meja 2: bg-[#008236], Meja 3: bg-yellow-500, Meja 4: bg-purple-500, Meja 5: bg-[#e17100]). Badge sits inline with the meja label.
    5. Add meja subtitle descriptions in text-[#99a1af] text-xs: Meja 1 "Pendaftaran & Kehadiran Balita", Meja 2 "Penimbangan & Pengukuran", Meja 3 "Pencatatan Klinis & Grafik", Meja 4 "Konseling & AI Early Warning", Meja 5 "Selesai Pelayanan & Imunisasi".

    If the Figma frame found differs significantly from the above minimum changes, follow the Figma design instead and note the actual changes in the task output.

    After changes: `docker compose exec sispos-frontend npx tsc --noEmit` must exit 0.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npx tsc --noEmit 2>&amp;1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - LockScreenPage.tsx contains `bg-[#008236]` in the header section (grep: file must contain this class)
    - LockScreenPage.tsx does NOT contain `bg-white border-b` in the header div (old plain header is removed)
    - LockScreenPage.tsx still contains `useMutationSetActiveMeja` import and call (functional logic preserved)
    - LockScreenPage.tsx still contains `navigate` to `/kader/meja/` on mutation success
    - TypeScript: `docker compose exec sispos-frontend npx tsc --noEmit` exits 0
    - Browser at http://localhost/kader/lock-screen (with kader JWT and a valid slotId in state) shows green header and card-style meja buttons without console errors
  </acceptance_criteria>

  <done>LockScreenPage has green header + card-style meja selector. Plain button list is gone. Functional navigation to meja routes is intact. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: KaderDashboardPage + Meja2Page + Meja4Page Figma alignment</name>
  <files>
    frontend/src/pages/kader/KaderDashboardPage.tsx,
    frontend/src/pages/kader/meja/Meja2Page.tsx,
    frontend/src/pages/kader/meja/Meja4Page.tsx
  </files>

  <read_first>
    - `frontend/src/pages/kader/KaderDashboardPage.tsx` — full implementation; identify stats row layout and jadwal card structure
    - `frontend/src/pages/kader/meja/Meja2Page.tsx` — lines 1-100 for numpad section; identify current numpad button className pattern
    - `frontend/src/pages/kader/meja/Meja4Page.tsx` — lines 1-80 for STT section and AI early warning card layout
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 (design tokens), section 3.2 Pattern 2 (card), section 3.3 anti-patterns (do not replace working logic)
    - `.planning/STATE.md` — decisions 2026-07-04: showInstall computed inline in KaderDashboardPage (not in usePwaStore); TooltipContent only rendered when !isOnline in Meja4
  </read_first>

  <action>
    Pull Figma design context for three frames:
    - KaderDashboardPage: nodeId `5:9717` (Dashboard kader)
    - Meja2Page: nodeId `5:9785` and `5:9865` (Meja 2 BB/TB numpad variants)
    - Meja4Page: search by name in Figma file ("Meja 4", "Konseling") — no confirmed frame ID

    For each screen, call `get_design_context` with the specified nodeId, then `get_screenshot` for visual reference. Compare against the current React implementation and apply the minimum changes needed to match.

    KaderDashboardPage alignment guidance:
    - Stats row: ensure 3 stat cards (total balita, antrian aktif, posyandu name) use the `bg-white border border-[#f3f4f6] rounded-2xl shadow-sm p-4` card pattern (not bare Tailwind gray colors).
    - Stats values: text-[#1e2939] font-bold text-xl; label: text-[#99a1af] text-xs.
    - Do NOT remove the PWA install button logic (showInstall computed inline from window.matchMedia per decisions log 2026-07-04).
    - Do NOT remove useKaderSocket import or any socket event handlers.

    Meja2Page numpad alignment guidance:
    - Numpad grid: each digit button should use consistent rounded-2xl or rounded-[14px] shape per Figma.
    - Button active/press state: bg-[#008236] text-white for selected/active, bg-[#f3f4f6] text-[#1e2939] for inactive.
    - Do NOT modify the numpad onChange handler or biological gate logic (BB > 30 kg confirmation).
    - Do NOT remove the `X-Konfirmasi-Biologis` header logic from the mutation.

    Meja4Page alignment guidance:
    - STT mic button: rounded-full, size w-16 h-16, bg-[#008236] when ready, bg-[#e7000b] when recording — maintain isRecording visual state.
    - AI early warning card: bg-white rounded-2xl border border-[#f3f4f6] shadow-sm p-4, warning text in text-[#1e2939].
    - Do NOT remove the TooltipContent conditional render when !isOnline (decisions log 2026-07-04).
    - Do NOT modify the auto-stop 45s timer or the voice recording hooks.

    After all changes: run `docker compose exec sispos-frontend npm run lint` to confirm TypeScript is clean.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - KaderDashboardPage.tsx: stats cards contain `rounded-2xl` class (Figma card container pattern applied)
    - KaderDashboardPage.tsx: contains `showInstall` variable (PWA install logic not removed)
    - Meja2Page.tsx: numpad buttons contain `rounded` class (not bare button without radius styling)
    - Meja2Page.tsx: contains `X-Konfirmasi-Biologis` string (biological gate header preserved)
    - Meja4Page.tsx: mic button contains `rounded-full` class
    - Meja4Page.tsx: contains `isRecording` state usage (STT recording logic preserved)
    - `docker compose exec sispos-frontend npm run lint` exits 0
    - Browser at http://localhost/kader/dashboard shows stats row without console errors
    - Browser at http://localhost/kader/meja/2 (with active meja state) shows numpad without console errors
  </acceptance_criteria>

  <done>KaderDashboardPage stats, Meja2Page numpad, and Meja4Page STT/AI layout aligned to Figma. All functional hooks and mutation handlers preserved. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader browser → PATCH /api/kader/active-meja | Kader selects meja; slotId validated backend-side |
| Figma MCP → design context | Read-only call to Figma cloud; no project data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Spoofing | LockScreenPage meja selection | accept | slotId comes from router state (set by KaderDashboardPage after authenticated jadwal fetch); PATCH /api/kader/active-meja validates slotId + kader role server-side; no new auth surface added |
| T-08-03-02 | Tampering | Meja2Page biological gate | accept | X-Konfirmasi-Biologis header logic is preserved in action constraints; alignment is className-only; backend rejects BB > 30 kg without confirmation header regardless of frontend state |
| T-08-03-03 | Tampering | Meja4Page STT auto-stop | accept | 45s auto-stop timer in useVoiceRecorder.ts is not touched by this plan; only Meja4Page className changes |
| T-08-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all components (Tooltip, Button, Select) already in package.json |
</threat_model>

<verification>
Wave 8.3 checks:
1. `docker compose exec sispos-frontend npm run lint` exits 0
2. Browser kader flow: Login → Dashboard → PelayananHariH → LockScreen → Meja 1 — no white screen, no console errors at any step
3. LockScreenPage header is green (bg-[#008236]) — visible by eye test
4. KaderDashboardPage stats row shows rounded cards (not bare div)
5. Meja2Page numpad buttons are rounded (matches Figma)
</verification>

<success_criteria>
- SC-2 (partial): Kader screens (Dashboard, LockScreen, Meja 2, Meja 4) visually match Figma
- UI-02: Kader screen alignment to Figma 4DIazKntakgAGXBDYefjbD
</success_criteria>

## Artifacts This Phase Produces

- Updated `frontend/src/pages/kader/LockScreenPage.tsx` — green header + card meja selector
- Updated `frontend/src/pages/kader/KaderDashboardPage.tsx` — stats alignment
- Updated `frontend/src/pages/kader/meja/Meja2Page.tsx` — numpad alignment
- Updated `frontend/src/pages/kader/meja/Meja4Page.tsx` — STT/AI layout alignment
- `.planning/phases/08-ui-figma-alignment/08-03-SUMMARY.md`

<output>
Create `.planning/phases/08-ui-figma-alignment/08-03-SUMMARY.md` when done.
</output>

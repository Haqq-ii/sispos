---
phase: 08-ui-figma-alignment
plan: 16
type: execute
wave: 16
depends_on:
  - 08-07
  - 08-08
  - 08-09
  - 08-10
  - 08-11
  - 08-12
  - 08-13
  - 08-14
  - 08-15
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "npm run lint exits 0 with no TypeScript errors"
    - "No console errors appear during core demo flow (login → citizen dashboard → antrian → kader meja 1-5)"
    - "Human confirms all key screens render correctly in browser on mobile viewport"
    - "TukarMejaModal blocks exit from meja pages without ketua PIN"
    - "Selesai Pelayanan at Meja 5 triggers Socket.IO broadcast (citizen countdown updates)"
  artifacts: []
  key_links:
    - from: "Final QA"
      to: "Phase 08 complete"
      via: "human approval in checkpoint"
      pattern: "approved"
---

<objective>
Wave 8.16: Final QA checkpoint for Phase 08 — all UI Figma alignment plans complete.

This is the final gate for Phase 08. All preceding plans (08-01 through 08-15) must be complete before this runs. The QA has two parts:

1. Automated lint + TypeScript check across the entire frontend (zero errors required).
2. Human-verify checkpoint — the developer walks through the core demo flow in a browser on mobile viewport (375px) and confirms all screens render correctly, all critical buttons work, and the TukarMeja + Selesai Pelayanan flows function end-to-end.

Purpose: Phase 08 is UI alignment. The gate is not just "code compiles" — it is "the UI actually looks right and works." Only a human can confirm that.

Output: Phase 08 marked complete in STATE.md. ROADMAP.md phase 08 checked off.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full frontend lint + TypeScript strict check</name>
  <files></files>

  <action>
    Run the full TypeScript compiler check and lint across the frontend. This catches any type errors introduced during the 08-08 through 08-15 plans.

    Steps:
    1. docker compose exec sispos-frontend npm run lint — must exit 0 (no ESLint errors)
    2. docker compose exec sispos-frontend npx tsc --noEmit -- if tsc is available, run TypeScript compiler check without emitting files. If the command fails with "not found", skip and use lint only.
    3. If any lint errors are found: fix them. Common issues from this phase:
       - Missing return types on event handlers (add `: void`)
       - Unused imports from the alignment work (remove them)
       - `any` types on imunisasi query data (add ImunisasiItem[] explicit type)
       - Missing dependency array items in useEffect (add them)
    4. After fixing: re-run lint to confirm exit 0.

    If lint passes with 0 errors: proceed to checkpoint task.
    If lint has irreducible errors (e.g., a pre-existing error not introduced by phase 08): document it in the summary with file name and line number, then proceed to checkpoint.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - npm run lint exits 0 (zero ESLint errors across all modified files in phase 08)
    - No new TypeScript errors introduced by phase 08 plans (08-07 through 08-15)
  </acceptance_criteria>

  <done>Frontend lint passes with 0 errors. Any pre-existing errors documented in summary.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
    All Phase 08 UI Figma alignment plans (08-01 through 08-15) are now complete:

    CITIZEN SCREENS:
    - 08-05: Login, Register, OTP, Onboarding, CitizenDashboard, ProfilSaya redesigned
    - 08-08: RegisterPage, VerifikasiOTP, OnboardingLokasi, LokasiSelesai aligned
    - 08-09: Antrian flow (PilihTanggal, PilihSesi, KonfirmasiAntrian, TiketAntrian) aligned
    - 08-10: TumbuhKembangPage grafik+imunisasi tabs implemented; ChatAssistantPage aligned
    - 08-15: LandingPage mobile-first Figma alignment

    KADER SCREENS:
    - 08-06: KaderDashboard with backend stats + Peta Stunting
    - 08-07: TukarMejaModal (fullscreen ketua-PIN gate) + PelayananHariHPage confirmation + all 5 meja pages wired
    - 08-11: Meja1Page header + antrian cards + go-show form aligned
    - 08-12: Meja2Page numpad + biologic Dialog; Meja3Page Z-Score + native checkboxes
    - 08-13: Meja4Page mic circle button states; Meja5Page orange header + Selesai Pelayanan (CRITICAL)
    - 08-14: KaderProfilPage implemented (STUB → functional with Logout)

    PUSKESMAS SCREENS:
    - 08-03: ManajemenPengguna green header + card design tokens
    - 08-04: Puskesmas screens aligned to design tokens
    - 08-14: AuditLogPage pagination, ManajemenJadwalPage, LaporanPage button audit
  </what-built>

  <how-to-verify>
    Open browser at http://localhost — set viewport to 375px mobile width (DevTools → iPhone SE or similar).

    CORE DEMO FLOW (in order):
    1. Landing page (/) — mobile single column, SISPOS hero, Masuk and Daftar Sekarang buttons visible and clickable
    2. Login (/login) — mint-green bg, detect role from input, login with Citizen NIK 3471012345670001 / Demo1234!
    3. Citizen Dashboard (/citizen/dashboard) — health stats, antrian card or CTA, bottom nav
    4. TumbuhKembang (/citizen/tumbuh-kembang) — Grafik tab shows ZScoreChart (or empty state), Imunisasi tab shows list (NOT "Segera Hadir" placeholder)
    5. ChatAssistant (/citizen/chat-assistant) — quick suggestion chips clickable, send message, assistant responds
    6. Logout from citizen. Login as Kader HP 081234560001 / PIN 123456
    7. KaderDashboard (/kader/dashboard) — stats loaded, Mulai Pelayanan button opens PelayananHariHPage (not direct meja selection)
    8. PelayananHariHPage — sesi selected, Konfirmasi Pilih Meja dialog shows before entering meja
    9. Meja1 (/kader/meja/1) — Lock icon in header, antrian cards with Hadir/Tangguhkan, Tukar Meja button visible
    10. Click Tukar Meja — TukarMejaModal fullscreen opens (NOT a shadcn Dialog), requires Ketua PIN
    11. Enter wrong PIN in TukarMejaModal — error message shows (e.g., "PIN tidak valid")
    12. Enter correct Ketua PIN 654321 — TukarMejaModal closes, returns to PelayananHariHPage meja selection
    13. Enter Meja2 — numpad renders 12 buttons (0-9, decimal, backspace), BB/TB toggle switches active input
    14. Meja3 — ZScoreChart visible, tanda klinis checkboxes clickable (native green checkboxes)
    15. Meja4 — mic button is w-16 h-16 rounded-full green (idle) or red (recording)
    16. Meja5 — orange #e17100 header background, Selesai Pelayanan button visible with confirmation
    17. KaderProfil (/kader/profil) — shows kader name, role chip, HP — NOT "Halaman sedang dikembangkan"
    18. Logout from kader. Login as Puskesmas demo@puskesmas-mergangsan.go.id / Demo1234!
    19. Puskesmas Dashboard — stats loaded, navigation works
    20. ManajemenJadwal — Buat Jadwal Baru button opens dialog
    21. Laporan — bulan filter and download buttons visible
    22. AuditLog — pagination prev/next buttons visible and clickable

    OPEN BROWSER CONSOLE during the flow:
    - No unhandled JavaScript errors (red console errors)
    - Network 4xx/5xx errors are acceptable if they are pre-existing API limitations (document them)
    - No "Warning: Each child in a list should have a unique key" React errors (fix if any appear)
  </how-to-verify>

  <resume-signal>
    Type "approved" if all screens render correctly and the core demo flow works end-to-end.
    If issues found, describe which screen and what is wrong — Claude will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA human tester → browser at localhost | Local dev environment; no production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16-01 | Information Disclosure | Console.log statements in production build | mitigate | Run npm run lint — ESLint no-console rule should flag any debug console.log added during phase 08; remove all before marking complete |
| T-08-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Wave 16 gate (FINAL PHASE GATE):
1. npm run lint exits 0 — zero ESLint errors
2. Human checkpoint approved — all screens render correctly on 375px mobile viewport
3. TukarMejaModal flow works end-to-end (enter meja → wrong PIN rejected → correct PIN 654321 accepted → returns to meja selection)
4. Selesai Pelayanan at Meja 5 triggers Socket.IO broadcast visible in network tab (WS frame `queue:update`)
5. No "Halaman sedang dikembangkan" placeholder text on KaderProfilPage
6. TumbuhKembangPage Grafik and Imunisasi tabs show real content (no "Segera Hadir" text)
</verification>

<success_criteria>
- UI-01: All citizen screens aligned to Figma — no placeholder stubs remaining
- UI-02: All kader screens aligned to Figma — TukarMeja gate works, 5-meja flow complete
- UI-03: Selesai Pelayanan broadcasts queue:update via Socket.IO
- Phase 08 complete — ROADMAP.md Phase 08 checkbox marked done
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-16-SUMMARY.md` when done.

After human approval: update `.planning/STATE.md` to mark Phase 08 complete and set currentPhase to the next pending phase.
Update `.planning/ROADMAP.md` Phase 08 checklist item to [x].
</output>

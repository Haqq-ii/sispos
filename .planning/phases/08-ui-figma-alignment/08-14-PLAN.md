---
phase: 08-ui-figma-alignment
plan: 14
type: execute
wave: 14
depends_on: []
files_modified:
  - frontend/src/pages/kader/KaderProfilPage.tsx
  - frontend/src/pages/puskesmas/AuditLogPage.tsx
  - frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx
  - frontend/src/pages/puskesmas/LaporanPage.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "KaderProfilPage shows kader name, HP, role from useAuthStore and has a working Logout button"
    - "AuditLogPage pagination prev/next buttons have onClick handlers calling setPage"
    - "ManajemenJadwalPage Buat Jadwal Baru button opens BuatJadwalDialog with working Simpan"
    - "LaporanPage Download Excel and Download PDF buttons call window.open or equivalent download handler"
  artifacts:
    - path: "frontend/src/pages/kader/KaderProfilPage.tsx"
      provides: "Kader profile page showing auth store data with Logout button"
      contains: "useAuthStore"
  key_links:
    - from: "KaderProfilPage Logout button"
      to: "POST /api/auth/logout"
      via: "logoutMutation.mutate or handleLogout"
      pattern: "logout"
    - from: "AuditLogPage pagination"
      to: "GET /api/audit-log?page=N"
      via: "setPage(page + 1) / setPage(page - 1)"
      pattern: "setPage"
---

<objective>
Wave 8.14: KaderProfilPage implementation + Puskesmas supporting screens alignment.

KaderProfilPage is currently a STUB ("Halaman sedang dikembangkan"). It needs to show kader profile info from useAuthStore and provide a Logout button. This is a basic implementation — no API calls needed, data is already in auth store.

AuditLogPage is BASIC (exists, functional, but not fully audited). Pagination buttons (prev/next) must be verified to have real handlers. ManajemenJadwalPage and LaporanPage are FULL but their buttons need a final audit to confirm all handlers are wired.

Purpose: KaderProfilPage is visible in the kader nav. AuditLogPage and Laporan are important Puskesmas tools. All buttons must work for demo.

Output: KaderProfilPage functional. AuditLogPage, ManajemenJadwalPage, LaporanPage button handlers verified.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: KaderProfilPage implementation (STUB → functional) + AuditLogPage alignment</name>
  <files>
    frontend/src/pages/kader/KaderProfilPage.tsx,
    frontend/src/pages/puskesmas/AuditLogPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/kader/KaderProfilPage.tsx` — full file (18 lines stub); note it only has a placeholder div
    - `frontend/src/pages/puskesmas/AuditLogPage.tsx` — full file; note pagination state (page, setPage), prev/next button onClick handlers, filter controls, Skeleton loading state
    - `frontend/src/pages/kader/KaderDashboardPage.tsx` — lines 1-50: find how handleLogout is implemented (likely useMutation + apiClient.post('/auth/logout') + useAuthStore.clear()); copy the same pattern
    - `frontend/src/stores/useAuthStore.ts` — note user fields available (namaLengkap, role, identifier or nomorHp)
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "27:4566" (KaderProfil). Then get_design_context same params. Also get_screenshot nodeId "5:17126" (AuditLog). Study both frames.

    KADER PROFIL PAGE — replace STUB with functional implementation:

    Imports: useAuthStore, useMutation, useNavigate, apiClient, Lucide icons (User, Phone, LogOut, Shield).

    Get kader data: `const { user, clearAuth } = useAuthStore()`.

    logoutMutation: useMutation({ mutationFn: () => apiClient.post('/auth/logout'), onSuccess: () => { clearAuth(); navigate('/login', { replace: true }) }, onSettled: () => { clearAuth(); navigate('/login', { replace: true }) } }). The onSettled handles both success and error (cookie cleared by server either way).

    Layout (match Figma 27:4566):
    - Root: min-h-full bg-[#f9fafb].
    - Header: bg-[#008236] px-4 pt-10 pb-6. Title "Profil Kader" text-white font-bold text-xl. Subtitle "Kelola akun kader Posyandu" text-[#7bf1a8] text-xs.
    - Avatar section: centered, w-20 h-20 rounded-full bg-[#dcfce7] flex items-center justify-center with initials letter `<span className="text-[#008236] font-bold text-3xl">{user?.namaLengkap?.[0]?.toUpperCase() ?? 'K'}</span>`. Below: namaLengkap text-[#1e2939] font-bold text-xl. Role chip: span bg-[#f0fdf4] text-[#008236] text-xs font-semibold px-3 py-1 rounded-full "Kader Posyandu" or "Ketua Kader" based on user?.role.
    - Info cards: two cards in a column, each bg-white border border-[#f3f4f6] rounded-2xl px-4 py-3 shadow-sm:
      a. Phone: Phone icon text-[#008236] + "Nomor HP" label text-[#99a1af] text-xs + user?.identifier or user?.nomorHp text-[#1e2939] font-semibold text-sm (check what field useAuthStore exposes for the kader identifier)
      b. Role: Shield icon text-[#008236] + "Role" label + user?.role text-[#1e2939] font-semibold
    - Logout button: bg-[#fef2f2] border border-[#ffc9c9] text-[#e7000b] font-semibold rounded-[14px] py-3.5 w-full mx-4. LogOut icon. onClick: logoutMutation.mutate(). disabled when logoutMutation.isPending.

    AUDIT LOG PAGE — visual alignment + button audit:
    - Confirm pagination: ChevronLeft (prev) button has onClick={() => setPage(p => Math.max(1, p-1))} disabled={page <= 1}. ChevronRight (next) button has onClick={() => setPage(p => Math.min(meta.totalPages, p+1))} disabled={page >= meta?.totalPages}. If handlers are missing, add them.
    - Verify each AuditLogEntry row renders: waktu (formatWaktu), userId truncated, userRole Badge, aksi string, tabelTerkait, recordId truncated.
    - Skeleton shown during isLoading.
    - Match Figma 5:17126 header: bg-[#008236] with "Riwayat Aktivitas" title and subtitle.
    - If Figma shows filter controls (date range, role filter): add them as UI-only controls with useState filter state; pass as params to the useQuery queryFn.
    Do NOT change: the main useQuery for audit logs, AuditLogMeta interface, pagination logic if already correct.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - KaderProfilPage.tsx contains "useAuthStore" import (kader data from store)
    - KaderProfilPage.tsx contains "namaLengkap" property access (kader name displayed)
    - KaderProfilPage.tsx contains "logout" mutation or handler
    - KaderProfilPage.tsx contains "onClick" on Logout button calling logoutMutation.mutate or handleLogout
    - KaderProfilPage.tsx contains "bg-[#fef2f2]" on Logout button (danger style)
    - KaderProfilPage.tsx does NOT contain "Halaman sedang dikembangkan" placeholder text (stub removed)
    - AuditLogPage.tsx contains "onClick" on ChevronLeft/prev button with setPage call
    - AuditLogPage.tsx contains "onClick" on ChevronRight/next button with setPage call
    - AuditLogPage.tsx contains "disabled" condition on prev button (page <= 1)
    - AuditLogPage.tsx contains "disabled" condition on next button (page >= totalPages)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>KaderProfilPage shows real kader info from auth store with functional Logout. AuditLogPage pagination prev/next buttons have working handlers with disabled states.</done>
</task>

<task type="auto">
  <name>Task 2: ManajemenJadwalPage + LaporanPage button audit and alignment</name>
  <files>
    frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx,
    frontend/src/pages/puskesmas/LaporanPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/puskesmas/jadwal/ManajemenJadwalPage.tsx` — full file; note: dialogOpen state, setDialogOpen, BuatJadwalDialog component, JadwalCard/JadwalTable, "Buat Jadwal Baru" button onClick
    - `frontend/src/pages/puskesmas/LaporanPage.tsx` — full file; note: bulan state, setBulan onChange, download Excel handler, download PDF handler — confirm both use window.open or direct fetch with responseType blob
    - `frontend/src/components/jadwal/BuatJadwalDialog.tsx` — check: Simpan button onClick in the dialog, form fields, mutation
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "5:15526" (ManajemenJadwal). Then get_design_context same params. Also get_screenshot nodeId "5:16705" (Laporan). Study both frames.

    MANAJEMEN JADWAL PAGE — alignment + button audit:
    - Header: bg-[#008236] with "Manajemen Jadwal" title. Align to Figma.
    - "Buat Jadwal Baru" button: verify onClick={() => setDialogOpen(true) or onClick={() => setDialogOpen((d) => !d)). If the button's onClick is missing or navigates somewhere wrong, fix it. Button style: bg-[#008236] text-white rounded-[14px] py-2.5 px-4 font-semibold.
    - BuatJadwalDialog: already in frontend/src/components/jadwal/BuatJadwalDialog.tsx. Verify it opens when dialogOpen=true. The dialog's Simpan/Buat Jadwal button must call the jadwal creation mutation. If dialog's Simpan has no handler, read BuatJadwalDialog.tsx and fix it.
    - JadwalCard or JadwalTable: verify each jadwal item shows tanggal, sesi count, kuota; if Figma shows Edit/Hapus per jadwal, verify those buttons have handlers (or note as out-of-scope if not implemented in prior phases).
    - Empty state: "Belum ada jadwal" message if jadwals.length === 0.

    LAPORAN PAGE — alignment + download button audit:
    - Header: bg-[#008236] with "Laporan e-PPGBM" title. Align to Figma 5:16705.
    - Filter bulan: `<input type="month" value={bulan} onChange={(e) => setBulan(e.target.value) className="...">` — verify onChange updates bulan state and triggers re-query.
    - Stats summary: verify DashboardStats display (totalPemeriksaan, totalBalita, breakdown).
    - Download Excel button: FileSpreadsheet icon. onClick must call the download handler. Current implementation likely uses `window.open('/api/laporan/excel?bulan=' + bulan)` or fetches blob — verify it is NOT a no-op. If disabled without reason, check and fix.
    - Download PDF button: FileText icon. Same verification — confirm onClick triggers download.
    - Both download buttons: disabled when isLoading or when bulan is empty.
    - Verify both buttons have specific onClick calling a function (not decorative).

    TypeScript: if any changes needed, ensure strict mode compliance.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - ManajemenJadwalPage.tsx contains "onClick" on "Buat Jadwal Baru" button that calls setDialogOpen(true) or similar
    - ManajemenJadwalPage.tsx contains "BuatJadwalDialog" component with open={dialogOpen}
    - ManajemenJadwalPage.tsx contains "bg-[#008236]" on the Buat Jadwal Baru button
    - LaporanPage.tsx contains "onChange" on the bulan month filter input (filter state updates)
    - LaporanPage.tsx contains "onClick" on Download Excel button calling a download handler (window.open or fetch blob)
    - LaporanPage.tsx contains "onClick" on Download PDF button calling a download handler
    - LaporanPage.tsx contains "FileSpreadsheet" icon in Download Excel button area
    - LaporanPage.tsx contains "FileText" icon in Download PDF button area
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>ManajemenJadwalPage Buat Jadwal Baru opens dialog. LaporanPage Excel and PDF download buttons verified to have real onClick handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader → POST /api/auth/logout | Clears JWT httpOnly cookie |
| Puskesmas → GET /api/laporan/excel or /pdf | Downloads monthly report (scoped to puskesmasId from JWT) |
| Puskesmas → GET /api/audit-log | Reads audit log (scoped to puskesmas from JWT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14-01 | Information Disclosure | KaderProfilPage shows role from auth store | accept | Data from JWT already in browser memory (useAuthStore); no new data exposed |
| T-08-14-02 | Information Disclosure | AuditLogPage shows userId + aksi history | accept | Scoped to puskesmas's kader audit trail from Phase 5 backend; puskesmasId from JWT |
| T-08-14-03 | Information Disclosure | Laporan exports health data | accept | Scoped to puskesmasId from JWT (backend IDOR guard from Phase 5); download URL requires valid session cookie |
| T-08-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 14 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /kader/profil — shows kader name, role chip, HP, Logout button (no placeholder text)
3. Browser: click Logout on KaderProfilPage — navigates to /login
4. Browser: /puskesmas/audit-log — table renders, prev/next pagination buttons functional
5. Browser: /puskesmas/jadwal — Buat Jadwal Baru button opens dialog
6. Browser: /puskesmas/laporan — bulan filter updates, Excel and PDF buttons visible and clickable
</verification>

<success_criteria>
- UI-02: KaderProfilPage functional (from STUB) — shows kader data + working Logout
- UI-02: AuditLogPage pagination prev/next verified
- UI-02: ManajemenJadwalPage Buat Jadwal Baru verified open dialog
- UI-02: LaporanPage Excel + PDF download buttons verified to have handlers
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-14-SUMMARY.md` when done.
</output>

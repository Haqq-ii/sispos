---
phase: 08-ui-figma-alignment
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/src/modules/growth/growth.routes.ts
  - backend/src/modules/growth/growth.controller.ts
  - backend/src/modules/growth/growth.service.ts
  - frontend/src/pages/citizen/TumbuhKembangPage.tsx
  - frontend/src/pages/auth/LoginPage.tsx
  - frontend/src/pages/citizen/CitizenDashboardPage.tsx
  - frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx
  - frontend/src/pages/citizen/ChatAssistantPage.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "GET /api/growth/riwayat returns 200 for citizen role with JWT, 403 for kader/puskesmas"
    - "TumbuhKembangPage Grafik tab renders a LineChart (recharts ZScoreChart) with real data points from /growth/riwayat"
    - "TumbuhKembangPage Imunisasi tab shows a message or list instead of Fitur akan segera tersedia"
    - "Citizen screens (Login, Dashboard, antrian flow, Chat) have no spacing/color/layout deviation from Figma frames 5:5654, 5:3116, 5:2314, 5:2630, 5:2902"
  artifacts:
    - path: "backend/src/modules/growth/growth.routes.ts"
      provides: "GET /riwayat citizen-restricted route"
      contains: "requireRole('citizen')"
    - path: "backend/src/modules/growth/growth.controller.ts"
      provides: "getCitizenRiwayatHandler function"
      exports: ["getCitizenRiwayatHandler"]
    - path: "backend/src/modules/growth/growth.service.ts"
      provides: "getCitizenGrowthRiwayat(wargaId) service function"
      contains: "getCitizenGrowthRiwayat"
    - path: "frontend/src/pages/citizen/TumbuhKembangPage.tsx"
      provides: "Grafik tab with ZScoreChart using real data"
      contains: "ZScoreChart"
  key_links:
    - from: "TumbuhKembangPage.tsx (grafik tab)"
      to: "GET /api/growth/riwayat"
      via: "useQuery queryKey: ['growth', 'riwayat']"
      pattern: "growth.*riwayat"
    - from: "getCitizenRiwayatHandler"
      to: "getCitizenGrowthRiwayat"
      via: "req.user!.userId → balita lookup → pemeriksaan.findMany"
      pattern: "getCitizenGrowthRiwayat"
---

<objective>
Wave 8.2: Implement the TumbuhKembangPage grafik tab (currently a "Fitur akan segera tersedia" placeholder) using the existing ZScoreChart component. Requires a new citizen-facing backend endpoint GET /api/growth/riwayat that is IDOR-safe (filters by JWT userId). Then perform Figma alignment for all Citizen screens (Login, Register, Dashboard, antrian flow, Chat) to ensure visual match against Figma file 4DIazKntakgAGXBDYefjbD.

Purpose: UI-03 requires the Z-Score chart to be readable by demo time. UI-01 requires all citizen screens to visually match Figma. This wave delivers both.

Output: Working ZScoreChart in TumbuhKembangPage + visually aligned citizen screens.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend — GET /api/growth/riwayat citizen endpoint</name>
  <files>
    backend/src/modules/growth/growth.routes.ts,
    backend/src/modules/growth/growth.controller.ts,
    backend/src/modules/growth/growth.service.ts
  </files>

  <read_first>
    - `backend/src/modules/growth/growth.routes.ts` — existing routes structure; requireRole and authMiddleware import pattern
    - `backend/src/modules/growth/growth.controller.ts` — getPemeriksaanHistoryHandler pattern (req.params.balitaId flow)
    - `backend/src/modules/growth/growth.service.ts` — getPemeriksaanHistory(balitaId) full implementation; note that select already includes zScoreBbU, zScoreTbU, zScoreBbTb
    - `backend/src/modules/child/child.routes.ts` — requireRole('citizen') import pattern
    - `backend/src/modules/child/child.controller.ts` — getBalitaSayaHandler pattern: req.user!.userId → wargaId lookup → balita list
    - `prisma/schema.prisma` — Pemeriksaan model fields: zScoreBbU, zScoreTbU, zScoreBbTb, tanggalPemeriksaan, beratBadan, tinggiBadan, statusGizi
  </read_first>

  <action>
    In growth.service.ts, add a new exported function `getCitizenGrowthRiwayat(wargaId: string)`. The function must:
    1. Query `prisma.balita.findFirst({ where: { wargaId }, orderBy: { createdAt: 'asc' }, select: { id: true } })`. If no balita found, return empty array.
    2. Call `prisma.pemeriksaan.findMany({ where: { balitaId: balita.id }, orderBy: { tanggalPemeriksaan: 'asc' }, select: { id, tanggalPemeriksaan, beratBadan, tinggiBadan, zScoreBbU, zScoreTbU, zScoreBbTb, statusGizi, statusGiziOverride } })`.
    3. Map the result to the response shape: `{ id, tanggalPemeriksaan (as ISO string), beratBadan, tinggiBadan, zScoreBbU, zScoreTbU, zScoreBbTb, statusGizi (uses statusGiziOverride when set, otherwise statusGizi) }`.

    In growth.controller.ts, add `getCitizenRiwayatHandler`. It must:
    1. Extract `userId` from `req.user!.userId` (same as child.controller.ts getBalitaSayaHandler pattern).
    2. Call `getCitizenGrowthRiwayat(userId)`.
    3. Return `res.json({ success: true, data: result })` on success.
    4. Catch errors and forward to `next(err)`.

    In growth.routes.ts, add a new route: `growthRouter.get('/riwayat', authMiddleware, requireRole('citizen'), getCitizenRiwayatHandler)`. Place it BEFORE the existing `/balita/:balitaId/history` route to avoid any future path conflict. Import `getCitizenRiwayatHandler` from growth.controller.ts.

    Do NOT modify any existing handlers. Do NOT change `createPemeriksaan` or `getPemeriksaanHistory`.
  </action>

  <verify>
    <automated>docker compose exec sispos-backend sh -c "curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/growth/riwayat" 2>&amp;1</automated>
  </verify>

  <acceptance_criteria>
    - `GET /api/growth/riwayat` without a JWT cookie returns 401 (authMiddleware enforcing auth)
    - `GET /api/growth/riwayat` with a valid kader JWT returns 403 (requireRole('citizen') enforcing role)
    - `GET /api/growth/riwayat` with a valid citizen JWT returns 200 with `{ success: true, data: [...] }` — data is an array (may be empty if citizen has no balita)
    - growth.routes.ts contains the string `requireRole('citizen')` on the /riwayat route
    - growth.service.ts contains function named `getCitizenGrowthRiwayat` with a `prisma.balita.findFirst` call using `wargaId` in the where clause
    - TypeScript compilation passes: `docker compose exec sispos-backend npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>GET /api/growth/riwayat returns 200 for citizen JWT, 403 for kader/puskesmas JWT, 401 for unauthenticated requests. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — TumbuhKembangPage grafik + imunisasi tabs</name>
  <files>frontend/src/pages/citizen/TumbuhKembangPage.tsx</files>

  <read_first>
    - `frontend/src/pages/citizen/TumbuhKembangPage.tsx` — full current implementation; note existing RiwayatRecord interface, useQuery pattern, and tab structure
    - `frontend/src/components/kader/ZScoreChart.tsx` — ZScoreDataPoint interface: `{ tanggal: string, bbU: number | null, tbU: number | null, bbTb: number | null }`; component signature `ZScoreChart({ data: ZScoreDataPoint[] })`
    - `frontend/src/pages/kader/meja/Meja3Page.tsx` — lines 1-50 for import pattern of ZScoreChart and usePemeriksaanHistory data mapping
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 3.2 Pattern 6 (Skeleton loading), section 3.3 anti-patterns (do not replace working logic)
  </read_first>

  <action>
    Modify TumbuhKembangPage.tsx to implement the Grafik tab and improve the Imunisasi tab. All changes are additive — the Riwayat tab logic must remain unchanged.

    Step 1 — Update RiwayatRecord interface: Add fields `zScoreBbU: number | null`, `zScoreTbU: number | null`, `zScoreBbTb: number | null` alongside the existing `zScore` field. Keep `zScore` for backward compatibility but it will no longer be used in the grafik tab.

    Step 2 — Add ZScoreChart import: Import `ZScoreChart` and `ZScoreDataPoint` from `@/components/kader/ZScoreChart` at the top of the file.

    Step 3 — Add data transformation: Below the `latest` derivation, add a `grafikData` constant that maps `riwayat` to `ZScoreDataPoint[]`. The mapping function formats `tanggalPemeriksaan` to DD/MM/YY (two-digit year) using `new Date(record.tanggalPemeriksaan).toLocaleDateString('id-ID', { day: '2-digit', month: '2-digit', year: '2-digit' })`. Map `zScoreBbU` → `bbU`, `zScoreTbU` → `tbU`, `zScoreBbTb` → `bbTb`. Use null if the field is null/undefined.

    Step 4 — Implement Grafik tab: Replace the placeholder "Fitur akan segera tersedia" div for `activeTab === 'grafik'` with:
    - If `isLoading`: render `<Skeleton className="h-[260px] rounded-2xl" />`
    - If `!isLoading && grafikData.length === 0`: render a card with message "Belum ada data pemeriksaan untuk grafik. Lakukan pemeriksaan pertama di Posyandu."
    - If `grafikData.length > 0`: render a white card with `rounded-2xl border border-[#f3f4f6] shadow-sm p-4`, title "Tren Z-Score", subtitle "BB/U · TB/U · BB/TB", then `<ZScoreChart data={grafikData} />`.

    Step 5 — Improve Imunisasi tab: Replace the placeholder for `activeTab === 'imunisasi'` with a card that says "Riwayat imunisasi tersedia setelah pemeriksaan Meja 5 di Posyandu." Use text-[#1e2939] font-semibold for the heading and text-[#99a1af] text-xs for the body.

    Do NOT touch the Riwayat tab render, the useQuery, or the green header section.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - TumbuhKembangPage.tsx contains `import.*ZScoreChart` at the top
    - TumbuhKembangPage.tsx contains `ZScoreChart` in the JSX render (grep: `&lt;ZScoreChart`)
    - TumbuhKembangPage.tsx no longer renders "Fitur akan segera tersedia" string in the grafik tab (grep: file must not have `Fitur akan segera tersedia` inside the `activeTab === 'grafik'` block)
    - TypeScript compilation of frontend passes: `docker compose exec sispos-frontend npx tsc --noEmit` exits 0
    - In browser at /citizen/tumbuh-kembang with demo citizen logged in: clicking "Grafik" tab shows a recharts LineChart (not a placeholder), OR shows "Belum ada data pemeriksaan" card if no data exists (not "Fitur akan segera tersedia")
  </acceptance_criteria>

  <done>TumbuhKembangPage Grafik tab renders ZScoreChart with real seed data. Imunisasi tab shows informative message. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Citizen screens Figma visual alignment</name>
  <files>
    frontend/src/pages/auth/LoginPage.tsx,
    frontend/src/pages/citizen/CitizenDashboardPage.tsx,
    frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx,
    frontend/src/pages/citizen/ChatAssistantPage.tsx
  </files>

  <read_first>
    - `frontend/src/pages/auth/LoginPage.tsx` — current implementation
    - `frontend/src/pages/citizen/CitizenDashboardPage.tsx` — current implementation (lines 1-80 for header + antrian card pattern)
    - `frontend/src/pages/citizen/antrian/TiketAntrianPage.tsx` — current implementation
    - `frontend/src/pages/citizen/ChatAssistantPage.tsx` — current implementation
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 (design tokens), section 3.2 (component patterns), section 3.3 (anti-patterns to avoid)
  </read_first>

  <action>
    For each citizen screen listed, pull the corresponding Figma design context before making any changes. Use Figma MCP tool `get_design_context` with fileKey `4DIazKntakgAGXBDYefjbD` and the frame IDs below. Then pull `get_screenshot` for a visual reference. Compare the Figma design against the current React implementation and apply the minimum className/JSX changes needed to close visual gaps.

    Frame assignments (verify each frame ID at runtime using get_design_context; if a frame returns unexpected content, search by frame name in the file instead):
    - LoginPage.tsx: nodeId `5:5654` (citizen login) and `5:5809` (kader PIN entry) — verify both login variants match Figma spacing and button styles
    - CitizenDashboardPage.tsx: no explicit frame ID in research; pull Figma file metadata to locate the citizen dashboard frame by name "Citizen Dashboard" or "Dashboard Citizen"
    - TiketAntrianPage.tsx: nodeId `5:3116` (cetak antrian / tiket)
    - ChatAssistantPage.tsx: locate by searching Figma file for frame named "Chat" or "AI Assistant" (no confirmed frame ID)

    Visual alignment rules (per RESEARCH.md section 1.4 design tokens):
    - Primary green: bg-[#008236] (headers, primary buttons) — never use bg-green-700 or similar Tailwind named colors
    - Background: bg-[#f9fafb] on page root div
    - Cards: rounded-2xl border border-[#f3f4f6] shadow-sm
    - Buttons: rounded-[14px] for pill-style primary buttons
    - Text primary: text-[#1e2939] for headlines
    - Text muted: text-[#99a1af] for captions

    Preservation rules (per RESEARCH.md section 3.3 anti-patterns):
    - Do NOT remove Socket.IO hook calls from TiketAntrianPage (useAntrianSocket remains intact)
    - Do NOT remove mutation handlers (useMutation calls remain intact)
    - Do NOT install new npm packages — shadcn/ui components already installed are the limit
    - Do NOT use @radix-ui/react-checkbox — use native input[type=checkbox] per decisions log 2026-07-02
    - Do NOT add key prop to any MapContainer component

    After alignment, run `docker compose exec sispos-frontend npm run lint` to confirm no TypeScript errors introduced.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - LoginPage.tsx uses bg-[#008236] for the primary login button (not bg-primary, not bg-green-600)
    - CitizenDashboardPage.tsx page root div has className containing bg-[#f9fafb]
    - TiketAntrianPage.tsx contains the CountdownEstimasi component (not removed during alignment)
    - TiketAntrianPage.tsx contains useAntrianSocket import or call (not removed)
    - `docker compose exec sispos-frontend npm run lint` exits 0 (no TypeScript errors after changes)
    - Browser at http://localhost/login shows login form without console errors
    - Browser at http://localhost/citizen/dashboard (logged in as citizen) shows antrian aktif card without console errors
    - Browser at http://localhost/citizen/antrian/tiket (with active antrian) shows countdown and tiket layout without console errors
  </acceptance_criteria>

  <done>Citizen screens (Login, Dashboard, TiketAntrian, Chat) visually aligned to Figma design tokens. No new TypeScript errors introduced. No console errors on any citizen route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/growth/riwayat | New citizen-only endpoint; JWT required; returns pemeriksaan Z-Score data |
| Figma MCP → design context | Read-only Figma API call; no sensitive data leaves the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Broken Access Control (IDOR) | GET /api/growth/riwayat | mitigate | requireRole('citizen') + filter by JWT userId (wargaId); kader/puskesmas receive 403; citizen can only see their own balita's data via `WHERE wargaId = req.user.userId` |
| T-08-02-02 | Information Disclosure | Pemeriksaan Z-Score in API response | accept | Z-Score data (BB, TB, growth metrics) is not UU PDP-sensitive (not catatanKonsultasi or rekomendasiAi); those encrypted fields are excluded from the select in getCitizenGrowthRiwayat |
| T-08-02-03 | Tampering | Figma alignment className changes | accept | Only Tailwind className strings modified; no business logic altered; TypeScript compilation gate prevents type errors |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; ZScoreChart (recharts) already in package.json |
</threat_model>

<verification>
Overall phase 8.2 checks:
1. `docker compose exec sispos-backend npx tsc --noEmit` — exits 0 (backend TypeScript clean)
2. `docker compose exec sispos-frontend npm run lint` — exits 0 (frontend TypeScript clean)
3. Browser at /citizen/tumbuh-kembang → click "Grafik" tab → recharts renders or shows empty message (no "Fitur akan segera tersedia")
4. `curl -s -o /dev/null -w '%{http_code}' -H 'Cookie: ...' http://localhost/api/growth/riwayat` with citizen cookie → 200
5. `curl -s -o /dev/null -w '%{http_code}' -H 'Cookie: ...' http://localhost/api/growth/riwayat` with kader cookie → 403
</verification>

<success_criteria>
- SC-1 (partial): Citizen screens Login, Dashboard, TiketAntrian, Chat visually match Figma design tokens (color, spacing, border-radius)
- SC-4b: Z-Score chart is readable and populated with real data in TumbuhKembangPage Grafik tab
- UI-01: Citizen screens aligned to Figma file 4DIazKntakgAGXBDYefjbD
- UI-03: grafik tab renders ZScoreChart (no longer placeholder)
</success_criteria>

## Artifacts This Phase Produces

- Updated `backend/src/modules/growth/growth.routes.ts` — new GET /riwayat route
- Updated `backend/src/modules/growth/growth.controller.ts` — getCitizenRiwayatHandler
- Updated `backend/src/modules/growth/growth.service.ts` — getCitizenGrowthRiwayat function
- Updated `frontend/src/pages/citizen/TumbuhKembangPage.tsx` — grafik tab with ZScoreChart
- Updated citizen screen files (LoginPage, CitizenDashboardPage, TiketAntrianPage, ChatAssistantPage) — Figma aligned
- `.planning/phases/08-ui-figma-alignment/08-02-SUMMARY.md`

<output>
Create `.planning/phases/08-ui-figma-alignment/08-02-SUMMARY.md` when done.
</output>

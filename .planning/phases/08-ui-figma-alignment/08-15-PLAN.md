---
phase: 08-ui-figma-alignment
plan: 15
type: execute
wave: 15
depends_on: []
files_modified:
  - frontend/src/pages/LandingPage.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "LandingPage is mobile-first (max-w-sm centered or full mobile width) matching Figma 2001:691"
    - "LandingPage hero section shows app name, tagline, and primary CTA"
    - "LandingPage Masuk button navigates to /login"
    - "LandingPage Daftar Sekarang button navigates to /register"
    - "LandingPage 3-role feature cards are visible (Orang Tua, Kader, Puskesmas)"
    - "Authenticated users are still redirected to their dashboard (preserved logic)"
  artifacts:
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Mobile-first landing page matching Figma 2001:691"
      contains: "Masuk"
  key_links:
    - from: "LandingPage Masuk button"
      to: "/login route"
      via: "Link or navigate('/login')"
      pattern: "login"
    - from: "LandingPage Daftar Sekarang button"
      to: "/register route"
      via: "Link or navigate('/register')"
      pattern: "register"
---

<objective>
Wave 8.15: LandingPage Figma alignment + Puskesmas Login check.

LandingPage currently exists at 152 lines — a full desktop-first implementation with hero, features grid, and 3-role CTA cards. The Figma design at 2001:691 is mobile-first (PWA context — users arrive on mobile). The existing desktop layout may need to be converted to a mobile-first single-column layout.

The redirect logic for authenticated users (useAuthStore → navigate to role dashboard) must be preserved entirely. Only the visual layout changes.

Note: Puskesmas Login (Figma 5:13077) is the same LoginPage as citizen/kader login. The single LoginPage (already aligned in 08-05) handles all three roles. The "check" is only to confirm the Puskesmas login frame is visually covered by the current LoginPage implementation — no code change expected.

Purpose: LandingPage is the first screen a new user sees. Mobile-first alignment makes it match the PWA context.

Output: LandingPage mobile-first layout matching Figma 2001:691. Masuk and Daftar Sekarang buttons verified to navigate correctly.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LandingPage mobile-first Figma alignment (frame 2001:691)</name>
  <files>frontend/src/pages/LandingPage.tsx</files>

  <read_first>
    - `frontend/src/pages/LandingPage.tsx` — full file (152 lines); note: the hero section, features grid (3 items), role CTA cards (3 cards), Masuk and Daftar Sekarang button/link hrefs, the authenticated-redirect logic at the top (useAuthStore + useEffect or conditional navigate), any className involving `grid-cols` or `md:` responsive prefixes that indicate desktop-first layout
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 1.5 Figma frame inventory (2001:691 LandingPage note)
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:691". Then get_design_context same params. Study carefully: is it a mobile layout (single column, ~375px wide frame)? What is the hero content, section ordering, and CTA button arrangement?

    Also call get_screenshot for Puskesmas Login frame "5:13077" to do the visual check noted in the objective.

    PUSKESMAS LOGIN CHECK (no code change):
    - Compare Figma 5:13077 (Puskesmas Login) against the current LoginPage (already aligned in 08-05).
    - If the Puskesmas login frame is structurally identical to the citizen/kader login (same layout, only different placeholder text or role detection), note in this task that no further changes are needed for LoginPage.
    - If Figma 5:13077 shows a DIFFERENT layout (e.g., a separate puskesmas login form at /puskesmas/login), then note this as a gap to address in 08-16 final QA. Do NOT create new routes or pages in this task.

    LANDING PAGE — mobile-first alignment:

    PRESERVED LOGIC (do NOT change):
    - The authenticated-redirect block: useAuthStore check → navigate to /citizen/dashboard, /kader/dashboard, or /puskesmas/dashboard based on role. Keep exactly as-is.
    - All Link hrefs or navigate targets: Masuk → /login, Daftar Sekarang → /register.
    - The 3 role descriptions (Orang Tua/Citizen, Kader, Puskesmas).

    MOBILE-FIRST LAYOUT (change className values to match Figma 2001:691):
    - Remove `md:grid-cols-2`, `lg:grid-cols-3`, or any desktop-responsive grid classes. Replace all grid layouts with single-column flex-col stacks.
    - Root: `min-h-screen bg-[#f9fafb] flex flex-col items-center`.
    - Main container: `w-full max-w-sm mx-auto px-4 flex flex-col` (PWA mobile width 375px).
    - Hero section (from Figma): green header area with app logo/icon + app name "SISPOS" text-white font-bold text-2xl + tagline text-[#7bf1a8] text-sm. Background: bg-[#008236] px-4 pt-12 pb-8. Center-aligned.
    - Features/benefits section: IF Figma shows 3 feature pills/chips (antrian online, pencatatan kesehatan, AI chatbot) as a single-column list: implement as vertical list of cards `bg-white border border-[#f3f4f6] rounded-2xl px-4 py-3 shadow-sm flex items-center gap-3 mb-3`. Icon + title (text-[#1e2939] font-semibold text-sm) + description (text-[#99a1af] text-xs).
    - 3-role section: IF Figma shows role cards as vertical stack (not grid): change from grid to flex-col gap-3. Each card: bg-white border border-[#f3f4f6] rounded-2xl px-4 py-4 shadow-sm. Role name text-[#1e2939] font-semibold, role description text-[#99a1af] text-xs, role-specific icon in bg-[#f0fdf4] or role color circle.
    - CTAs (buttons at bottom or after hero):
      a. "Masuk" button: bg-[#008236] text-white rounded-[14px] py-3.5 w-full font-semibold. Use `<Link to="/login">` wrapping the button or `<button onClick={() => navigate('/login')}>`. Verify it navigates to /login.
      b. "Daftar Sekarang" button: bg-white border-2 border-[#008236] text-[#008236] rounded-[14px] py-3.5 w-full font-semibold. Navigates to /register.
    - If Figma 2001:691 layout differs substantially from the above description: ALWAYS follow the Figma screenshot, not the description above. The Figma is the source of truth.

    TypeScript: no implicit any, no new imports beyond what already exists in LandingPage.tsx. Lucide icons already imported — use them.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - LandingPage.tsx contains "SISPOS" text in the hero section (app name visible)
    - LandingPage.tsx does NOT contain "md:grid-cols" or "lg:grid-cols" classes (no desktop grid layout)
    - LandingPage.tsx contains "max-w-sm" container (mobile-first width constraint)
    - LandingPage.tsx contains "/login" as Masuk button/link target
    - LandingPage.tsx contains "/register" as Daftar Sekarang button/link target
    - LandingPage.tsx contains "onClick" or "to=" (Masuk and Daftar Sekarang are interactive, not decorative)
    - LandingPage.tsx still contains the authenticated-redirect logic (useAuthStore or navigate by role — preserved)
    - LandingPage.tsx contains "bg-[#008236]" on the Masuk button (primary green CTA)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>LandingPage mobile-first layout matching Figma 2001:691. Masuk and Daftar Sekarang buttons navigate correctly. Authenticated redirect preserved. No desktop grid classes remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → / (LandingPage) | Unauthenticated entry point — no data fetched, no API calls |
| LandingPage → /login or /register | Client-side navigation only, no server interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15-01 | Information Disclosure | LandingPage visible to unauthenticated users | accept | Only shows marketing content (app name, features, CTA); no user data or auth tokens exposed |
| T-08-15-02 | Elevation of Privilege | Authenticated user stays on landing page | accept | Redirect logic (useAuthStore) already in place from previous phases — navigates to role dashboard automatically |
| T-08-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 15 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: / (unauthenticated) — mobile-first single column layout, no wide desktop grid
3. Browser: / — "Masuk" button navigates to /login
4. Browser: / — "Daftar Sekarang" navigates to /register
5. Browser: / (as authenticated citizen) — auto-redirects to /citizen/dashboard
6. Figma check: LandingPage matches 2001:691 frame layout
</verification>

<success_criteria>
- UI-01: LandingPage aligned to Figma 2001:691 (mobile-first single column)
- UI-01: Masuk and Daftar Sekarang buttons navigate to correct routes
- Authenticated redirect logic preserved
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-15-SUMMARY.md` when done.
</output>

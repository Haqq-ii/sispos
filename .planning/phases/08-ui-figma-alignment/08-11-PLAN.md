---
phase: 08-ui-figma-alignment
plan: 11
type: execute
wave: 11
depends_on: [08-07]
files_modified:
  - frontend/src/pages/kader/meja/Meja1Page.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Meja1Page header shows Lock icon, Meja 1 title, stat badges (Hadir/Belum/Total), and progress bar"
    - "Meja1Page antrian cards have Hadir and Tangguhkan buttons with real onClick handlers"
    - "Meja1Page Daftar Manual (go-show) form has Batal and Daftarkan buttons wired to mutations"
    - "Meja1Page Tukar Meja button (already wired in 08-07) is confirmed functional with TukarMejaModal"
  artifacts:
    - path: "frontend/src/pages/kader/meja/Meja1Page.tsx"
      provides: "Figma-aligned Meja 1 with functional antrian list and go-show form"
      contains: "handleHadir"
  key_links:
    - from: "Meja1Page Hadir button"
      to: "PATCH /api/antrian/:id/hadir"
      via: "handleHadir → hadirMutation.mutate or offline enqueueOperation"
      pattern: "handleHadir"
    - from: "Meja1Page Daftarkan button"
      to: "POST /api/kader/go-show"
      via: "goShowMutation.mutate"
      pattern: "goShowMutation"
---

<objective>
Wave 8.11: Meja 1 Figma alignment (depends on 08-07 — TukarMejaModal must exist).

Meja1Page is FULL status (all logic implemented in prior phases). This plan aligns the visual design to Figma frames 2001:5551 (main antrian list) and 2001:5995 (Daftar Manual go-show form). The Tukar Meja button was already wired to TukarMejaModal in 08-07.

Key visual elements to align: header layout (Lock icon + stat badges + progress bar), antrian card design (number badge, balita name, Hadir/Tangguhkan buttons), go-show form layout.

Purpose: Meja 1 is the entry point for kader pelayanan. Visual misalignment makes the kader flow harder to demo.

Output: Meja1Page JSX updated to match Figma 2001:5551 and 2001:5995. All existing logic preserved.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ASUS/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-figma-alignment/08-RESEARCH.md
@.planning/phases/08-ui-figma-alignment/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meja1Page header + antrian card Figma alignment (frame 2001:5551)</name>
  <files>frontend/src/pages/kader/meja/Meja1Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja1Page.tsx` — full file (484 lines); identify all className values in the header section (lines 244-288), antrian card section (lines 335-413), and footer section (lines 415-481)
    - `.planning/phases/08-ui-figma-alignment/08-RESEARCH.md` — section 1.4 design tokens, section 3.2 Pattern 1 (green header)
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:5551". Then get_design_context same params. Study the header, antrian cards, and footer layout carefully.

    HEADER ALIGNMENT (bg-[#008236] section):
    - Ensure header has: Lock icon + "MEJA 1 — Pendaftaran" title text-white font-bold text-sm + "Mode Pelayanan Aktif · Layar Terkunci" subtitle text-[#b9f8cf] text-xs
    - Stat badges row: Hadir count (bg-[rgba(5,223,114,0.3)] text-[#dcfce7]), Belum count (bg-[rgba(5,223,114,0.2)] text-[#b9f8cf]), Total count (bg-[rgba(255,255,255,0.2)] text-white) — all as rounded-full pills
    - Progress bar: h-[8px] bg-[#016630] track with bg-[#05df72] fill at progressPct width
    - "N/total balita sudah hadir" progress text: text-[#b9f8cf] text-xs text-right
    - Tukar Meja button: already bg-[rgba(0,166,62,0.6)] border border-[rgba(0,201,80,0.5)] rounded-xl px-3 py-1.5 text-white text-xs font-medium with onClick={() => setShowTukarMeja(true)} from 08-07. Confirm this is intact.
    - SyncPendingBadge already rendered. Keep as-is.
    - Compare against Figma screenshot: adjust spacing, icon sizes, badge padding if Figma differs.

    ANTRIAN CARD ALIGNMENT (each AntrianItem in filtered list):
    From Figma 2001:5551, each card has:
    - Card container: bg-white rounded-[14px] border (border color varies by status). Current borderClass logic is correct — keep it.
    - Left: nomorUrut padded 2-digit, text-[#99a1af] text-sm font-semibold w-6
    - Center: namaBalita text-[#1e2939] font-bold text-sm; "{usia} bln · RT {rt}" text-[#99a1af] text-xs
    - Right action area:
      a. isBelum (menunggu/ditangguhkan): Hadir button bg-[#f0fdf4] border-[#b9f8cf] text-[#008236] rounded-full px-3 py-1.5 text-xs font-semibold with onClick calling handleHadir({ antrianId, balitaId, namaBalita }). Tangguhkan button bg-[#fff7ed] border-[#fed7aa] text-[#ea580c] rounded-full px-3 py-1.5 text-xs font-semibold with onClick calling handleTangguhkan(antrian.id).
      b. isHadir: "Hadir ✓" badge bg-[#dcfce7] text-[#008236] rounded-full px-3 py-1
      c. isTidakHadir: "Tidak Hadir" badge bg-[#f3f4f6] text-[#6a7282] rounded-full px-3 py-1 opacity-60
    - Align button sizes and spacing to Figma if different from current.

    SEARCH BAR: Keep as-is (already has working onChange → setSearchQuery).
    RT FILTER CHIPS: Keep as-is (already has onClick → setSelectedRt).

    Do NOT change: handleHadir logic, handleTangguhkan logic, offline branch, socket hook, mutation callbacks.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja1Page.tsx contains "Lock" icon import or usage in the header
    - Meja1Page.tsx contains "MEJA 1" text in header
    - Meja1Page.tsx contains "progressPct" style usage for progress bar width
    - Meja1Page.tsx contains "handleHadir" function and onClick call on Hadir button per antrian card
    - Meja1Page.tsx contains "handleTangguhkan" function and onClick call on Tangguhkan button per antrian card
    - Meja1Page.tsx contains "setShowTukarMeja(true)" on Tukar Meja button (from 08-07, preserved)
    - Meja1Page.tsx contains "onChange" on the search input (filter functional)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja1Page header, antrian cards, and filter bar visually aligned to Figma 2001:5551. All button handlers preserved and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Meja1Page Daftar Manual (go-show) form alignment (frame 2001:5995)</name>
  <files>frontend/src/pages/kader/meja/Meja1Page.tsx</files>

  <read_first>
    - `frontend/src/pages/kader/meja/Meja1Page.tsx` — footer section (lines 415-481): go-show form (showGoShow state, balitaId/wargaId inputs, Batal button, Daftarkan button), handleKeluarMeja
    - Note: the go-show form uses UUID inputs which is a dev-only UX (not great for demo). The Figma frame 2001:5995 likely shows a better form. Check the Figma.
  </read_first>

  <action>
    MANDATORY FIRST STEP: Call mcp__plugin_figma_figma__get_screenshot with fileKey "4DIazKntakgAGXBDYefjbD" and nodeId "2001:5995". Then get_design_context same params. Study the Daftar Manual form design.

    GO-SHOW FORM ALIGNMENT — the "Daftar Manual" button toggles showGoShow state (already working):
    - The Figma frame 2001:5995 likely shows a form with fields for the walk-in citizen.
    - Current implementation uses UUID inputs (goShowBalitaId, goShowWargaId) which are developer-unfriendly.
    - IF Figma shows name/NIK inputs instead of UUID: update the form fields to show more user-friendly inputs (namaBalita text input, optional NIK input). The backend POST /api/kader/go-show may need these — check what fields it accepts. If it only accepts balitaId + wargaId, keep UUID inputs but improve the label to "Nomor Antrian Manual" or use a cleaner form layout.
    - Regardless of field content: style the form container to bg-white border border-[#f3f4f6] rounded-2xl p-4 (matching Figma card pattern).
    - Form title: "Daftar Manual (Tanpa Antrian Online)" text-[#1e2939] font-semibold text-sm.
    - Inputs: bg-[#f9fafb] border border-[#e5e7eb] rounded-xl px-3 py-2.5 text-sm focus:ring-2 focus:ring-[#008236].
    - Batal button: border border-[#e5e7eb] text-[#364153] rounded-2xl py-3 font-semibold. onClick: setShowGoShow(false), clear input states. Verify this onClick is present.
    - Daftarkan button: bg-[#008236] text-white rounded-2xl py-3 font-semibold. Verify disabled when inputs empty or goShowMutation.isPending. Verify onClick calls goShowMutation.mutate({ slotId: activeSlotId, balitaId: goShowBalitaId, wargaId: goShowWargaId }).

    FOOTER BOTTOM BUTTONS:
    - "Daftar Manual" toggle button: border border-[#05df72] text-[#008236] rounded-2xl py-3 with UserPlus icon. onClick={()=>setShowGoShow(!showGoShow)}. Verify present.
    - "Selesai Meja 1" button: bg-[#fef2f2] border border-[#ffc9c9] text-[#e7000b] rounded-2xl py-3. onClick calls handleKeluarMeja. disabled when clearActiveMejaMutation.isPending. Verify present.

    Do NOT change: goShowMutation.mutate call, handleKeluarMeja logic, clearActiveMejaMutation.
  </action>

  <verify>
    <automated>docker compose exec sispos-frontend npm run lint 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Meja1Page.tsx contains "onClick={() => setShowGoShow(!showGoShow)" or equivalent on the Daftar Manual toggle button
    - Meja1Page.tsx contains "UserPlus" icon in the Daftar Manual button
    - Meja1Page.tsx contains "onClick" on Batal button inside go-show form that sets showGoShow false
    - Meja1Page.tsx contains "goShowMutation.mutate" call on Daftarkan button onClick
    - Meja1Page.tsx contains "disabled" condition on Daftarkan button (empty inputs or isPending)
    - Meja1Page.tsx contains "handleKeluarMeja" as onClick on "Selesai Meja 1" button
    - Meja1Page.tsx contains "bg-[#fef2f2]" on Selesai Meja 1 button (danger style)
    - docker compose exec sispos-frontend npm run lint exits 0
  </acceptance_criteria>

  <done>Meja1Page Daftar Manual go-show form and footer buttons visually aligned to Figma 2001:5995. All handlers verified functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kader → PATCH /api/antrian/:id/hadir | Kader marks balita as present (authorized kader action) |
| Kader → POST /api/kader/go-show | Kader creates walk-in antrian (no online registration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-01 | Tampering | go-show mutation with arbitrary UUID inputs | accept | Backend POST /api/kader/go-show validates balitaId and wargaId exist in DB and belong to same posyandu; IDOR guard from Phase 3 |
| T-08-11-02 | Spoofing | Hadir mutation without meja lock | accept | Meja lock (TukarMejaModal from 08-07) prevents unauthorized meja access; once inside, kader actions are valid |
| T-08-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
Wave 11 gate:
1. docker compose exec sispos-frontend npm run lint exits 0
2. Browser: /kader/meja/1 — header shows Lock icon, stat badges, green progress bar
3. Browser: antrian card shows Hadir and Tangguhkan buttons for menunggu status
4. Browser: Daftar Manual button toggles form panel; Batal closes it; Daftarkan has disabled state when inputs empty
5. Browser: Tukar Meja button opens TukarMejaModal (from 08-07)
6. Browser: Selesai Meja 1 button at bottom calls clearActiveMeja
</verification>

<success_criteria>
- UI-02: Meja1Page visually aligned to Figma 2001:5551 and 2001:5995
- All button handlers verified functional (handleHadir, handleTangguhkan, goShowMutation, handleKeluarMeja)
- TukarMejaModal integration from 08-07 preserved
</success_criteria>

<output>
Create `.planning/phases/08-ui-figma-alignment/08-11-SUMMARY.md` when done.
</output>
